# 내부 검증 로그 (Internal Verification Log) — unit-7-test.md

## 대상 산출물
- 파일: `docs/harness/units/unit-7-test.md` (+ `docs/harness/traceability.md` REQ-007 행 갱신)
- 작성 에이전트: `06-unit-tester`
- 적용 Tier: High (규칙 B 원문 — 최소 2회, 결함 0건까지 반복)
- 버전: v0(초안) → v1(1차 수정) → v2(2차 수정) → v3(최종)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 06 작성자 본인 — 결과서의 모든 수치·주장을 실행 원자료(`.harness-tmp/u7t_s1~s7.out`, 워커/uvicorn 로그, DB 조회)와 대조
- 일시: 2026-09-20 (재개 후, 결과서 초안 직후)
- 체크리스트
  - [x] 입력 계약 반영: 노트 §2·§3·§4~7, 코드 전 파일, 설계 §1.3/§2.4/§3/§4.3/§4.4/§5.1/§5.4/§6.3, DEC 목록, 템플릿·선례 확인
  - [x] 출력 계약: 템플릿 1~10절 전부 존재(5·8·10절 "미해당" 없음), 인수조건 AC1~6 각각 TC와 추적표로 1:1 연결, 정상/경계/예외/권한 경계 케이스 포함
  - [x] 상위 산출물과 모순 없음: 설계 §6.3의 "control 화이트리스트 밖 값은 none으로 치환"과 구현("거부+재시도+폴백")의 차이를 TC-006 비고에 편차로 기록
  - [ ] 추측/부정확 서술 점검 → **결함 발견(아래)**
  - [x] 20년차 관점 구조 결함: 결함 11건 ↔ 실패 TC 11건 1:1 매핑 확인
  - [ ] 오탈자/형식/집계 → **결함 발견(아래)**
- 발견된 결함 목록:
  1. TC-003 "미치환 플레이스홀더 9건 중 8건" — 원자료 재계수 결과 7건(S1 A·B, S2 R0, 추가표본 1·2·4·5). 수치 오기.
  2. TC-005 셀이 표본 22건 안의 관측과 표본 밖 관측(영어 응답=프로브, "로깅→스트레스 관리" 환각=S3b)을 뒤섞어 기술.
  3. TC-026 "3회 시나리오 모두" — 실제 검증은 S1·S3b 2개 시나리오.
  4. TC-028 "500·1500자 정상(질문형 응답)" — 500자 응답은 답변 반복 평서문이라 "질문형"이라 쓸 수 없음. 연쇄 폴백 횟수 서술("4턴")도 원자료(3,360자·2,240자·프로브 2건 = 연속 4회 폴백)와 표현이 불일치.
  5. TC-035 "Kubernetes 답변 2턴의 question_id가 캐시 질문" — 실제로 무관한 것은 Kubernetes 턴 1건(Redis 캐시 턴은 정상 매칭).
  6. TC-045 "3회 세션 중 1개 세션" — 실제는 S3 세션 3/3 낭독, S3b 세션 0/6 낭독(정확히 재기술 필요).
  7. TC 집계 문단이 초안 상태로 뒤엉킴(32/13/11/35 혼재, 존재하지 않는 "결합 표기 조정").
  8. §3/§7 프로세스 목록 오기(27432 중복, 실수로 기동한 워커의 존재와 경위 누락).
  9. §10이 검증 수행 전에 "3차까지 결함 0건 도달"을 미리 서술 — 검증 결과를 선행 주장(허위 위험).
  10. TC-044/DEF-011이 "LAN에서 구독·위조 가능"을 단정 — 실제 LAN 경유 실측은 PING·DBSIZE까지이고 위조 publish는 로컬호스트에서 확인.
  11. §7 강제 중단 서술의 "워커도 살아 있어야 하는 시점" 근거가 모호.
- 조치 내용: 위 1~11 전부 수정(수치는 원자료 재계수, 서술은 실제 실행 범위로 한정, §10은 검증 완료 후 실제 결과로 대체) → v1

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
> "오늘 처음 이 결과서를 받은 심사자(다음 단계 07/08 담당 또는 사용자)"가 표만 보고 재현·판단할 수 있는가, 판정이 근거에 비해 부풀려지지 않았는가를 의심하며 재검토.
- 검증자(역할): 독립 심사자(07 통합테스터/사용자 관점)
- 일시: 2026-09-20 (v1 수정 후)
- 체크리스트
  - [x] 1차 지적 항목 반영 확인: 위 1~11 모두 문서에 반영됨을 본문 재독으로 확인
  - [ ] 엣지 케이스/예외 누락 → **결함 발견(아래)**
  - [ ] 문서만 보고 추가 질문 없이 재현/판단 가능한가 → **결함 발견(아래)**
  - [x] 비가역 결정(스키마 유니크 제약, 모델 승격)은 06이 임의로 정하지 않고 8절 Q1~Q5로 상신
  - [x] 판정 부풀림 점검: "AC 충족 PASS"와 "품질/신뢰성 FAIL"을 분리 표기, 통계는 표본 수·정규식 기준을 명시. 보안 위험은 unit-8 범위 판정이 아니라 관측으로 인계(단 unit-7 자체 구성인 Redis 노출은 DEF-011로 결함 처리 — 근거: 이 유닛이 도입한 compose 서비스이며 사용자 지시가 로컬 포트 노출 범위를 명시적으로 요구)
- 발견된 결함 목록:
  1. TC-018 ③ 뒤에 이어 수행한 CPU 생성 호출이 예외로 끝난 사실을 표에서 누락 — 독자가 "CPU 생성이 실패했나"로 오해할 수 있음(테스트 프롬프트 결함으로 인한 스키마 위반이며 CPU 생성 성공은 TC-016에서 확인).
  2. DEF-005 설명이 "≳2,000~2,500자"라고 써서 실측(1,500자 정상 / 2,500자 실패, 사이 구간 미측정)보다 정밀해 보임.
  3. DEF-002의 발생 조건(현실성)이 없어 심각도 판단 근거가 부족 — 프런트 12초 타임아웃 < 콜드스타트(노트 §3)라는 연결고리를 명시해야 함.
  4. DEF-003이 표본 밖 관측(영어 응답=프로브)을 본표본 통계와 같은 문장에 나열.
  5. 테스트 스크립트가 Teardown에서 삭제되므로 재현성이 표 서술에만 의존 — 결과서에 "스크립트는 일회성이며 절차는 실행 절차 열이 전부"임을 명시하지 않음(재현 가능성 기술의 한계 고지 누락).
  6. 판정(9절)이 CONDITIONAL PASS인 이유(FAIL이 아닌 근거)가 조건은 있으나 설명이 짧음 — AC 충족과 미해결 결함의 관계를 심사자가 곧바로 알 수 있어야 함.
- 조치 내용: 1~4는 표/결함표 문구 수정, 5는 §3 전제 조건에 "테스트 스크립트는 `.harness-tmp` 일회성(삭제됨), 재현은 4절 실행 절차 열과 결함표 재현 절차로 수행"을 명시, 6은 §9에 판정 근거 문장 추가 → v2

## 3차 검증 (반복 — 결함 0건 확인 라운드)
- 검증자(역할): 독립 심사자 재검토 + 기계적 정합성 점검
- 일시: 2026-09-20 (v2 수정 후, Teardown 완료 후)
- 점검 내용과 결과
  - [x] 기계 점검: `unit-7-test.md`의 `TC-` 행 47개, PASS 35 / FAIL 11 / OBS 1 산술 일치(스크립트로 열 파싱 확인). DEF 11건 = FAIL TC 11건 1:1(DEF-001↔TC-003, 002↔030, 003↔005, 004↔045, 005↔028, 006↔039, 007↔035, 008↔014, 009↔041, 010↔042, 011↔044). 심각도 집계 High 2/Medium 6/Low 3 = 11.
  - [x] 인수조건 추적: AC1~AC6 각각 최소 1개의 PASS 근거 TC + 필요 시 부가 FAIL TC 연결.
  - [x] 노트 §2·§3 이월 항목 (a)~(h)가 각각 TC에 매핑되고 실제 수행됨(5절).
  - [x] 규칙 K: `.harness-tmp/u7t_*` 삭제 후 존재 0개, 기존 잔여물(venv_05_unit5/6/7/9/12/13, venv_06_unit5, unit9/12/13_server.log) 미접촉, `bin_vulkan/llama-server.exe` 존재·sha256 동일·`.bak` 없음, DB/Redis/WAV 기준선 일치, 06 프로세스 없음 — 7절의 각 문장에 대응하는 재확인 명령을 실제로 실행했다.
  - [x] `git status` 전문이 7절에 요약 없이 첨부되고, 06 산출물/unit-7 변경/병렬 타 유닛 변경이 구분되어 있음. 6·7절 placeholder(`@@...@@`) 잔존 0건.
  - [x] traceability.md는 REQ-007 행만 수정(git diff상 이 파일의 변경 행이 1행), 열 수(파이프 10개)가 인접 행과 동일.
  - [x] 새 결함 없음.
- 발견된 결함 목록: 없음 (0건)
- 조치 내용: 없음 → v3 최종

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료 — 1차 결함 11건, 2차 결함 6건 수정 후 3차에서 0건) — **이 검증 로그가 대상으로 삼은 산출물(테스트 결과서 자체)의 검증 통과**이며, 테스트 대상(unit-7)의 판정은 결과서 9절의 **CONDITIONAL PASS**(Open 결함 11건)다. 두 판정을 혼동하지 않는다.
- [ ] PASS (Low 등급 2차 생략) — 해당 없음(Tier High)
- [ ] FAIL
