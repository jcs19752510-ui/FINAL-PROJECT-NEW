# 의사결정 로그 (Decision Log)

> 규칙 A(모르면 질문)에 따라 사용자에게 질문하고 답을 받았거나, 규칙 A-3(비가역적 결정)에 따라 에이전트가 근거를 갖고 스스로 결정을 내린 경우 반드시 여기에 append한다. 절대 삭제/수정하지 않고 append-only로 유지한다.

| ID | 일시 | 단계 | 질문/이슈 | 결정 내용 | 결정자 (사용자 응답 / 에이전트 자체판단) | 비가역성 | 영향받는 산출물 |
|----|------|------|-----------|-----------|------------------------------------------|----------|------------------|
| DEC-001 | 2026-09-18 | 0(프로젝트 시작) | MCP(GitHub/Playwright/Sentry/Slack 등) 연동 여부 | 연동 안 함. 하네스는 MCP 없이도 100% 동작하므로 기존 방식(코드/문서/Bash 기반)으로 전 단계 진행 | 사용자 응답 | Low | 전 단계 공통 |
| DEC-002 | 2026-09-18 | 0(프로젝트 시작) | 프로젝트 위험도 등급(Tier) | **High로 확정.** 근거: (1) 규칙 J — 서비스 핵심 기능이 LLM 기반 AI 면접관(적응형 꼬리질문, RAG, 자동 채점)이라 AI/LLM 기능 내장에 해당. (2) 지원자의 얼굴 영상·음성 등 생체 개인정보를 처리. 사용자는 "로컬서버 내부 테스트용이라 개인정보 무관, 가장 낮은 등급으로 빠르게 진행"을 요청했으나, ORCHESTRATOR.md 1장 "Tier" 정의상 규칙 I/J 해당 프로젝트는 배포 범위(로컬/내부)와 무관하게 완화 조항이 전혀 적용되지 않는 High로 자동 분류되며 에이전트가 임의로 낮출 수 없음. 이 사실을 사용자에게 설명 후 확인 절차 진행. **개발 속도 요구는 Tier가 아닌 Speed Track(L1~L5, DEC-003)으로 해소.** | 사용자 응답 + 에이전트 규칙 적용(하드 룰) | High | 전 단계 공통, 특히 6·7단계 검증 강도, 9단계 보안검증 |
| DEC-003 | 2026-09-18 | 0(프로젝트 시작) | 개발 속도 우선순위를 어떤 하네스 장치로 반영할지 | 각 업무단위(feature)는 기본값 **Speed Track L1(최고속)**로 개발 착수. 06 단위테스트는 경량(정상 경로 1~2케이스), 07 통합테스트는 "L1 부채"로 등록해 미실행. 단, 08(전체 시스템 테스트) 착수 전 모든 feature의 부채(07 미실행분)를 반드시 정산해야 하며, 09(보안검증)·규칙 J의 안전장치(프롬프트 인젝션 방어·출력 새니타이즈·도구 권한 최소화·레이트리밋)는 트랙과 무관하게 예외 없이 통과해야 완료 처리됨. 사용자가 특정 기능(인증, 결제성 로직 등)에 대해 다른 트랙을 지정하면 그 유닛에 한해 예외 적용 | 사용자 응답(개발속도 최우선 요청) + 에이전트 제안 | Medium | 5~8단계 전체 |
| DEC-004 | 2026-09-18 | 0(프로젝트 시작) | 메인 DB: 계획서 원안(Oracle, 유료 상용 DB)을 로컬서버/소규모 환경에 맞게 대체할지 | PostgreSQL(오픈소스)로 대체 확정. Oracle 라이선스 비용·설치 복잡도가 로컬서버 소규모 프로젝트에 부적합 | 사용자 응답 | High | 03 설계서, ERD, 05 구현 |
| DEC-005 | 2026-09-18 | 0(프로젝트 시작) | 계획서 명시 유료 AI API(OpenAI/GPT-4V, Deepgram, Hume AI, ElevenLabs, Pinecone) 예산·키 보유 여부 | 예산 없음. 전부 오픈소스/로컬 대안으로 대체: OpenAI(LLM/비전)→로컬 오픈소스 LLM, Deepgram(STT)→Whisper(로컬), Hume AI/ElevenLabs(TTS)→오픈소스 TTS, Pinecone(벡터DB)→pgvector 또는 Chroma. 구체적 모델/라이브러리 선정은 1~3단계(트렌드분석/기획/설계)에서 확정 | 사용자 응답 | High | 03 설계서(AI 파이프라인), 05 구현 |
| DEC-006 | 2026-09-18 | 0(프로젝트 시작) | 계획서 원안(동시접속 500명, Kubernetes 오토스케일링) vs 실제 타깃(50명) | 50명 동시접속 기준으로 REQ-N-002 재정의. 로컬서버 단일 인스턴스 기준으로 설계하며, Kubernetes/오토스케일링 등 클라우드 확장성 요구사항은 제거 | 사용자 응답 | High | 03 설계서, 08 성능테스트 기준 |
| DEC-007 | 2026-09-18 | 0(프로젝트 시작) | "로컬서버에만 진행"의 정확한 의미 | 자체/사내 소유 로컬서버에서 개발·운영하며, 인터넷 접속 및 외부 API 호출은 전면 가능함. AWS/GCP 등 퍼블릭 클라우드 배포 및 Kubernetes 오토스케일링만 사용하지 않는다는 의미 (완전 폐쇄망이 아님) | 사용자 응답 | High | 03 설계서(배포 아키텍처), 10 배포테스트 |
| DEC-008 | 2026-09-18 | 2(기획서) | 원 계획서 REQ-F-001~007을 예산없음/로컬서버/50명/L1 제약 안에서 전부 In-Scope로 넣을 수 없어, 어떤 것을 Out-of-Scope로 뺄지 결정 필요 | 표정기반 감정분석(DeepFace), 음성 운율(Prosody) 분석, 실시간 자동 끼어들기(VAD turn-taking), 화이트보드 AI 비전 자동분석(GPT-4V 대체), 코드 실제 실행/자동채점(샌드박스)을 MVP Out-of-Scope로 확정. 근거: (1) 실측 GPU가 4GB VRAM(DEC-009)로 실시간 멀티모달 분석에 부적합, (2) EU AI Act가 면접 감정인식 AI를 금지관행으로 논의 중이고 국내 개인정보보호법상 생체인식정보 리스크 존재(01단계 §5.1·5.3), (3) L1 속도트랙과 무거운 실시간 파이프라인의 궁합이 나쁨, (4) 임의 코드실행은 원 계획서에 없던 신규 보안위험. `02-planning.md` §4.3·traceability.md REQ-018~022에 반영 | 에이전트 자체판단(오케스트레이터 지시사항에 따라 우선순위 재산정은 02단계 권한으로 위임됨, 규칙 D) | Medium (설계 이후 되돌리기 어려워지나, 후속 Phase에서 REQ-ID 추가로 재도입 가능하므로 완전 비가역은 아님) | 02-planning.md, traceability.md, 이후 3~9단계 |
| DEC-009 | 2026-09-18 | 2(기획서) | 01단계에서 Critical로 남은 "로컬서버 하드웨어 스펙 미확인"을 02단계가 어떻게 처리할지 | 별도 확인 경로가 없어, 현재 작업 환경(Bash `nvidia-smi`, PowerShell `Get-CimInstance`로 직접 실측)을 성능설계 최소 기준선으로 채택: Intel i5-1135G7(4코어/8스레드), RAM 32GB, GPU GTX 1650 Ti(VRAM 4GB). 이 머신이 실제 운영 로컬서버와 동일한지는 확인되지 않았음을 명시하고, 3단계에서 실제 운영서버 스펙을 재확인하도록 리스크로 남김(`02-planning.md` §0, §8 A1, §7 리스크 10) | 에이전트 자체판단(실측 데이터 기반) | Low (가정일 뿐이며 실제 서버 확인 시 즉시 교체 가능) | 02-planning.md, 03 설계서(성능 설계 기준) |
| DEC-010 | 2026-09-18 | 2(기획서) | 원 계획서의 "End-to-End Latency 800ms~1.5s" 목표를 오픈소스/로컬 스택에서 그대로 유지할지 | 폐기하고 턴제(발화종료→STT→LLM→TTS 완료) 기준의 잠정 KPI(P95 8초, 텍스트 전용은 3초)로 대체. 정확한 수치는 3단계 실측 벤치마크로 재검증·확정 예정 | 에이전트 자체판단(01단계 §2.3·§8 시사점 3번의 명시적 지시에 따름) | Medium (성공지표 변경, 3단계 벤치마크 결과에 따라 재조정 가능) | 02-planning.md §5, 08 성능테스트 기준 |
| DEC-011 | 2026-09-18 | 2(기획서) | REQ-N-003(GDPR/CCPA 등 국제 규제 준수)을 로컬/국내 서비스에도 그대로 적용할지 | 서비스 대상을 국내(한국어/한국 채용시장)로 한정한다는 가정(A5) 하에, GDPR/CCPA 직접 준수는 범위에서 제외하고 한국 개인정보보호법(생체정보·자동화된 결정) 대응(REQ-029~034)으로 대체 | 에이전트 자체판단(로컬서버·국내 채용시장 전제에 근거) | Medium (해외 진출 논의 시 재검토 필요) | 02-planning.md §4.3·§6.1, traceability.md REQ-028 |
| DEC-012 | 2026-09-18 | 2(기획서) | 지원자(Candidate)용 기능과 채용담당자(Recruiter)용 기능 중 MVP 우선순위 | 지원자 연습 경험을 Must로 우선하고, 채용담당자 기능(리포트 뷰어, 질문지 커스터마이징)은 Should로 최소화 | 에이전트 자체판단(01단계 §3 경쟁환경 조사 — 국내 시장이 이미 두 축으로 분화되어 있다는 근거) | Low (사용자 피드백에 따라 8단계 UAT 이후 재조정 가능) | 02-planning.md §2, §4.2 |
| DEC-013 | 2026-09-18 | 2→3(기획서→설계 전환) | DEC-009에서 "가정"으로 채택한 로컬서버 하드웨어(i5-1135G7/RAM32GB/GTX1650Ti 4GB VRAM)가 실제 운영 서버와 동일한지 확인 필요 | **확인됨 — 개발용 PC = 운영용 서버, 동일 장비.** DEC-009의 실측치를 가정이 아닌 확정 사양으로 격상. 단, 이 확정으로 인해 "GPU VRAM 4GB로 50명 동시접속 시 실시간 AI(STT/LLM/TTS) 처리가 물리적으로 가능한가"라는 별도 실현가능성 문제가 발생해 추가 확인 질문 진행(DEC-014) | 사용자 응답 | High | 03 설계서 전체(모델 크기, 동시처리 아키텍처, 큐잉 방식), 08 성능테스트 기준 |
| DEC-014 | 2026-09-18 | 2→3(기획서→설계 전환) | "동시접속 50명"의 실제 의미 — GPU 1장(VRAM 4GB)으로는 AI(STT/LLM/TTS) 동시 병렬 처리가 불가능함을 설명하고 재확인 | **확정: 50명은 웹소켓/세션 동시 접속·대기 인원 기준이며, AI 처리(STT→LLM→TTS)는 큐(Queue) 기반 순차 처리로 구현.** REQ-N-002(동시접속)를 "50명 동시 접속 허용 + GPU 자원은 큐로 직렬화, 대기시간 UI로 노출"로 재정의. 이 결정은 3단계 아키텍처(큐/워커 구조)와 08단계 성능테스트 시나리오(대기열 길이·평균 대기시간 측정)에 직접 반영되어야 함 | 사용자 응답 | High | 03 설계서(동시처리 아키텍처), 08 성능테스트 기준, traceability.md REQ-N-002 |
| DEC-015 | 2026-09-18 | 3(설계서) | 원 계획서(PDF §4.1)는 REQ-N-001(초저지연) 달성을 위해 WebRTC + SFU/미디어서버(aiortc, Signaling Server) 구조를 전제했음. 이를 그대로 설계에 승계할지 | **WebRTC/미디어서버 계층을 설계에서 전부 제거하고 단순 HTTP(S) 업로드 + WebSocket 상태 push로 대체.** 근거: 02-planning §4.2에서 이미 REQ-004(음성)를 "턴제, 실시간 스트리밍 아님"으로 재정의했고 REQ-F-003(VAD 실시간 끼어들기)은 REQ-020으로 Out-of-Scope 확정되어, 서버가 연속 미디어 스트림을 실시간 수신·분석해야 할 필요 자체가 사라졌음. WebRTC/SFU/TURN 서버 유지는 불필요한 인프라 복잡도이며 L1 속도트랙(DEC-003)과 배치됨 | 에이전트 자체판단(02-planning의 기존 재정의 결정을 아키텍처 계층에 그대로 반영한 것으로, 새로운 제품 동작 변경이 아님) | Medium (나중에 실시간 끼어들기 재도입 시 미디어서버 계층 재설계 필요하나, REQ-F-003 자체가 구현된 적 없어 "제거"가 아닌 "범위조정") | 03-system-design.md §1.1·§2.2, 04 디자인서(음성 UI 흐름) |
| DEC-016 | 2026-09-18 | 3(설계서) | LLM 최종 후보 선정 — 02-planning §6.1이 예시로 든 Llama 3.1 8B급/Qwen2.5 7B급 GGUF 양자화판 중 무엇을 1차 후보로 확정할지, 실제 VRAM에 맞는지 | **Qwen2.5-Instruct 계열 GGUF(llama.cpp)를 1차 후보로 확정.** MVP 기본값은 Qwen2.5-3B-Instruct Q4_K_M(약 2.1GB, 전량 GPU 상주 가능 — 실측 가용 VRAM 3387MiB 대비 안전마진 확보), 품질 부족 시 Qwen2.5-7B-Instruct Q4_K_M(약 4.7GB, 부분 GPU 오프로드 필요)으로 업그레이드하는 2단 옵션으로 설계. **단, Qwen2.5 시리즈는 파라미터 크기별로 라이선스 조항이 다를 수 있다는 보고가 있어 최종 라이선스는 "확인 필요" 상태 — 05단계 착수 전 HuggingFace 모델 카드 LICENSE 원문 재확인이 선행 게이트다.** LLM 실제 GPU 벤치마크는 이 세션에서 llama-cpp-python 설치를 시도했으나 (1) Windows MAX_PATH(260자) 제한으로 소스빌드 실패, (2) Python 3.13용 프리빌드 휠 부재로 실측 불가 — 공개 커뮤니티 벤치마크 수치와 VRAM 수학적 계산으로 대체 추정함(03-system-design.md §5.1) | 에이전트 자체판단(실측 시도 후 환경 제약으로 대체 추정, 근거 명시) | High (LLM 선택은 프롬프트/평가 스키마 설계 전반에 영향, 되돌리기 어려움) | 03-system-design.md §2.4·§5.1, 05단계 구현 착수 전 라이선스 재확인 게이트 |
| DEC-017 | 2026-09-18 | 3(설계서) | 02-planning §6.1/§7 리스크6이 "XTTS-v2(CPML 비상업) 회피 목적으로 Piper(MIT) 우선 검토"를 권고했는데, 실제 Piper를 설치·실측하는 과정에서 라이선스 정보가 다르게 확인됨 | **신규 리스크 발견 및 대응 확정.** 실측 결과 현재 PyPI 배포판(`piper-tts` v1.8.0, `OHF-voice/piper1-gpl` 기반)은 `License: GPL-3.0-or-later`로 확인됨(02-planning이 인용한 "Piper=MIT"는 archive된 구버전 rhasspy/piper 기준 정보였을 가능성이 높음). 대응: (1) Piper를 별도 프로세스/사이드카로 격리해 정적 링크 없이 subprocess/파일 인터페이스로만 연동, (2) TTS 모듈을 어댑터 인터페이스로 추상화해 즉시 교체 가능하게 설계, (3) **실제 서비스 공개 전 반드시 법무 검토 필요 — 이 설계서가 법적 판단을 대신하지 않음**, (4) 리스크 확정 시 1순위 대체 후보는 Fish Speech(Apache 2.0) | 에이전트 자체판단(실측 중 신규 사실 발견, 규칙 A/I 원칙에 따라 확인 필요로 명시하고 완화책과 함께 진행) | Medium (TTS 엔진 교체는 어댑터 설계로 완화되어 있어 완전 비가역은 아님) | 03-system-design.md §2.5·§8, 09단계 보안검증(라이선스 컴플라이언스 항목) |
| DEC-018 | 2026-09-18 | 3(설계서) | DEC-014가 "GPU를 STT/LLM/TTS가 순차로 나눠 써야 한다"고 전제했는데, 실측 결과가 이 전제와 다르면 어떻게 반영할지 | **STT(faster-whisper)·TTS(Piper) 모두 CPU만으로 실시간 대비 4.5배·18배 속도가 실측되어(VRAM 사용 0), GPU는 LLM 추론 전용으로 단순화 확정.** 다만 MVP 단계에서는 구현 단순성을 위해 STT→LLM→TTS를 하나의 워커 job으로 묶어 순차 처리하는 기본 설계(DEC-014의 큐 구조)는 그대로 유지하고, STT/TTS 전용 별도 CPU 워커 풀 분리는 08단계 성능테스트 후 병목이 확인되면 검토하는 옵션으로 남김 | 에이전트 자체판단(본 세션에서 직접 실행한 실측 데이터 근거) | Low (아키텍처 단순화 방향이며, 필요 시 08단계 이후 워커 풀 분리로 확장 가능) | 03-system-design.md §1.3·§2.3·§2.5 |
| DEC-019 | 2026-09-18 | 3(설계서) | Task Queue 워커 구성 방식 — Celery 기본 실행 풀(prefork)을 그대로 쓸지 | **Celery 워커를 `--concurrency=1 --pool=solo`로 확정.** 근거: GPU/CUDA 컨텍스트는 프로세스 fork와 호환성 문제가 잘 알려져 있어(fork 후 자식 프로세스 CUDA 재초기화 오류), 기본 `prefork` 풀 사용 시 GPU 초기화가 깨질 위험이 있음. 워커 수를 2개 이상으로 늘리는 것은 GPU 1장 제약과 직접 모순되므로 인프라 투자(GPU 추가) 없이는 금지 | 에이전트 자체판단(GPU 실무 운영에서 잘 알려진 제약 근거) | Medium (워커 실행 방식 변경은 배포 스크립트/운영 절차에 영향) | 03-system-design.md §1.3, 10단계 배포테스트 |
| DEC-020 | 2026-09-18 | 3(설계서) | DEC-010 잠정 KPI(음성 P95 8초/텍스트 3초)를 3단계 실측/추정으로 재검증한 결과가 목표에 크게 미달할 경우 어떻게 처리할지 | **잠정치를 2차로 재조정: 음성 턴 P95 ≤20초, 텍스트 전용 P95 ≤8초.** 근거: 실측된 STT(약 6.7초/30초 답변 환산)·TTS(약 0.5~1.5초) + 공개벤치마크·VRAM 수학 기반 추정 LLM(3B 기준 4~9초, 7B 부분오프로드 기준 8~15초)을 합산. **이 수치도 "확정"이 아니라 2차 잠정치이며, 05단계에서 llama-cpp-python 실제 GPU 실측이 완료되어야 최종 확정된다** — 규칙 F(피드백 루프) 대상으로 남김 | 에이전트 자체판단(01단계 §8/02단계 A2가 이미 예정한 "3단계 실측 재검증"을 수행한 결과) | Medium (성공지표 2차 변경, 05단계 실측으로 3차 재조정 가능) | 03-system-design.md §5.1, 08 성능테스트 기준, 02-planning.md §5(향후 갱신 필요) |
| DEC-021 | 2026-09-19 | 3→4(설계→디자인 전환) | DEC-017이 제시한 Piper(GPL-3.0-or-later) 처리 방향(격리+어댑터 vs 즉시 Fish Speech 교체)을 사용자에게 최종 확인 | **지금은 Piper 유지 + 사이드카/어댑터 구조로 진행 확정.** 로컬 내부 개발·테스트 단계에서는 GPL 배포 조건이 발동하지 않음. 실제 외부 배포/상용화 시점에는 법무 검토 또는 Fish Speech(Apache-2.0) 교체를 재검토하기로 확인 | 사용자 응답 | Medium | 03-system-design.md §2.5, 05단계 TTS 어댑터 구현, 향후 상용화 시 재검토 항목 |
| DEC-022 | 2026-09-19 | 4(디자인서) | 04단계 UX 설계 중 03단계 API/데이터 모델만으로는 완전히 해소되지 않는 화면 요구사항 8건을 발견 — 화면을 어떻게 그릴지(없는 API를 있는 것처럼 그릴지, 우회할지) 결정 필요 | **없는 데이터/동작을 있는 것처럼 그리지 않고, 각 화면에 그레이스풀 디그레이드 방안을 명시한 채 "확인 필요" 항목으로 남긴다**(03단계의 Qwen2.5/Piper 라이선스 "확인 필요" 선례와 동일 원칙). 8건 요약: (1)(2) 지원자 본인 면접목록/동의이력/삭제요청상태 조회 GET 부재, (3)(4) 코드제출물/화이트보드 재조회 GET 부재(세션 재개 시 미복원 가능), (5) 지원자용 리포트완료 비동기 알림 경로 부재, (6) **[정책 이슈, 가장 중요]** 03-system-design §6.2가 세션 시작 시 생체정보(음성) 동의를 텍스트 전용 사용자에게도 강제하도록 설계되어 있어(`/start`가 `biometric_voice` 동의 없으면 무조건 403) 개인정보보호법의 자유로운 동의 원칙과 긴장 관계가 있을 수 있고, 진행 중 세션에서 동의를 철회했을 때 서버가 실제로 음성 처리를 차단하는지도 03단계에 명시되어 있지 않음, (7) STAR 리포트가 `EVALUATION_REPORTS.summary_text` 단일 필드뿐이라 상황/과제/행동/결과를 데이터 레벨에서 분리할 수 없고 새니타이즈(REQ-036) 대상 필드 목록에도 리포트 필드가 명시되어 있지 않음, (8) 세션 시작 직후 AI 첫 질문이 전달되는 API/WS 경로가 03단계에 정의되어 있지 않음. 04-ux-design.md §7.3에 전체 표와 그레이스풀 디그레이드/권장 조치를 상세 기록 | 에이전트 자체판단(03단계 산출물과의 실제 대조 과정에서 발견, 규칙 A/필수원칙에 따라 임의로 화면 스펙을 축소하거나 없는 API를 있다고 가정하지 않고 명시) | Medium (대부분 API 보강으로 해소 가능한 부가기능 갭이나, 갭6은 정책/법률 판단이 필요해 별도 자문 권고 대상) | 04-ux-design.md §0·§2(C-03/C-04/C-06/C-07/C-08/C-10/C-11/C-13/R-02)·§7.3, 05단계 착수 전 오케스트레이터가 3단계 API 보강 여부를 규칙 A로 재확인 필요 |
| DEC-023 | 2026-09-19 | 4→3(피드백 루프, 규칙 F) | DEC-022 갭6 — 생체정보(음성) 동의 강제 범위를 어떻게 재설계할지 최종 확인 | **텍스트 전용 이용자는 세션 시작 시 음성 동의 절차를 생략하고, 실제로 음성 답변 기능을 선택하는 시점에만 별도로 음성(생체정보) 동의를 받는 방식으로 확정.** `/start`의 `biometric_voice` 동의 강제 403 로직을 제거하고, 음성 제출 API 경로에 동의 게이트를 이동. 세션 도중 동의 철회 시 서버가 이후 음성 제출을 실제로 차단하도록 강제 로직도 함께 03단계 재작업에 반영. 근거: 개인정보보호법 최소수집·목적특정 원칙에 더 부합, 텍스트 전용 이용자의 불필요한 동의 부담 제거 | 사용자 응답 | High | 03-system-design.md §4·§6.2(재작업 대상), traceability.md REQ-029~034, 04-ux-design.md 동의 화면(재확인 필요) |
| DEC-024 | 2026-09-19 | 3(설계서, 규칙F 재작업) | DEC-023(정책 확정) 및 DEC-022(갭 8건)를 `03-system-design.md`에 실제로 반영하는 재작업 완료 보고 | **DEC-023을 반영해 `03-system-design.md`를 재작업함(v1→v2), 8건 모두 해소.** (1)(2) `GET /api/v1/interviews`(내 목록), `GET /api/v1/users/me/consents`(동의이력) 신설. (2) `GET /api/v1/users/me/deletion-requests`(삭제요청 처리상태) 신설. (3) `GET /api/v1/interviews/{id}/code-submissions` 신설. (4) `GET /api/v1/interviews/{id}/whiteboard` 신설. (5) `INTERVIEWS.report_status`(none/queued/ready/failed) 필드 신설 + `POST /interviews/{id}/report/regenerate` 신설로 "WS 실시간 통지 + 재방문 시 목록 조회로 상태 확인 + 실패 시 재시도"의 3중 경로로 지원자용 비동기 알림 요건 충족. (6) **[DEC-023 정책 반영]** `/start`는 `ai_interview_notice` 동의만 검사(`403 CONSENT_REQUIRED_NOTICE`)하도록 축소, `biometric_voice` 검사는 `POST /interviews/{id}/turns`(음성 제출)로 이동해 **매 요청마다 DB를 실시간 재조회**(세션 컨텍스트 캐시 금지) — 철회 시 다음 음성 제출부터 즉시 `403 CONSENT_REQUIRED_VOICE`로 차단되고 job이 큐에 들어가지 않으며 오디오도 저장되지 않음(워커 단계에서 이중검사까지 추가해 단순 플래그 저장이 아닌 실제 차단 동작으로 명시). (7) `EVALUATION_REPORTS.star_json`(situation/task/action/result) 신설, `summary_text`는 파싱실패 폴백 전용으로 격하, 리포트 생성 전용 구조화 출력 스키마(§4.4) 신설, REQ-036 새니타이즈 대상 필드 목록에 `star_json`/`summary_text`/`details_json` 명시적 포함. (8) `/start` 성공 시 `opening_question` job을 GPU 큐에 자동 enqueue(`202 {job_id}`)하고 이후 일반 턴과 동일한 WS 이벤트(`stage_update`/`turn_result`)로 AI 첫 질문을 전달하는 경로 확정. `traceability.md`의 REQ-002/008/009/010/012/013/017/029/030/032/036 설계 매핑 컬럼을 갱신했고, 내부 검증 3차(회귀 검증 — "갭 8건 실제 해소 여부"와 "04단계와의 재정합성"을 검증 관점에 포함)를 수행해 결함 0건으로 PASS 확정(`verify-log_03-system-design.md` 3차 라운드). `04-ux-design.md`도 이 API 보강에 맞춰 §1.1/§2(C-04/C-06/C-07/C-08/C-10/C-13)/§7 등을 최소 수정하고 변경이력에 기록함(04는 이미 그레이스풀 디그레이드로 설계되어 있었으나, 갭6 정책 변경은 화면 문구·플로우 자체에 영향을 주어 04단계 문서 수정이 필요하다고 판단) | 에이전트 자체판단(03-system-designer 역할, DEC-023의 정책 결정을 설계 스펙으로 구체화) | Medium (API 계약 확정이나, 05단계 착수 전이라 구현에는 영향 없어 되돌리기 어렵지 않음) | 03-system-design.md 전체(v2), traceability.md REQ-002/008/009/010/012/013/017/029/030/032/036, verify-log_03-system-design.md(3차), 04-ux-design.md(§1.1/§2/§7/변경이력) |
| DEC-025 | 2026-09-19 | 3→5(설계→구현 전환, 선행 게이트) | DEC-016이 "확인 필요"로 남긴 Qwen2.5 크기별 라이선스를 05단계 착수 전 HuggingFace 모델 카드 원문으로 실제 확인 | **확인 결과 DEC-016의 전제가 틀렸음을 발견하고 즉시 수정.** WebFetch로 HuggingFace 모델 카드를 직접 대조한 결과: Qwen2.5-3B-Instruct(DEC-016의 MVP 기본값)는 `License: qwen-research` — **비상업 연구전용 라이선스**로, 상업/실사용 서비스에 그대로 쓸 수 없음. 반면 Qwen2.5-1.5B-Instruct·Qwen2.5-0.5B-Instruct·Qwen2.5-7B-Instruct는 모두 `License: apache-2.0`(상업적 이용 명시적 허용). **MVP LLM 기본값을 Qwen2.5-3B-Instruct에서 Qwen2.5-1.5B-Instruct(Apache-2.0, GGUF Q4_K_M 약 1GB 내외, VRAM 여유 큼, 3B보다 응답속도 빠름)로 변경 확정.** 품질 부족 시 업그레이드 경로는 기존 DEC-016의 7B(Apache-2.0으로 라이선스도 문제 없음, 부분 GPU 오프로드 필요)를 그대로 유지. 3B는 라이선스 사유로 후보에서 완전히 제외 | 에이전트 자체판단(HuggingFace 모델 카드 라이선스 메타데이터 실측 근거, DEC-016의 선행 게이트 조건을 이행하는 과정에서 오류 발견) | High (LLM 모델 크기 변경은 프롬프트 설계·응답 품질·05단계 구현 전반에 영향, 구현 착수 전이라 지금 고치는 것이 최소비용) | 03-system-design.md §2.4·§5.1(수정 필요), traceability.md, 05단계 LLM 서빙 구현 |
| DEC-026 | 2026-09-19 | 5(구현, unit-3) | 04-ux-design.md [C-12]가 "SESSION_EXPIRED의 정확한 시간 값은 05단계 확정"이라고 명시적으로 위임했고, 03-design도 `status=paused`로의 전이를 실제로 트리거하는 메커니즘(하트비트/배치 등)을 정의하지 않음 — unit-3(세션 중단/재접속)이 이 두 갭을 어떻게 메울지 결정 필요 | **(1) 세션 만료 임계값을 24시간으로 확정**(`live`/`paused` 상태에서 `started_at` 기준 경과시간이 24시간을 넘으면 `SESSION_EXPIRED`). 근거: 아직 WS/turn 인프라가 없어(unit-4 이후 범위) 서버가 참조할 수 있는 활동 시각이 `started_at`뿐이고, 하루 넘게 끝나지 않은 연습 세션은 방치된 세션으로 보는 것이 합리적 기본값. 판정은 배치 잡을 신설하지 않고 조회/재개 시점에 반응적으로(lazy) 계산 후 DB에 확정 반영. (2) **`live`→`paused`로 전이시키는 별도 트리거(하트비트, WS 접속끊김 감지 등)는 이번 유닛에서 만들지 않는다** — 그런 감지 인프라 자체가 03-design에 정의된 적이 없고, 새로 발명하면 REQ-013 범위를 벗어나는 신규 서브시스템이 되므로. 대신 재접속 시나리오는 "브라우저가 닫혀도 서버 DB의 `status`는 `live`로 그대로 남아있고, 재인증(JWT 재로그인)만으로 동일 세션을 다시 조회/재개할 수 있다"로 처리하며, `paused`는 ERD에 존재하는 상태를 그대로 받아들이는 방어적 처리(향후 유닛이 명시적으로 설정하면 `/resume`이 `live`로 되돌림)로만 다룸. 두 결정 모두 상수/조건문 단위로 격리되어 되돌리기 쉬움 | 에이전트 자체판단(04-ux-design이 수치 결정을 05단계에 명시적으로 위임했고, 메커니즘 신설은 범위외 변경 금지 원칙에 따라 보류) | Low(임계값은 상수 변경만으로 조정 가능, paused 트리거 메커니즘은 아직 아무것도 구축하지 않아 추후 unit-4 WS 게이트웨이가 자유롭게 설계 가능) | backend/app/api/v1/interviews.py(`SESSION_EXPIRY`, `_apply_lazy_expiry`), docs/harness/units/unit-3-note.md, traceability.md REQ-013 |
| DEC-027 | 2026-09-19 | 5(구현, 병렬 실행 중 발견) | 여러 05/06 에이전트를 동시 병렬 실행하는 과정에서, 공유 임시 venv(`.harness-tmp/venv_05_unit1`)를 먼저 끝난 한 에이전트의 Teardown(규칙K)이 통째로 삭제해버려, 아직 그 venv를 쓰고 있던 다른 병렬 에이전트에 영향을 줄 수 있는 위험이 실측으로 확인됨(unit-18 06단계 보고에서 발견) | **공유 venv 재사용 관행을 중단하고, 이후 병렬 실행되는 유닛은 각자 독립된 `.harness-tmp/venv_05_unit<n>`을 사용하도록 지시를 갱신한다.** 이미 진행 중인 에이전트가 이 문제로 실패하면 해당 유닛만 즉시 재시도(새 venv 생성)하고, 다른 유닛에는 영향 없음을 확인한다. 근본 원인은 "여러 병렬 에이전트가 같은 임시 리소스를 공유"한 설계 실수이지 코드 결함이 아니므로 규칙 F(피드백 루프)의 근본원인 재작업 대상은 아님 — 향후 지시 방식만 개선 | 에이전트 자체판단(오케스트레이터, 실측 발견 근거) | Low(운영 관행 변경일 뿐 산출물/요구사항에 영향 없음) | 향후 05단계 병렬 실행 지시문, .harness-tmp/ 사용 관례 |
| DEC-028 | 2026-09-19 | 5(구현, 병렬 실행 중 발견) | unit-12가 Teardown 과정에서 `taskkill /F /IM python.exe`(이미지 이름 기준 광범위 종료)를 실행해, 동시에 실행 중이던 다른 병렬 유닛(unit-5 등) 소유의 python.exe 프로세스까지 함께 종료됐을 위험이 보고됨 | **프로세스 정리는 반드시 자신이 직접 기동한 PID만 지정해 종료한다(예: `Stop-Process -Id <PID>` 또는 `taskkill /F /PID <PID>`), 이미지 이름 기준 와일드카드 종료(`/IM python.exe`, `pkill python` 등)는 병렬 실행 중에는 절대 금지.** 향후 05/06단계 지시문에 이 원칙을 명시적으로 포함시킨다. 영향받았을 수 있는 다른 진행 중 유닛은 각자 완료 보고 시 자기 서버 프로세스 생존 여부를 스스로 확인하고, 죽어있었다면 그 사실과 영향을 note에 남기도록 안내 | 에이전트 자체판단(오케스트레이터, 실측 발견 근거) | Low(운영 관행 변경, 산출물/요구사항에 직접 영향 없음 — 단 영향받은 유닛이 있다면 해당 유닛만 재검증 필요) | 향후 05/06단계 병렬 실행 지시문, .harness-tmp/ 프로세스 관리 관례 |
| DEC-029 | 2026-09-20 01:33:16 KST | 2(계획 변경, 규칙 F) | unit-7 진행 중 코드 실측으로, 어느 작업 단위에도 배정되지 않은 갭 3건 발견: (1) `GET /interviews`(내 면접 목록, [C-03] 지원자 홈 의존, DEC-024 갭1) 미구현, (2) `CodeEditorPanel`/`WhiteboardCanvas`/`WebcamPreview`가 정의만 있고 어느 화면에도 배선되지 않음, (3) REQ-033 자동 파기 배치(`delete_requested_data`) 미구현 — `DELETION_REQUESTS`가 pending에서 넘어가지 않음 | **신규 작업 단위로 등록**: unit-19(`GET /interviews` + [C-03] 지원자 홈, Feature B, REQ-002) / unit-20(면접장 [C-05]·[C-06] 화면에 코드 에디터·화이트보드·웹캠 배선 + unit-16 DEF-001 해소, 신규 Feature I "면접장 통합 UI", REQ-008·015·017) / unit-21(REQ-030·033 자동 파기 배치 Celery beat, Feature G) / unit-22(공통 테스트 인프라, DEC-030). **신규 REQ-ID는 만들지 않고 기존 REQ에 매핑**한다. 03/04 설계 근거(§4.2 GET /interviews, [C-03], [C-05]/[C-06], §6.2/§7.4)는 이미 존재하므로 설계 변경 없음(02 갱신 에이전트가 앵커 실존을 재확인). Feature I 신설과 unit-22를 feature가 아닌 공통 인프라로 분류한 것은 07 그룹핑을 위한 오케스트레이터 자체판단이며 사용자가 이견을 주면 변경한다 | 사용자 응답(질문 1번 (a)) + 오케스트레이터 분류 판단 | Medium(계획서 작업 단위 목록 변경, 되돌리려면 02 §9·traceability 재수정 필요) | 02-planning.md §9·§10, traceability.md(REQ-002/008/015/017/030/033 작업단위 컬럼), 05~07단계 |
| DEC-030 | 2026-09-20 01:33:16 KST | 5·7·8(검증 수단) | 지금까지 모든 프런트 유닛이 브라우저 자동화 도구 부재로 UI 검증을 못 했고, 07 회귀·08이 재실행할 테스트 코드가 저장소에 없음(임시 스크립트를 매번 삭제) | (a) **Playwright(`@playwright/test`)를 `frontend` devDependency로 추가**하고 (b) `backend/tests/`(pytest 기반)·`frontend/e2e/`에 **테스트 코드를 저장소에 보존**한다. 작업 단위 unit-22로 구현·검증. **DEC-001(MCP 미연동)은 그대로 유지** — Claude in Chrome MCP는 도입하지 않음 | 사용자 응답(질문 2번 (a), 3번 "남김") | Medium(패키지 추가는 제거 가능하나 브라우저 바이너리 다운로드 발생) | frontend/package.json·package-lock.json, backend/requirements-dev.txt, .gitignore, 07·08단계 |
| DEC-031 | 2026-09-20 01:33:16 KST | 속도 트랙 | unit-8/10/11 및 신규 unit-19/20/21/22의 속도 트랙 | 전부 **L3(일반)** — 06·07 정식(10섹션 전체), 규칙 B 원문(Tier=High), 부채 없음. unit-8/10/11은 사용자 응답(4번), 신규 유닛은 하네스 기본값(응답 없으면 L3, 사용자가 별도 지정하지 않음). DEC-003(기본 L1)의 예외이며 기존 L1 부채 정산 의무(08 착수 전)는 그대로 유지 | 사용자 응답(질문 4번) + 하네스 기본값 | Low | 05~07단계 산출물 |
| DEC-032 | 2026-09-20 01:33:16 KST | 5~7(병렬 실행 정책) | 사용자가 "병렬 처리 가능한 것은 모두 병렬로, 단 품질 저하 금지"를 지시. ORCHESTRATOR.md 5-1/5-2 조건으로 후보 판정 | **병렬 가능**: 테스트 대상 코드와 파일이 겹치지 않는 작업(현재 unit-7 06과 동시에 unit-22 인프라, unit-20 프런트 배선, 02 문서 갱신). **병렬 불가**: unit-8(unit-7 파일 수정, 06 진행 중), unit-10↔unit-8(LLM 출력이 unit-8 새니타이즈 입력 계약에 들어감, tasks.py·llm_engine·job_queue 겹침), unit-10→unit-11(동일 EVALUATION_REPORTS/report job), unit-19↔unit-8/unit-10(interviews.py 겹침). 병렬 에이전트 공통 규칙: 독립 포트·테스트 계정, 자신이 띄운 PID만 종료(DEC-028), `traceability.md`/`decisions.md`는 오케스트레이터만 편집, `frontend` 빌드/dev 서버 직렬화(`.next` 충돌), Alembic 리비전은 오케스트레이터가 사전 배정 | 사용자 지시 + 오케스트레이터 판정(5-1/5-2 조건 적용) | Low(운영 관행) | 05~09단계 병렬 호출 지시문 |
| DEC-033 | 2026-09-20 03:04:28 KST | 6(unit-22 판정) | unit-22 06단계 결과 CONDITIONAL PASS — 인수조건 13/13 통과, Low 결함 3건 Open: DEF-001(`cleanup.py`의 `psycopg.connect`에 `connect_timeout` 없음 → DB 불통 시 60~100초 무응답), DEF-002(test-infra.md §5.2가 `--incremental false` 없는 tsc를 안내해 추적 생성물 `tsconfig.tsbuildinfo`가 갱신됨), DEF-003(`--all` dry-run이 UUID 형식이 아닌 마커형 계정이 1건이라도 있으면 건수 없이 전체 중단 — unit-19가 만든 3건으로 실제 발생) | **Deferred로 수용하지 않고 5단계 재작업으로 수정**한다(수 줄+문서 1줄 규모, 08의 "잔여 마커 0건 확인"과 무인 실행 안정성에 직결). 수정 방향: DEF-001 connect_timeout 지정+예외를 `CleanupError`로 승격, DEF-002 문서를 `--incremental false`로 통일, DEF-003 dry-run은 비-UUID 마커형 행을 stray로 별도 집계·보고하고 계속 진행하되 **실삭제는 종전대로 중단 유지**(안전 계약 약화 금지). 재작업 후 06이 TC-053/054/055만 재검증한다. unit-20 06은 이 재작업과 병렬로 진행 가능(브라우저 e2e 경로는 정리 헬퍼 수정과 무관, 계정은 반드시 `account_factory` UUID 마커로 생성) | 에이전트 자체판단(오케스트레이터; 하네스 기본 흐름 = 결함은 수정, 결정 로그에 사유 기록) | Low(테스트 인프라 코드/문서, 되돌리기 쉬움) | backend/tests/support/cleanup.py, docs/harness/test-infra.md, unit-22-note/test |
| DEC-034 | 2026-09-20 03:04:28 KST | 6(unit-7 판정, 결정 대기 기록) | unit-7 06단계 결과 CONDITIONAL PASS — 인수조건 6/6 통과, 결함 11건 Open(High 2: DEF-001 오프닝 품질 불량, DEF-011 Redis 무인증 외부 노출). 근거 `docs/harness/units/unit-7-test.md`, `verify-log_unit-7-test.md` | **결정 대기(사용자 응답 전에는 재작업 착수 안 함)**: A LLM 품질 대응(오프닝 질문은행 원문+후속 출력 검증·재시도·폴백 vs 7B 승격, 설계 §4.3 편차/§2.4 재검토 여부), B Redis·PostgreSQL 127.0.0.1 바인딩+Redis requirepass, C 처리 중 워커 크래시 job 재처리(acks_late+멱등) vs 실패 통지, D 긴 답변(`-c 8192`+이력 토큰 예산 자르기), E `(interview_id, turn_index)` 유니크 제약+원자적 채번(Alembic). unit-8·unit-21은 같은 파일을 수정하므로 unit-7 재작업·재검증 이후에 착수한다(DEC-032 판정) | 대기 — 사용자 결정 필요 | High(모델 변경/스키마 변경 포함 시 비가역적) | unit-7 05 재작업, unit-8, unit-21, 03 §2.4·§4.3 |
| DEC-035 | 2026-09-20 22:2x KST | 5(unit-7 재작업 착수 전, DEC-034 응답) | DEC-034가 결정 대기로 남긴 Q1~Q5(단, 세션 인터페이스 제약상 AskUserQuestion 1회당 최대 4문항이라 Q1~Q4/Q5를 두 번에 나눠 질문) | **Q1(LLM 1.5B 품질)**: (a) 가드 추가(오프닝=뱅크 원문, 후속=출력검증·재시도·뱅크 폴백, 1.5B 유지) 채택 — 7B 승격(b)·현상유지(c) 기각. **Q2(Redis 노출, DEF-011)**: 09/10으로 미루지 않고 지금(unit-7 재작업 범위) 즉시 반영 — 127.0.0.1 바인딩+requirepass. **Q3(워커 크래시 job 정책)**: 실패 통지(단순) 채택 — acks_late+멱등 재처리는 기각(복잡도·중복응답 위험). **Q4(긴 답변 정책)**: 서버측 잘라내기/요약 채택 — API 상한 하향·컨텍스트 확대(`-c 8192`)는 기각. **Q5(`(interview_id, turn_index)` 유니크 제약)**: 지금 도입(마이그레이션, 기존 데이터 0건이라 영향 없음) 채택 — 추후 보류 기각. 이 5가지 모두 unit-7 05 재작업 범위에 포함하며, 재작업 후 06이 해당 TC를 재검증한다(unit-7-test.md 9절 조건) | 사용자 응답(AskUserQuestion, 전부 Recommended 옵션 선택) | High(모델 가드 설계·Redis 인증·스키마 변경 포함, 재작업 착수 전이라 지금 반영이 최소비용) | unit-7 05 재작업(전면), unit-7-test.md TC 재검증 대상, unit-8·unit-10·unit-11·unit-21(순차 대기 해제 조건), traceability.md |
| DEC-036 | 2026-09-20 22:2x KST | 규칙 K(세션 재개, 강제중단 잔존물 점검) | 세션 재개 시 `.harness-tmp` 스캔 결과, 이전 세션이 강제 중단된 채 살아있던 python 프로세스 6개 발견: unit-19 06 검증용 백엔드(PID 7412/9360, 03:08:24 기동, 발견 시점 기준 약 19시간 경과), unit-20 06 검증용 백엔드(PID 13168/19396, 03:10:13 기동, 약 19시간), unit-22 06 검증용 백엔드(PID 35324/6612, 07:22:40 기동, 약 15시간). unit-22-test.md는 이미 Teardown 완료를 주장했으나 실제로는 프로세스가 살아있어 기록과 실측이 불일치 — 근본 원인 미상(재작업 v5 검증 도중 두 번째 강제중단으로 teardown 스크립트 실행 전에 세션이 끊겼을 가능성이 유력하나 확정 불가) | **PID 지정 종료 승인**(이미지명 와일드카드 금지, DEC-028) — 6개 PID 전부 `Stop-Process -Id <PID> -Force`로 개별 종료, 이후 재조회로 전부 소멸 확인, `git status` 클린 재확인(사용자 소유 미추적 파일 `99.현재상태/현재상태_02.png` 1건 제외, 손대지 않음). **`.harness-tmp` 내 unit-19·unit-20 파일 아티팩트(스크립트·중간 스펙·로그)는 06 재개 시 필요한 진행 상태이므로 삭제하지 않고 보존**, unit-22 파일 아티팩트(이미 v5 PASS 확정된 유닛의 잔여물) 정리는 이번 결정에 포함되지 않아 별도 확인 후 처리 | 사용자 응답(AskUserQuestion) | Low(재생성 가능한 검증용 프로세스 종료, 원본 코드·데이터 영향 없음) | .harness-tmp/ 운영 상태, unit-19·unit-20·unit-22 06 재개 절차 |
| DEC-037 | 2026-09-22(대화 기록 근거, 2026-09-23 사후 기록) | 5(③ 매트릭스 후속 착수) | ③ 전환 가능성 매트릭스 "가능" 15개 항목을 어떤 순서/범위로 착수할지 — 규칙 G에 따라 즉시 기록됐어야 하나 세션이 연속 진행되며 결정 시점에 실제로 기록되지 않고 각 unit-N-note.md 안에 "사용자 승인"으로만 산발적으로 남음(DEC-038~049는 그 각각을 이번에 정식 기록으로 승격한 것) | 사용자가 ③ 매트릭스의 우선순위 목록(소→중→대, 위험도 순)을 승인해 unit-23부터 순차 착수 지시. 원칙: "실제로 실행·검증된 것만 완료로 표시"(가짜 진행률 금지), 외부 API 키 등 자원이 없는 항목은 코드만 작성 후 "코드준비·미검증"으로 명확히 구분, 법무/자원/하드웨어 제약이 있는 항목은 진행하되 제약을 결과서에 명시 | 사용자 응답(대화, 정식 DEC 미부여 상태로 진행됨 — 이번에 소급 기록) | High(다수의 기존 Out-of-Scope 결정(DEC-004/006/008)과 REQ-031 등 정책 결정을 부분적으로 뒤집는 근원 승인) | unit-23~35 전체, traceability.md REQ-018/019/021/023 등, 09단계(보안/컴플라이언스 감사)가 이 배치 전체를 재검토해야 함 |
| DEC-038 | 2026-09-22 21:27:11 KST(사후 기록) | 5(unit-23) | "합격/불합격 추천 의견" 필드가 원 계획서(REQ-F-006/007)에는 있으나 구현(REQ-031)에서 개인정보보호법 제37조의2(자동화된 결정 거부권) 리스크로 의도적으로 빠져있던 불일치를 사용자가 직접 지적 | 법적 리스크를 사용자가 명시적으로 인지한 상태로 복원 승인. `pass_fail_recommendation` 필드를 기존 `overall_recommendation`(REQ-031, 3단계 권고)에 **추가**(대체 아님)로 신설. unit-23-note.md 자체가 "정식 DEC 번호 미부여 — 09단계 재검토 필요"라고 이미 명시해뒀던 상태를 이번에 정식 DEC로 승격 | 사용자 응답(대화 기록 근거) | High(개인정보보호법 리스크가 있는 필드 신설, 배포 전 법무 검토 필수 — 09단계 Critical 항목으로 취급) | evaluation_report.py, a7c3e9f14b02 마이그레이션, llm_engine.py, interview_prompts.py, 프론트 리포트 화면, traceability.md REQ-031(갱신 필요, 미반영 상태였음 — 이번 결정과 함께 반영), 09단계 |
| DEC-039 | 2026-09-22(사후 기록) | 5(unit-24) | DEC-037 순서상 "STT 실시간 스트리밍"이 Prosody보다 먼저였으나, 착수 직전 발견한 `interviews.py`의 당일 실제 장애 이력(음성 제출 경로) 때문에 브라우저 테스트 도구 없이 그 자리를 재설계하는 위험을 감수할지 결정 필요 | **순서를 재조정**해 STT 스트리밍을 보류하고 Prosody 분석(REQ-018/019 축소판, librosa 기반 pitch/jitter/voiced_ratio 원시 수치만)을 먼저 진행 — REQ-018/019는 DEC-008로 Out-of-Scope였으나 오픈소스라 예산 문제가 없어 부분 재도입. "자신감/긴장도" 같은 해석 라벨은 신뢰도 검증(벤치마크) 없이는 붙이지 않음 | 에이전트 자체판단(사용자 승인 순서에서 이탈, 근거를 unit-24-note.md §0에 명시 — 20년차 엔지니어 판단으로 고위험 미검증 재설계보다 안전한 항목 우선) | Medium(순서 변경 자체는 가역적, Out-of-Scope 재도입은 REQ-018/019 traceability 갱신 필요) | librosa 의존성(requirements.txt), traceability.md REQ-018/019(당시 미반영 — 이번에 갱신), STT 스트리밍은 보류 상태로 재개 여부 결정 대기 |
| DEC-040 | 2026-09-22 22:46:44 KST(사후 기록) | 5(unit-25) | 원 계획서 "모든 데이터는 전송·저장 시 암호화"를 문자 그대로 전부 구현할지, 범위를 좁힐지 | **"모든 데이터"가 아닌 부분 구현으로 범위 축소**: 전송 구간(nginx TLS1.3 샘플+보안헤더)과 저장 구간 중 `consents.ip_address`(AES-256-GCM, 기존 699건 포함)만 실제 적용. `TRANSCRIPTS.content_text` 등은 unit-24에서 발견된 별도 인코딩 손상 결함 조사가 먼저 끝나야 안전하게 암호화할 수 있어 의도적으로 제외 | 에이전트 자체판단(사용자 지시 "현재 작업 마무리 후 추가 작업"에 따른 범위 결정, 근거는 unit-25-note.md §1) | Medium(암호화 미적용 컬럼이 남아있다는 사실 자체가 09단계 감사 대상) | consent.py, 신규 AES 암호화 모듈, nginx 샘플, 09단계(미암호화 컬럼 목록 재확인 필요) |
| DEC-041 | 2026-09-22(사후 기록) | 5(unit-26) | 원 계획서가 명시한 "LangChain+LangGraph 오케스트레이션"으로 운영 파이프라인(`llm_engine.py`)을 실제 교체할지, 아니면 별도 검증만 할지 | **운영 파이프라인은 그대로 유지**하고 동등 기능의 LangChain 검증 모듈을 별도로 구축해 실제 llama-server로 정상/재시도/실패 3분기 검증만 수행. 이유: 프레임워크 전환은 기능 추가가 아닌 순수 리팩터링이라 실익이 낮고, unit-7에서 이미 11건 결함을 거쳐 안정화된 로직(GPU/CPU 폴백, 이중바인드 방지 등)을 재구현하면 같은 결함을 재도입할 위험 | 에이전트 자체판단(근거는 unit-26-note.md §1, "실제 전환은 회귀위험 때문에 보류 — 별도 결정 필요"라고 자체 명시) | Medium(운영 전환 여부는 여전히 미결정 상태로 남음 — 향후 별도 결정 필요) | langchain-core/langchain-openai 의존성, langchain_orchestrator.py(검증 전용, 운영 미연결) |
| DEC-042 | 2026-09-22 23:4x대 KST(사후 기록) | 5(unit-27) | traceability.md가 8단계 착수 전 미착수로 남겨뒀던 AI 안전장치 5종(REQ-035~039, 규칙 J)을 지금 구현할지 | REQ-036(출력 새니타이즈)·REQ-037(함수호출 최소권한)은 기존 코드(React JSX 자동이스케이프, pydantic Literal 화이트리스트)로 이미 충족돼 재구현 없이 실측만 확인. REQ-035(인젝션 감사로그)·038(레이트리밋)·039(유출 감사로그)는 신규 구현, 실제 HTTP로 레이트리밋 10/11 경계 검증 중 설계 결함 1건(키를 interview_id 기준→user_id 기준으로 재설계) 발견·수정 | 에이전트 자체판단(traceability.md에 이미 8단계 전 완료 의무로 등록돼 있던 항목의 실행, 규칙 J 절차) | Medium(보안 관련 신규 로직, 09단계 Critical 검증 대상) | prompt_safety.py(신규), traceability.md REQ-035~039(당시 미반영 — 이번에 갱신) |
| DEC-043 | 2026-09-23 00:1x대 KST(사후 기록) | 5(unit-28) | 원 계획서·03-design REQ-015("서버 미전송" 설계 원칙, WebSocket은 서버→클라이언트 push 전용)를 뒤집고 실제 WebRTC 영상 수신을 구현할지 — 전체(시그널링+미디어서버)를 다 할지, 일부만 할지 | **시그널링 서버(SDP/ICE 중계)만** 실제 구현·2피어(Python websockets 클라이언트) 실측 검증. 미디어 서버(aiortc)·프론트엔드 RTCPeerConnection·STUN/TURN은 의도적으로 범위 밖 — "영상을 실제로 서버가 본다"는 원안 목표는 아직 미달성이며, 이 사실을 결과서에 명확히 남김 | 에이전트 자체판단(근거는 unit-28-note.md §1 "정직한 스코프 선언" — 대규모 작업을 정직하게 끝낼 수 없어 범위를 명시적으로 좁힘, 브라우저 자동화 도구 부재로 실제 미디어 스트림 검증 불가) | High(REQ-015 설계 원칙 자체를 부분적으로 뒤집는 결정, DeepFace 등 후속 기능의 선행조건) | webrtc_signaling.py(신규), traceability.md REQ-015(설계 원칙 변경 가능성 — 09단계 재확인 필요) |
| DEC-044 | 2026-09-22 23:5x대 KST(사후 기록) | 5(unit-29) | 코드 실행 샌드박스(REQ-022, Out-of-Scope였음)를 구현할지, 구현하면 실제 실행까지 검증할지 | 코드(Docker 9중 격리)는 작성했으나 **Claude Code 플랫폼 자체의 안전 분류기가 "RCE 위험 생성"으로 실행 테스트를 차단** — 이 세션이 임의로 우회하지 않고 그대로 수용, 단 하나도 실측 검증되지 않은 상태로 명시. **사용자 승인 없이 프로덕션 연결 절대 금지**를 결과서·매트릭스에 강조 표기 | 플랫폼 제약(세션이 우회 불가) + 에이전트 자체판단(안전상 미검증 코드를 검증된 것처럼 표시하지 않기로 결정) | High(임의 코드실행 기능은 원 계획서에 없던 신규 보안위험, 검증 전 연결 시 RCE 리스크) | code_sandbox.py(신규, 미검증), traceability.md REQ-022, 09단계(침투테스트 수준 검증 필요) |
| DEC-045 | 2026-09-23 00:2x대 KST(사후 기록) | 5(unit-30) | K8s/클라우드 배포 매니페스트(REQ-023, 500명 목표와 함께 Out-of-Scope였음)를 실제 클러스터 없이 작성하는 것이 의미가 있는지 | 실제 클러스터/클라우드 계정이 없어 `yaml.safe_load()` 문법 검증만 수행 — "참고용 설정"으로 명시하고 "배포가 실제로 동작한다"는 의미로 사용하지 않음. API 계층은 stateless라 문서상 수평확장 가능하나 GPU 워커는 여전히 1개 고정(매니페스트 주석에 명시) | 에이전트 자체판단(근거는 unit-30-note.md §1 "정직한 스코프 선언") | Low(실제 인프라에 적용된 바 없어 되돌릴 것도 없음, 참고 문서 성격) | backend/deploy/k8s/*.yaml(신규, 미적용) |
| DEC-046 | 2026-09-23 00:1x대 KST(사후 기록) | 5(unit-31) | DeepFace 표정 감정분석(REQ-018)은 DEC-008이 EU AI Act 금지관행 논의·국내 생체정보 리스크로 Out-of-Scope 처리했던 항목 — 그 리스크를 인지한 상태에서도 기술 검증만 진행할지 | 사용자가 리스크를 인지한 후 **기술 검증만** 승인(서비스 연결은 별개 승인 필요). 정지 이미지 1장→7종 감정 확률분포만 구현·검증(confidence 0.97 실측), 원안의 연속 프레임/시계열/타임라인은 미구현. GPU 4GB 중 LLM이 이미 ~1.7GB 점유해 실시간 처리 추가 시 자원경합 문제도 별도 명시. **법무 검토 전 서비스 연결 절대 금지**를 결과서에 반복 강조 | 사용자 응답(리스크 인지 후 기술검증 승인, unit-31-note.md 서두에 명시) | High(생체인식정보 처리, EU AI Act·개인정보보호법 리스크 — 법무 검토 없이는 배포 불가) | emotion_engine.py(신규), traceability.md REQ-018(당시 미반영 — 이번에 갱신), 09단계 |
| DEC-047 | 2026-09-23 00:0x대 KST(사후 기록) | 5(unit-32) | 유료 벤더(ElevenLabs/Deepgram/Pinecone) 어댑터를 API 키 없이 코드만 작성하는 것으로 진행할지 | 2026-09-22 합의 그대로 **코드/어댑터만 작성, 실제 연동 테스트는 키 확보 후**로 범위 확정. 실행 테스트 0건 — "코드 준비 완료·미검증"으로 명확히 구분해 "완료"로 표시하지 않음 | 사용자 응답(대화 기록 근거, unit-32-note.md §2 "사용자 승인 범위 그대로") | Low(운영 코드에 연결되지 않은 격리 파일, 키 확보 전까지 아무 영향 없음) | tts_adapter_elevenlabs.py, stt_adapter_deepgram.py, vectordb_adapter_pinecone.py(전부 신규, 미연결) |
| DEC-048 | 2026-09-23 00:4x대 KST(사후 기록) | 5(unit-33) | GDPR/CCPA(원안이 "국내 시장 대상이라 PIPA로 대체"하며 Out-of-Scope 처리) 준수를 코드로 어디까지 대응할 수 있는지 | 이 세션은 법무 자문이 아니므로 **갭 분석(엔지니어 관점)까지만** 수행하고, 갭 중 기술적으로 작고 명확한 것 1건(데이터 이동권 Art.20, `GET /users/me/data-export`)만 실제 구현·검증. 국외 이전 안전조치·DPO 지정 등은 "법무 판단 필요"로 명시하고 코드로 대신 결정하지 않음 | 에이전트 자체판단(근거는 unit-33-note.md §1, 법무 영역과 기술 영역을 명확히 분리) | Medium(부분 구현이 "GDPR 준수 완료"로 오인되지 않도록 결과서·매트릭스에 반복 명시 필요) | data-export 신규 엔드포인트, traceability.md REQ-028(부분 갱신 필요) |
| DEC-049 | 2026-09-23(사후 기록) | 5(unit-34) | unit-33이 갭 분석에서 남긴 "비교적 작은 범위" 2건(적법근거 기록, CCPA 판매안함 고지)을 후속 유닛으로 바로 착수할지 | 사용자가 "10개 중 바로 작업 가능한 것"으로 지목해 착수 승인. 구현 중 **법률 고지 문구 정합성 버그**를 발견(기존 문구가 "합격/불합격 확정 기능 없음"이라 단정했는데 unit-23이 그 필드를 실제로 추가해 사실과 어긋나 있었음) — 별도 지시 없이도 방치하면 안 되는 문제로 판단해 즉시 함께 수정 | 사용자 응답(대화 기록 근거) + 에이전트 자체판단(정합성 버그 수정 범위, unit-34-note.md §1-3) | Low(additive 컬럼, 기본값 있어 기존 API 계약 손상 없음) | consent.py, d4f7b2c8a913 마이그레이션, complianceContent.ts, traceability.md REQ-028 |
| DEC-050 | 2026-09-23 | 5·6(unit-35, 본 세션) | 화이트보드 AI 비전 분석(REQ-021)은 DEC-008이 "예산 없음, 오픈소스 대안 로컬구동 검증 안 됨"으로 Out-of-Scope 처리했던 항목. `whiteboard_vision.py` 모듈 docstring에 "2026-09-23 사용자 승인, unit-35"라는 기록만 있고 decisions.md에는 반영된 적이 없어(DEC-037과 동일한 유형의 규칙 G 공백), 이번 세션이 이를 인계받아 정식 06단계 테스트를 완료 | 렌더링(`render_strokes_to_png`, 좌표→PNG, 외부 의존성 없음)은 이번 세션이 실제 실행 검증 완료(TC-001~007, 함수 단위 테스트, requirements.txt에 누락돼 있던 Pillow 의존성도 함께 수정). 비전 모델 호출(`analyze_diagram_with_vision`, GPT-4V)은 API 키가 없어 실제 네트워크 검증은 여전히 불가하나, 이번 세션이 응답 파싱·에러 처리 로직을 mock으로 검증(TC-008~010, unit-32/unit-30 등 이전 미검증 어댑터들에는 없었던 추가 검증). **어떤 API 엔드포인트에도 배선하지 않음** — DEC-008 취지(예산 없이는 비전 호출 자체가 무의미) 중 렌더링 부분만 실질적으로 해소됐을 뿐 전체 기능은 "부분착수"임을 명시 | 사용자 응답(전 세션, 모듈 docstring 근거로 인계) + 에이전트 자체판단(이번 세션의 검증 범위 결정) | Medium(DEC-008 Out-of-Scope 부분 재도입, API 미배선이라 되돌리기 어렵지 않음) | whiteboard_vision.py, requirements.txt(Pillow), traceability.md REQ-021(이번에 갱신), unit-35-note.md, unit-35-test.md |
| DEC-051 | 2026-09-23 | 5(unit-24 재조사, STT 스트리밍 재개 직전) | STT 실시간 스트리밍 재개에 앞서, unit-24가 "심각도 잠정 High~Critical"로 보고했던 `TRANSCRIPTS.content_text` DB 손상(U+FFFD 치환문자) 결함이 실재하는지 재확인 필요(음성 제출 경로를 다시 건드리기 전 필수 확인) | **재조사 결과: 실제 데이터 손상이 아닌 오탐(false positive)으로 판정.** 근거: (1) 현재 DB `transcripts` 96건 전량 U+FFFD 0건. (2) raw psycopg로 한글 문장 INSERT→SELECT 왕복, `original_bytes == roundtrip_bytes` 완전 일치(hex 비교). (3) 프로덕션과 동일한 SQLAlchemy ORM 경로(`Transcript(content_text=...)`, User/Interview FK 포함 실제 flush)로 재현, 역시 바이트 단위 완전 일치(테스트 후 rollback, DB 영향 없음). (4) 결정적 정황: 이번 진단 스크립트 자체의 `print()` 출력이 이 세션의 Windows 터미널(cp949 콘솔)에서 한글이 깨져 표시됐으나, 파일로 UTF-8 기록해 대조한 실제 값은 전부 정상이었음 — "화면에는 깨져 보이지만 데이터는 멀쩡한" 상황을 이 세션에서 직접 재현함. unit-4-test.md가 과거 겪은 것과 동일한 클래스의 콘솔 표시 오류로, unit-24가 당시 이 함정을 unit-4처럼 바이트 단위로 재검증하지 않고 화면 조회만으로 "손상"이라 단정했던 것으로 추정. **STT 스트리밍 재개에 이 결함이 걸림돌이 아님을 확인**. unit-24-note.md §3, unit-25-note.md §1, `field_encryption.py` 모듈 docstring에 정정 주석 추가(원문은 append-only 원칙에 따라 삭제하지 않고 보존) | 에이전트 자체판단(사용자가 "손상 버그 먼저 진단"을 지시, 재조사 자체를 수행) | Medium(이전 결정(DEC-040, unit-25의 TLS/AES 암호화 범위에서 content_text 제외)의 전제가 되는 사실관계가 정정됨 — 암호화 범위 확대는 후속 유닛으로 사용자가 원할 때 별도 진행, 이 DEC는 오탐 정정만 다룸) | unit-24-note.md, unit-25-note.md, field_encryption.py, DEC-040(전제 정정, 결정 자체는 유지) |
| DEC-052 | 2026-09-23 | 5·6(unit-36) | unit-24(DEC-039)가 보류했던 STT 실시간 스트리밍을 이번 세션(브라우저 도구 보유)에서 재개할지, 재개한다면 어떤 아키텍처로 할지(진짜 청크 재조립 vs 미니 세그먼트 주기 업로드) | 사용자가 재개를 승인하고, 두 아키텍처 중 **미니 세그먼트 주기 업로드**(4초마다 독립 MediaRecorder 재시작, 완전한 WebM 파일 단위로 서버 전송)를 선택 — WebM 연속 청크 재조립(첫 조각에만 헤더가 있어 서버가 스트림을 재구성해야 하는 신규 복잡도)보다 위험이 훨씬 낮고, 오늘 실제 장애가 있었던 `_submit_voice_turn`을 전혀 건드리지 않는 완전히 분리된 새 엔드포인트(`POST /interviews/{id}/turns/preview`)로 구현. 별도의 더 여유로운 레이트리밋(분당 20회, 기존 LLM 턴 레이트리밋과 독립)을 신설 | 사용자 응답(AskUserQuestion, 두 질문 모두 권장 옵션 선택) | Medium(신규 엔드포인트·신규 프런트 녹음 루프 추가, 기존 코드는 무변경이라 되돌리기 쉬움) | interviews.py(`submit_voice_preview`), transcript.py(`VoicePreviewResponse`), prompt_safety.py(`check_and_increment_preview_rate_limit`), frontend/lib/api.ts, frontend/app/interviews/[id]/page.tsx, unit-36-note.md, unit-36-test.md |
| DEC-053 | 2026-09-23 | 세션 재개(unit-36 이후) — 정합성 정리 | 세션 재개 시 검토 결과: (1) `traceability.md`의 REQ-009/010/012가 "Not Started"인데 실제 코드는 2026-09-22 커밋 `18d5ceb`(리포트 생성 전 계층)·`a182388`(unit-23)에 이미 있고, 배정된 unit-10/11의 구현 노트·테스트 결과서가 없음(규칙 H·D 공백, DEC-037/050과 같은 유형). (2) 이번 세션 PC가 기존 개발 서버와 다름: i5-7300U/Intel HD 620(NVIDIA GPU 없음)/RAM 16GB, Python 미설치, Docker Desktop 미기동, `backend/var/`(LLM 모델·llama-server·Piper 음성)·`backend/.env`·`node_modules` 없음 → 이 PC에서 리포트 생성 실측 불가. (3) 원래 진행현황 아티팩트(`추가개선사항정리.md`의 링크)는 삭제됐거나 이 계정에 공유되지 않아 읽을 수 없음 | 사용자 선택(AskUserQuestion 2회): **작업 범위 = "정합성 정리"만**(07 부채 정산·③ 매트릭스 잔여 항목 계속·unit-36 브라우저 마무리 검증은 이번 범위 밖). **검증 환경 = "문서 정합성만 지금"** — 이 PC에 환경을 새로 구축하지 않고, 코드 정적 검토 + git 이력 + 기존 개발 서버 실측 기록(`bugfix-20260922-event-loop-hang-test.md` TC-H04/H05, `unit-23-test.md`)을 근거로 unit-10/11 노트·결과서를 소급 작성. **새 실측이 없으므로 결과서는 PASS로 처리하지 않고** 개발 서버에서 돌릴 실측 절차를 결과서에 남김. **아티팩트 = 새로 만들기**(원본 목록을 못 읽어 저장소 문서 기준으로 재구성했다는 사실을 페이지에 명시). 루트 `TEST` 파일 = 그대로 둠. 서비스 직접 기동은 승인받았으나 위 (2) 때문에 실행하지 않음 | 사용자 응답 | Low(문서만 변경, 코드 무변경) | traceability.md REQ-009/010/012, units/unit-10-note.md·unit-10-test.md·unit-11-note.md·unit-11-test.md(신규), verify-log_unit-10-test.md·verify-log_unit-11-test.md(신규), 진행현황 아티팩트(신규) |
| DEC-054 | 2026-09-23 | 6(unit-10/11 소급 정적 검토) → 규칙 F 근본원인 판단 대기 | unit-10/11 정적 검토에서 설계·기획과 구현이 다른 결함 확인: **U11-DEF-001(High)** REQ-010은 기획서(02 §4.2)상 "사전 정의 루브릭 템플릿 기반" 채점인데, 실제 채점은 프롬프트(`interview_prompts._REPORT_PERSONA`)에 고정된 3축(기술/의사소통/조직적합)뿐이고 `RUBRIC_TEMPLATES.criteria_json`·`INTERVIEWS.rubric_template_id`는 리포트 생성 경로 어디에서도 읽지 않음(이미 `bugfix-20260922-ws-fallback-test.md` §8에 "루브릭 템플릿 미연결"로 언급됐으나 결함으로 등록된 적 없음). 03 설계서 §4.4도 고정 3축 스키마만 정의하고 템플릿 연결 방식을 정하지 않아 **근본 원인이 3단계 설계 공백일 가능성**이 있음. **U11-DEF-002(High)** REQ-012 평가 근거(`details_json`)를 API는 반환하지만 지원자([C-11])·채용담당자([R-02]) 어느 화면에도 렌더링하지 않음(04 디자인서 `RubricEvidenceAccordion` 미구현). 그 외 U10-DEF-001(대화 전체 길이 상한 없음 → 긴 면접은 4096 컨텍스트 초과로 리포트 실패 가능, 정적 추정·미실측), U10-DEF-002·U11-DEF-003(Medium) | **이번 세션에서는 고치지 않음** — 사용자가 이번 범위를 "정합성 정리"로 한정했고(DEC-053), U11-DEF-001은 설계 해석(템플릿 기준을 LLM 프롬프트에 주입할지, 점수 축 자체를 템플릿으로 바꿀지 등)이 두 가지 이상이라 규칙 A 질문 대상. 결함은 unit-10/11 결과서에 Open으로 등록하고, 수정 방향은 다음 작업 착수 시 사용자에게 질문한다 | 에이전트 자체판단(근거: 규칙 A-2 해석 복수, DEC-053 범위 한정) — 수정 방향은 사용자 결정 대기 | Medium(수정 방향에 따라 03 설계서 §4.4·DB 사용 방식이 바뀔 수 있음) | 03-system-design.md §4.4(규칙 F 재작업 후보), 04-ux-design.md [C-11]/[R-02], interview_prompts.py, worker/tasks.py, report/page.tsx, recruiter/[id]/page.tsx |
| DEC-055 | 2026-09-23 | 5(원안 일치 작업 재개) + 환경 | 사용자 지시 "스펙 비교 아티팩트의 원안 미반영 항목을 계속 원안에 일치하도록 처리". 미반영 항목은 코드로 가능한 것 / 외부 자원(유료 키·법무·클라우드) 필요 / 예산·하드웨어 문제로 나뉘고, 이 PC에는 실행 환경이 없음(DEC-053) | 사용자 응답(AskUserQuestion): (1) **실행 환경 = 이 PC에 새로 구축**. 설치·다운로드 9개 항목(Python 3.13, backend `.venv` 패키지, Qwen2.5-1.5B GGUF, llama.cpp b11050 CPU·Vulkan, Piper ko_KR-kss-medium, faster-whisper small, MiniLM 임베딩, Docker 이미지 pgvector/redis, 프런트 node_modules + Playwright Chromium, 설치 후 약 5~6GB·다운로드 약 3.5~4.5GB) **전부 진행 승인**. 새 DB라 기존 개발 서버 데이터는 없음. (2) **이번 착수 = 리포트/스코어링만**(REQ-010/012 원안 일치, DEC-054 결함 해소). 화이트보드 VLM·WebRTC 미디어·LangChain 운영 전환은 이번 범위 밖. (3) **루브릭 방식 = 채용담당자 지정 + 항목별 채점**: 채용담당자가 면접에 템플릿을 지정하고(없으면 시스템 기본 템플릿), 그 템플릿의 항목·가중치별로 1~5점과 항목별 근거를 산출. 기존 3축 점수는 호환을 위해 유지. DB·API·화면 변경 수반 → 규칙 F에 따라 03 설계서 §3·§4.2·§4.4와 04 [C-11]/[R-02]를 먼저 고친 뒤 05→06 진행. (4) **유료 API 키 = 사용자가 추후 직접 `backend/.env`에 입력**(에이전트는 키 값을 입력하지 않음). 입력되면 실제 호출로 검증해 스펙 비교를 '일치'로 전환 | 사용자 응답 | High(DB 스키마 변경 — 마이그레이션 추가, 03 설계서 재작업) | 03-system-design.md, 04-ux-design.md, traceability.md REQ-010/012/014, 신규 unit(루브릭 채점), 스펙 비교 아티팩트, backend/.env(신규, 커밋 제외) |
| DEC-056 | 2026-09-23 | 6(unit-37 완료) | DEC-054가 대기 상태로 남겼던 루브릭 템플릿 연결 방향(DEC-055로 확정)의 구현·실측이 끝났는지, 그리고 unit-10/11의 FAIL을 대체해도 되는지 | **REQ-009/010/012 모두 unit-37로 PASS 전환**(traceability.md 갱신 완료). 실행 검증은 이번 세션이 이 PC에 직접 구축한 환경(Python 3.13, backend venv, Qwen2.5-1.5B GGUF+llama.cpp, Piper, faster-whisper, Docker DB/Redis, 프런트 node_modules — DEC-055)에서 수행. 구현 중 결함 2건을 발견 즉시 수정했다: (1) LLM이 `criteria_scores` 필드를 통째로 빠뜨리는 경우(3회 중 1회) → 전부 미채점이면 1회 재생성, (2) 리포트 전용 제한시간을 300초로 늘리며 `job_watchdog.py` 감시 타임아웃을 함께 분리하지 않아 정상 진행 중인 리포트를 유실로 오판할 뻔했던 것 → 작업 종류별 감시 타임아웃으로 수정(traceability 갱신 중 설계-구현 대조로 발견). **여전히 남은 것**: unit-10 DEF-003(파싱 실패 시 LLM 원문 노출), 진짜 워커 크래시 시 `queued` 영구 고착(복구 경로 없음), 503 QUEUE_FULL 미실측, Playwright 화면 검증 미실시, recruiter 템플릿을 통한 프롬프트 인젝션 테스트 미실시(09단계 인계) — 전부 unit-37-test.md §8에 후속 항목으로 기록됨. 07(통합테스트)은 여전히 전 feature 미착수 상태로 남아 있다 | 에이전트 자체판단(unit-37-test.md/verify-log 5회 검증 근거) + 사용자 승인(DEC-055 범위 내 진행) | Low(문서·상태 갱신, 코드는 이미 구현·검증됨) | traceability.md REQ-009/010/012, units/unit-37-note.md, units/unit-37-test.md, verify-log_unit-37-test.md |
| DEC-057 | 2026-09-23 | 5(스펙 비교 잔여 14개 미반영 항목 처리 방향) | 사용자가 "원안과 일치 안 되는 14개를 처리할 방법"을 요청(30시간 동안 2개만 반영돼 답답함 호소). 14개 중 5개(아키텍처 MSA·Oracle·VAD·500명 동시접속·지연시간)는 DEC-004/006/007로 처음부터 예산 없이 진행하기 위해 의도적으로 확정한 방향이라 코드로는 해소 불가 | 사용자 응답(AskUserQuestion 4개): (1) **유료 API 키(Deepgram/ElevenLabs/GPT-4V) = 곧 준비**해 제공 예정 → 받는 즉시 backend/.env에 반영해 실제 호출로 검증, 3개 항목 일치 전환 시도. (2) **해결 불가 5개 = 목표에서 그대로 둠**(제외 표시 안 함, 진행률 계산도 16개 기준 유지 — 사용자가 "그대로 둔다"를 명시적으로 선택). (3) **코드 샌드박스 실행 검증 = 사용자가 직접 로컬 실행**(`.harness-tmp/manual_sandbox_test.py` 신규 작성해 전달, Docker로 8개 TC 자동 실행). (4) **LangChain 전환 = 그대로 둠**(닫지 않음, "부분/대체"로 계속 표시) | 사용자 응답 | Low(분류·표시 방식 결정, 코드 변경 없음) | 스펙 비교 아티팩트(변경 없음), `.harness-tmp/manual_sandbox_test.py`(신규) |
| DEC-058 | 2026-09-23 | 6(unit-32 후속, Deepgram 실측) | 사용자가 Deepgram API 키를 제공(대화 중 스크린샷으로 노출됐으나 이후 .env 파일로만 취급, 대화에는 값 1회만 노출) — 이 키로 STT 어댑터를 실제로 검증할지 | 실제 호출로 검증. Piper 합성 음성 → Deepgram API 실호출 → 원문과 정확히 일치하는 한국어 텍스트 확인(`unit-32-test.md` 후속 실측 TC-002/003). `app/core/config.py`에 `deepgram_api_key`/`elevenlabs_api_key`/`openai_api_key` 필드 추가(둘은 아직 None). 운영 경로는 바꾸지 않음(DEC-005 유지, faster-whisper 그대로) — 이번 작업은 "전환"이 아니라 "검증"만 | 사용자 응답(키 제공) + 에이전트 실행 | Low(운영 코드 미변경, 검증 스크립트만 실행) | app/core/config.py, docs/harness/units/unit-32-test.md, traceability.md REQ-026, .harness-tmp/verify_deepgram.py(신규) |
| DEC-059 | 2026-09-23 | 5(무료 항목 범위 확정) | 사용자가 "원안에 유료는 모두 수정하지 않겠다"고 선언 — ElevenLabs(TTS)·OpenAI GPT-4V(화이트보드 비전) 유료 키 발급을 중단할지, 무료로 달성 가능한 항목들을 대신 정리해줄지 | 유료 키 발급 중단(ElevenLabs/OpenAI 보류 유지). 무료 대안 정리: (1) 코드 샌드박스 사용자 직접 실행(최우선), (2) 화이트보드 비전은 GPT-4V 대신 오픈소스 VLM(Qwen-VL 등) 로컬 구동으로 대체 가능, (3) K8s는 클라우드 대신 로컬 Minikube/kind로 무료 검증 가능, (4) GDPR/CCPA 남은 기술 조각은 계속 무료로 가능, (5) WebRTC 미디어서버는 무료(오픈소스)이나 개발 시간이 큼. 정정: 이전에 "예산 없이는 절대 불가" 5개 항목 중 아키텍처(MSA)·VAD는 실제로는 **돈이 아니라 개발 시간** 문제로 재분류(Oracle·GPU하드웨어 2개만 순수 예산 문제) — 진행 우선순위는 사용자 선택 대기 중 | 사용자 응답 | Low(범위·우선순위 결정, 코드 변경 없음) | 스펙 비교 아티팩트(향후 갱신 예정), Deepgram 외 유료 키 발급 중단 |
| DEC-060 | 2026-09-23 | 5(코드 샌드박스 보안 완화 요청 거절) | 사용자가 "코드 샌드박스의 보안 격리(네트워크 차단·읽기전용FS·비루트·capability제거·리소스제한)를 로컬 전용이므로 최소화해 달라"고 요청, 아울러 "이 세션(Claude Code)이 직접 샌드박스를 실행하도록 정책을 바꿔 달라"고 요청 | **둘 다 거절.** (1) 플랫폼 실행 제한: CLAUDE.md 등 프로젝트 지침으로는 Claude Code 플랫폼 자체의 안전 분류기(코드 실행 차단)를 바꿀 수 없음 — 프로젝트 정책의 적용 범위 밖. (2) 보안 완화 거절 사유: 이 기능이 실행하는 코드는 이 세션이 테스트로 넣는 코드가 아니라 **실제 서비스에서 면접 지원자가 제출하는, 신뢰할 수 없는 제3자 입력**이다. "로컬 서버 전용"이라는 배포 환경은 이 위험을 줄이지 않는다(공격 대상이 로컬 서버 자체가 됨). 네트워크 차단 제거 시 데이터 유출/내부망 공격, 읽기전용·권한최소화 제거 시 컨테이너 탈출 시 호스트 장악, 리소스 제한 제거 시 fork bomb로 호스트 전체 마비 가능 — 전부 실질적 피해로 이어지는 시나리오라 "바이브코딩이니 편의상 생략" 범주에 해당하지 않는다고 판단. 대신 사용자가 직접 로컬에서 실행해 검증하는 방법을 제시했고, 실제로 실행해 전부 PASS 확인함(unit-29-test.md) | 에이전트 자체판단(규칙 A-3 비가역적/영향범위 큰 결정 — 보안 완화는 되돌리기 쉬워도 그 사이 실제 서비스가 취약한 채로 운영될 위험이 있어 임의 진행 대신 거절 후 설명) | High(보안 원칙 — 제3자 신뢰불가 입력을 실행하는 기능의 격리 수준) | backend/app/services/code_sandbox.py(변경 없음, 그대로 유지됨), units/unit-29-note.md, units/unit-29-test.md |
| DEC-061 | 2026-09-23 | 5·6(unit-35 후속, 로컬 VLM 실연결) | 스펙 비교 아티팩트(`spec-compare.html`)의 "전환 가능성 매트릭스"에서 "부분착수"로 남아있던 10개 항목(자원필요/법무필요/보안승인필요/위험재승인필요/하드웨어투자) 중 사용자가 어느 것부터 진행할지 지정 요청. 화이트보드 AI비전은 렌더링만 끝난 상태(DEC-050)로, 남은 비전 호출부는 유료(GPT-4V)라 DEC-059(유료 중단) 방침과 충돌 | 사용자가 다중선택 질문에서 **"화이트보드 AI비전 로컬 VLM 연결"만** 선택(코드 샌드박스 배선·STT 실시간 VAD 재설계는 선택 안 함 — 둘 다 유료/보안승인/위험재승인이 필요해 질문에만 포함하고 임의 진행하지 않음). GPT-4V 대신 이미 다운로드돼 있던 **로컬 SmolVLM-500M-Instruct**(llama.cpp `--mmproj`, 포트 8093, Qwen 8091과 별도 프로세스)를 실제로 연결해 `POST /interviews/{id}/whiteboard/analyze` 엔드포인트를 신설, 프런트(`WhiteboardCanvas.tsx`)에 "AI 분석" 버튼과 저신뢰 disclaimer 표시를 추가. 실측 결과 모델 응답 품질이 매우 낮음(다이어그램 설명 요청에 한 글자 "2"만 반환)을 재확인했으나, 사용자가 이전에 "낮은 품질이라도 그대로 연결"하기로 결정했던 전제(직전 세션)를 유지해 품질 개선 없이 그대로 배선하고 disclaimer로 완화. Docker Desktop이 이 환경에서 기동되지 않아 실제 HTTP 왕복(인증 포함)은 검증하지 못함(서비스 계층 직접 호출 + 라우트 등록 확인까지만 실측) | 사용자 응답(AskUserQuestion, 4개 중 1개만 선택) | Medium(신규 엔드포인트 추가지만 GET/PUT 기존 계약은 무변경, 저품질 모델이라 disclaimer 없이 노출 시 오인 위험이 유일한 리스크) | whiteboard_vision.py(`analyze_diagram_local` 등 신규), schemas/whiteboard.py(`WhiteboardAnalysisOut`), api/v1/whiteboard.py(`POST .../analyze`), frontend/lib/api.ts, frontend/.../WhiteboardCanvas.tsx, unit-35-note.md §6, unit-35-test.md §4-1, traceability.md REQ-021(이번에 갱신), .harness-tmp(구 탐색 스크립트 정리) |
| DEC-062 | 2026-09-23 | 6(unit-35 후속, 실 HTTP 검증) | DEC-061 직후 남았던 유일한 검증 공백 — Docker Desktop이 이 환경에서 꺼져 있어 화이트보드 분석 엔드포인트의 실제 HTTP 왕복(인증 포함)을 검증하지 못한 상태. 사용자가 "도커 데스크톱 실행해서 실제 HTTP 검증해줘"라고 명시 요청 | Docker Desktop을 실제로 기동(최초 구동이라 약 4분 소요 — 사용자가 "계속 실패하는 것 같다"고 문의했으나 실패가 아니라 정상 지연이었음). 기존 `final-project-db`/`final-project-redis` 컨테이너가 자동 재기동됨을 확인(마이그레이션은 이미 head 상태). uvicorn 실행 후 httpx로 회원가입→로그인→면접생성→화이트보드저장→`POST .../whiteboard/analyze` 전 구간을 실제 HTTP로 검증 — 200 OK, SmolVLM 응답까지 정상 수신(TC-015). 테스트로 생성한 계정 2건·면접 2건·화이트보드 스냅샷 2건은 검증 직후 삭제, 테스트용 uvicorn/SmolVLM 프로세스도 종료(규칙 K). DB/Redis 컨테이너는 프로젝트 표준 개발 인프라이므로 유지 | 사용자 응답(명시적 지시) | Low(검증만 수행, 코드 변경 없음 — 테스트 데이터도 정리 완료) | unit-35-note.md §6/§7, unit-35-test.md TC-015/§9, traceability.md REQ-021(이번에 갱신) |
| DEC-063 | 2026-09-23 | 5·6(unit-32 후속, STT-Deepgram 실배선) | 사용자가 스펙 비교 아티팩트의 "부분착수(8)/코드준비·미검증(2)" 10개 항목을 보고 "완료로 만들 방법이 있는지" 질의, 동시에 "유료라서 진행 불가한 항목은 개발불가 그룹에 유료결제 필수로 표시해달라"고 요청 | 10개 항목을 사유별로 분류해 제시(순수 유료결제 필요 2건: TTS-ElevenLabs/벡터DB-Pinecone → 유료결제 필수 배지로 재분류, 무료로 완료 가능 3건: STT-Deepgram/코드샌드박스/K8s, 나머지 5건은 법무·테스트환경·개발기간 문제로 "유료결제 필수"가 부정확함을 설명) 후 다중선택 질문. **사용자가 "STT-Deepgram 배선 작업 진행"만 선택**(TTS/벡터DB 재분류, 코드샌드박스 배선, K8s는 선택 안 함 — 임의 진행하지 않음). Deepgram은 이미 키 검증까지 끝난 상태라 결제 없이 순수 개발로 완료 가능하다고 판단한 항목. 구현: `stt_adapter_deepgram.py`의 `Content-Type` 하드코딩을 실제 업로드 MIME 타입 인자로 교체, 신규 `app/services/stt_router.py::transcribe_audio_smart()`(키 있으면 Deepgram 우선, 없거나 실패 시 faster-whisper로 그레이스풀 폴백 — 운영 기본값은 여전히 무료 로컬 경로, DEC-005 유지), `app/api/v1/interviews.py`의 실제 음성 제출 2개 경로(`_submit_voice_turn`, unit-36 미리보기)에 배선. 직접 함수 호출(TC-004) + Docker 기동 상태에서 uvicorn 실 HTTP E2E(TC-005: 회원가입→로그인→동의→면접생성→start→음성턴제출→transcript 확인, 202/200, Deepgram 인식 결과와 정확히 일치)까지 검증 완료. 테스트 계정/동의/면접/transcript는 검증 직후 스크립트로 삭제, uvicorn 프로세스 종료(규칙 K) | 사용자 응답(AskUserQuestion, 4개 중 1개만 선택) | Medium(실제 서비스 음성 제출 경로 수정이나, 키 미설정 시 기존 faster-whisper 동작과 100% 동일하게 폴백 — 회귀 위험 낮음) | backend/app/services/stt_adapter_deepgram.py, backend/app/services/stt_router.py(신규), backend/app/api/v1/interviews.py, unit-32-test.md(후속 배선 TC-004/005), traceability.md REQ-026(이번에 갱신), spec-compare 아티팩트(③ 매트릭스 STT 완료 전환, TTS/Pinecone 유료결제필수 재분류) |
| DEC-064 | 2026-09-24 | 5·6(unit-15 후속, 로컬 서버 기동 후 장애 신고 진단) | 사용자가 로컬 백엔드/프런트 서버를 기동해 사용하던 중 (1) "새 면접 시작" 클릭 시 `/interviews/{id}/consent`가 404, (2) 지원자 홈의 면접 목록이 클릭마다 1~2건씩 늘어나는 현상을 스크린샷과 함께 신고하며 "다른 컴퓨터에서는 문제없었다"고 밝히고 "이전 소스처럼 정상 동작하도록 검토 후 소스를 복원해달라"고 요청 | 임의로 소스를 되돌리지 않고 먼저 실측으로 원인을 규명함(요청 원문 "검토해 보고"를 문자 그대로 따름). **(1) 404**: `git status`로 해당 라우트 파일이 저장소에 정상 존재·최신 상태임을 확인, 디렉터리명 바이트 검사로 손상 없음 확인, `curl` 응답의 RSC 트리로 Next.js 라우터가 해당 경로를 아예 인식하지 못함을 확인, 대조 테스트로 "동적 세그먼트 하위의 정적 하위 라우트"만 선택적으로 깨져 있음을 확인 → `frontend/.next` 삭제 후 재기동하자 즉시 정상화 — **이 컴퓨터에 `git pull` 이전부터 남아있던 Next.js(Turbopack) 로컬 빌드 캐시가 새로 반영된 라우트 파일을 인식하지 못한 이 컴퓨터만의 로컬 캐시 문제였고, 소스 코드에는 결함이 없었다**(되돌릴 소스 자체가 없음). **(2) 목록 중복 생성**: 실제로는 별개의 진짜 소스 결함(DEF-002)이었음을 발견 — `frontend/app/interviews/new/page.tsx`의 `useEffect`가 React(개발 모드) Strict Mode의 마운트→클린업→재마운트 이중 실행 때문에 `createInterview()`를 실제로 2번 호출하고 있었다(`cancelled` 플래그는 결과 사용만 막을 뿐 호출 자체는 막지 못함). 진단 전용 신규 계정으로 실측 재현(클릭 1회 → `POST /interviews` 201이 정확히 2회 관측) 후 `useRef` 가드로 수정, 같은 계정으로 재검증(클릭 1회 → 201이 정확히 1회, 목록도 정확히 1건만 증가, 사전고지 화면도 정상 렌더링)까지 완료. 사용자의 실제 계정(정찬성님)이 이미 만든 테스트 데이터는 사용자 소유라 임의 삭제하지 않았고, 삭제를 원하면 말씀해달라고 안내했다. 진단 전용 계정과 그 계정이 만든 면접 3건은 검증 직후 삭제(규칙 K) | 사용자 응답(명시적 지시 — 원인 검토 후 정상 동작 복원) | Low(코드 변경은 `frontend/app/interviews/new/page.tsx` 1개 파일, `useRef` 가드 추가뿐 — 기존 동작을 프로덕션에서 바꾸지 않고 개발 모드 중복 호출만 제거. 캐시 삭제는 코드 변경이 아님) | frontend/app/interviews/new/page.tsx(DEF-002 수정), frontend/.next(로컬 캐시 삭제, 추적 대상 아님), unit-15-test.md §11(신규, 진단·수정·재검증 전 과정 기록) |
| DEC-065 | 2026-09-24 | 6(unit-15 후속, DEC-064 수정의 자체 회귀 발견·수정) | DEC-064에서 "완료"로 보고한 DEF-002 수정을 사용자가 직접 재테스트했더니, "새 면접 시작" 클릭 후 화면이 "새 면접 세션을 만드는 중입니다..."에서 무한정 멈추고 브라우저에 hydration 에러 배지("1 Issue")가 뜬다고 스크린샷 2장과 함께 재신고. 사용자가 "테스트를 제대로 한 게 맞느냐"고 강하게 항의 | **정당한 지적이었다 — 인정하고 처음부터 다시 검증함.** DEC-064의 재검증은 "네트워크 요청이 1회만 나갔는가"만 확인했을 뿐 "그 요청의 성공 결과로 실제 화면이 다음 단계로 넘어가는가"를 실제 화면까지 끝까지 확인하지 않았다 — 그 사이에 새 결함(DEF-003)이 숨어 있었다. 원인: DEC-064의 `useRef` 가드 수정이 기존 `cancelled` 클린업 로직을 그대로 남겨둔 채였는데, React Strict Mode 순서상(1차 마운트[실제 호출 발생]→1차 클린업[cancelled=true 세팅]→2차 마운트[ref로 스킵]) 유일하게 살아있는 요청의 `.then` 핸들러가 항상 이미 `true`로 세팅된 `cancelled`를 보게 되어, 서버는 세션을 정상 생성하는데도 클라이언트가 그 성공 결과를 영원히 무시하고 화면 전환을 하지 않는 새로운 결함을 만들었던 것. (참고로 사용자가 함께 신고한 hydration 경고("1 Issue")는 별개 원인 — `<html>/<body>`에 `user-select:"auto"`가 주입된 흔적으로 보아 브라우저 확장 프로그램이 React 로드 전에 DOM을 건드린 전형적 패턴이며, Next.js 공식 경고문에도 명시된 원인이다. 완전히 새로운 탭+새 계정으로 재현했을 때 화면 멈춤은 동일하게 발생했지만 hydration 경고는 없었다는 점으로 "두 현상이 서로 다른 원인"임을 확인했다. 이 경고 자체가 화면 멈춤의 원인은 아니었다.) 수정: `cancelled` 클린업을 완전히 제거(ref가 이미 "실제 호출 1회"를 보장하므로 더 이상 필요 없음). 재검증: 이번엔 네트워크 로그뿐 아니라 완전히 새 브라우저 탭 + 완전히 새 계정으로 (1) 클릭 후 사전고지 화면 전문이 실제로 렌더링되는지 페이지 텍스트로 직접 확인, (2) `POST /interviews`가 정확히 1회인지, (3) 지원자 홈 목록이 정확히 1건만 느는지, (4) 콘솔 에러가 0건인지까지 4가지(TC-006~009)를 전부 통과 확인 후 보고 | 사용자 응답(강한 이의제기 — 재검토·재수정 명시 요구) | Low(코드 변경은 동일 파일에서 `cancelled` 클린업 블록 제거뿐, 순수 축소 변경) | frontend/app/interviews/new/page.tsx(DEF-003 수정 — cancelled 클린업 제거), unit-15-test.md §12(신규, DEF-003 원인·수정·TC-006~009 재검증 기록, §11의 PASS 판정을 FAIL로 정정) |

| DEC-066 | 2026-09-24 | 6(로컬 서버 3종 프로세스 중 1개 누락 — 환경설정 문제, 소스 결함 아님) | DEF-003 수정 확인 후 사용자가 실제 면접을 진행하다 "면접장" 화면에서 "AI 면접관이 첫 질문을 준비하고 있습니다"가 끝나지 않고 "첫 질문을 불러오는 데 예상보다 시간이 걸리고 있습니다"로 넘어가는 현상을 스크린샷과 함께 신고, "다른 컴퓨터에서는 잘 나왔다"고 지적 | 코드를 보기 전에 먼저 프로세스 목록부터 확인 — `celery -A app.services.celery_app worker --concurrency=1 --pool=solo -Q ai_pipeline` 프로세스가 **아예 떠 있지 않았음**을 확인. unit-7-note.md가 이미 "이 기능 검증에는 DB/Redis + Celery 워커 + uvicorn 3종 프로세스가 모두 떠 있어야 하며, 워커가 없으면 POST /turns(혹은 /start)는 202를 반환하지만 WS로 결과가 영원히 오지 않는다"고 명시한 바로 그 상황이었다 — 이번 세션이 "로컬서버 띄워줘" 요청을 처리할 때 uvicorn·Next.js(프런트) 2종만 .claude/launch.json에 등록하고 **Celery 워커를 빠뜨린 것**이 원인. 소스 코드 결함이 아니라 이 세션의 로컬 환경 구성 누락이었다. Celery 워커를 기동한 뒤(--pool=solo, DEC-019 그대로), httpx E2E 스크립트로 재현 계정을 만들어 면접 시작→오프닝 질문 생성까지 폴링 검증(16.8초 만에 정상 수신) → 이어서 실제 브라우저(신규 탭+신규 계정)로 회원가입→로그인→새 면접 시작→사전고지 동의→면접 시작까지 **사용자와 완전히 동일한 절차**를 재현해 WS로 오프닝 질문("간단히 자기소개와 함께...")이 실제 화면에 렌더링되는 것까지 확인, 콘솔 에러 0건도 확인. 진단용 계정 2개와 그 인터뷰는 검증 직후 삭제 | 사용자 응답(신고 + 강한 어조의 재요구) | Low(코드 변경 없음 — 누락된 로컬 프로세스를 추가로 기동한 것뿐) | (코드 변경 없음), unit-15-test.md §13(신규, 검증 기록) |

| DEC-067 | 2026-09-24 | 6(스펙 비교 아티팩트 재분류) | 사용자가 "부분착수에서 완료할 수 있는 건 알려주고, 개발불가에서 유료서비스라서 개발불가라면 따로 분리해달라"고 요청 | (1) 화이트보드 AI비전(unit-35 후속)을 ③ 매트릭스 "부분착수"에서 "완료"로 재분류 — 2026-09-23 DEC-061/062에서 이미 실제 서비스 경로 배선(POST .../whiteboard/analyze) + 인증 포함 실HTTP E2E 검증까지 끝낸 상태였는데, 같은 날 STT-Deepgram은 동일한 근거(배선+실HTTP검증)로 "완료" 처리(DEC-063)하면서 화이트보드는 "부분착수"로 남겨둔 것이 일관성 없는 분류였음을 발견 — 동일 기준 적용해 바로잡음(코드 변경 없음, 문서 재분류만, 대시보드 카운트 완료6→7/부분착수7→6 갱신). (2) ③ 매트릭스 "개발 불가" 5개 그룹에 시각적 서브그룹 구분 추가 — "💳 유료결제 필수 2개(TTS-ElevenLabs, 벡터DB-Pinecone)" vs "⚙ 하드웨어·아키텍처 근본 한계 3개(VAD, Oracle, 500명동접)"를 별도 헤더 행으로 분리(`.subgrouprow` CSS 신규). 나머지 부분착수 6개(WebRTC시그널링·DeepFace·K8s매니페스트·GDPR데이터이동권·STT실시간스트리밍·코드샌드박스) 각각에 대해 "완료 가능한지" 분석해 채팅으로 답변: DeepFace/GDPR은 법무 판단이 필요해 코드 작업만으로 완료 불가, WebRTC는 수주 단위 대규모 작업이라 즉시 완료 불가, K8s는 로컬 minikube/kind로 완료 가능성 있음, STT실시간스트리밍은 이번 세션이 consent 화면에서 스크롤게이트 우회 기법을 실증해 이제 기술적으로 재검증 가능, 코드샌드박스는 기술 검증은 끝났으나 배선 자체가 보안정책상 별도 사용자 승인 필요 — 이 3개(K8s/STT streaming/코드샌드박스 배선) 진행 여부는 사용자 선택 대기 | 사용자 응답(명시적 요청) | Low(문서 재분류·구조 개선뿐, 코드 변경 없음) | spec-compare 아티팩트(③ 매트릭스 화이트보드 완료 전환, 개발불가 서브그룹 분리) |

| DEC-068 | 2026-09-24 | 6(unit-36 후속, STT 실시간 스트리밍 재검증 시도) | 사용자가 이전 답변에서 제시한 3개 완료가능 후보 중 "STT 실시간 스트리밍 실브라우저 검증"을 선택해 진행 요청 | 신규 진단 계정으로 사전고지 동의 화면의 스크롤 게이트를 JS로 강제 스크롤(scrollTop=scrollHeight + scroll 이벤트 디스패치)해 실제로 통과, 면접장(LIVE) 진입까지 성공 — unit-36-test.md §8이 지목했던 그 블로커는 실제로 해소됨을 확인. 그러나 "음성으로 답변" 클릭 시 이 세션이 쓰는 Browser pane 환경 자체가 마이크 장치 접근을 플랫폼 수준에서 차단한다는 시스템 안내를 받음(getUserMedia 자체가 이 환경에서 근본적으로 불가) — 스크립트로 우회 가능한 제약이 아니었다. 코드는 이 상황을 04-ux-design [C-06] 명세대로 정확히 처리(그레이스풀 폴백, "마이크 권한이 거부되었거나..." 안내 후 텍스트 입력 유지)함을 확인했으나, TC-010의 본래 목적(실제 음성 스트리밍 STT 동작 확인)은 여전히 검증하지 못함 — 판정 CONDITIONAL PASS(부분착수) 유지, 완료로 승격하지 않음(정직하게 보고). 사용자가 실제 마이크 있는 일반 브라우저에서 직접 시도해야 최종 검증 가능함을 안내 | 사용자 응답(선택지 중 선택) | Low(코드 변경 없음, 검증 시도만 — 결과가 negative여도 그대로 보고) | unit-36-test.md §11(신규, 시도 경위·결과 기록, 판정 불변) |

| DEC-069 | 2026-09-24 | 5·6(unit-29 후속, 코드샌드박스 API 배선 — 사용자 명시 승인) | 사용자가 "K8s 로컬 클러스터 검증"·"코드샌드박스 API 배선" 둘 다 진행해달라고 명시 승인("모두 진행해 주세요, 최대한 완료를 모두 하고 싶어요") — unit-29-note.md §9/§10이 "별도 사용자 승인 필요"로 못박아뒀던 그 승인 | DEC-060(보안 격리 완화 거절)은 그대로 유지한 채, 격리 수준을 전혀 건드리지 않고 `POST /interviews/{id}/code-submissions/execute` 엔드포인트만 신규 추가. 방어 순서: 소유권 검사 → 전용 레이트리밋(분당 5회, 신규 `check_and_increment_sandbox_rate_limit`, 다른 두 한도보다 보수적) → `code_sandbox.execute_code_sandboxed()` 실행. 실행 결과는 DB에 저장하지 않음(저장/실행 완전 분리 원칙 유지). 프런트 `CodeEditorPanel.tsx`(이미 면접장에 배선돼 있던 컴포넌트)에 "실행" 버튼 추가(Python/JavaScript만 활성화). **부수 발견**: unit-29-note.md가 "이 세션 자체는 플랫폼이 코드 실행을 차단한다"고 기록했던 전제를 이번에 재확인했더니 실제로는 차단되지 않고 정상 실행됨(TC-010) — 세션/정책에 따라 달라질 수 있어 "항상 가능"을 보장하진 않는다고 명시. 검증: 서비스 계층 직접호출 + httpx 실HTTP E2E(정상실행 2개 언어, 미지원언어 422, 네트워크격리, 수평권한상승 방지 404, 레이트리밋 429) + 실 브라우저 UI(신규 탭·계정으로 회원가입부터 코드 실행 버튼 클릭까지 전 구간, 콘솔에러 0건)까지 총 8개 TC 전부 Pass(unit-29-test.md §11 TC-010~017). 테스트 데이터 전부 검증 직후 삭제, 좀비 컨테이너 0건 확인(규칙 K) | 사용자 응답(명시적 승인) | Medium(신뢰 불가 제3자 코드를 실제로 실행하는 새 엔드포인트 — 단 기존 9중 격리는 무변경, 레이트리밋·소유권검사로 방어) | backend/app/services/prompt_safety.py(레이트리밋 함수 신규), backend/app/schemas/code_submission.py(스키마 신규), backend/app/api/v1/code_submissions.py(엔드포인트 신규), frontend/app/interviews/[id]/components/CodeEditorPanel.tsx(실행 버튼 신규), unit-29-test.md §11(신규, TC-010~017) |
