# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/units/unit-11-test.md` (+ 함께 작성한 `units/unit-11-note.md`, `traceability.md` REQ-010/012 행)
- 작성 에이전트: 본 세션(오케스트레이터, 05 소급 문서화 + 06 역할)
- 적용 Tier: High — 최소 2회
- 버전: v0 → v1 → v2 → 최종

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 작성자(06 테스터)
- 일시: 2026-09-23
- 체크리스트
  - [x] 입력 계약: 02 §4.2 68·69행 원문("사전 정의 루브릭 템플릿", "어떤 답변 구간에 어떤 루브릭") 기준으로 판정했는지 확인
  - [x] 출력 계약: 10개 절 모두 있음
  - [x] 상위 산출물과 모순 없음: 03 §4.4 턴 계약의 `rubric_match` 등 필드 존재를 원문으로 확인(03-system-design.md 446행)
  - [x] 추측 항목: "0건" 주장은 grep 명령을 결과서에 그대로 적어 재현할 수 있게 함
  - [x] 구조적 결함: 아래 결함 3
  - [x] 형식 오류: 아래 결함 1·2
- 재확인한 근거:
  - `grep -rni rubric backend/app/worker backend/app/services/interview_prompts.py backend/app/services/llm_engine.py` → 1건(`llm_engine.py:82 rubric_match` 선언뿐)
  - `grep -rn "\.details\b" frontend/app frontend/components` → 0건
  - `key_observations`·턴 점수의 저장 코드 없음(`tasks.py`의 `technical_accuracy` 참조는 리포트 출력 쪽뿐)
- 발견된 결함 목록:
  1. 7절 `git status`를 실제 출력 없이 "같은 작업 트리"로 요약했다(템플릿 위반).
  2. 10절 검증 요약이 검증 전에 미리 적혀 있었다.
  3. 1절 관련 산출물에 루브릭 템플릿 화면 명세(04 [R-03])가 빠져 있었다. DEF-001의 "템플릿 구조(항목명+가중치/설명)" 근거가 이 화면이다.
- 조치 내용(→ v1): 결함 3은 [R-03]을 추가해 해결. 결함 1·2는 최종 단계에서 실제 출력과 이 로그 기준으로 다시 쓴다(3차에서 확인).

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
- 검증자(역할): 처음 받은 심사자(기획·설계 20년차 관점)
- 일시: 2026-09-23
- 심사 질문: "설계서가 정말 템플릿 연결을 정의하지 않았는가? 그리고 연결하려면 무엇이 먼저 필요한가?"
  - `grep -n "criteria_json|rubric_template_id|루브릭 템플릿"`으로 02/03/04 전체 확인 → ERD FK(03 243·263행), 템플릿 컬럼(308행), [R-03] 폼 구조(04 230행)뿐. 채점 연결 정의 없음 → 결과서 주장 유지
  - `create_interview`(`interviews.py` 165행)와 `InterviewOut` 확인 → **`rubric_template_id`를 쓰는 곳이 없어 항상 null**
- 발견된 결함 목록:
  1. 결과서가 "읽는 곳이 없다"만 지적하고 **"쓰는 곳도 없다"(면접에 템플릿을 지정하는 경로 자체가 없음)**를 놓쳤다. 그 결과 8절의 사용자 질문 선택지가 "채점 반영 방식"만 다루고, 먼저 정해야 할 "지정 주체"가 빠져 있었다. 그대로 질문하면 사용자가 답해도 구현이 막힌다.
- 조치 내용(→ v2): TC-006 실제 결과, DEF-001 설명, 8절 사용자 결정 항목(지정 주체 + 반영 방식 두 가지로 확장)을 수정했고, unit-11-note.md §2의 1번에도 반영했다. DEC-054는 append-only라 수정하지 않았다(이 추가 사실은 이 로그와 결과서가 근거가 된다).

## 3차 검증 (2차에서 결함이 나와 반복)
- 검증자(역할): 독립 심사자(전체 재독)
- 일시: 2026-09-23
- 확인 사항:
  - [x] 7절에 실제 `git status` 출력이 붙어 있다(최종 반영 후 확인)
  - [x] 10절 요약이 이 로그(1차 3건, 2차 1건, 3차 0건)와 같다
  - [x] traceability REQ-010/012 행이 결과서(FAIL, DEF-001/002 High, DEF-003 Medium)와 일치한다
  - [x] 8절 선택지 들여쓰기(하위 목록)가 올바르게 렌더링된다
- 발견된 결함 목록: 없음(0건)

## 최종 판정
- [x] PASS (결함 0건, 3회 검증 완료) — 결과서 문서가 완결되었다는 뜻이다. unit-11 자체의 판정은 FAIL 그대로다.
- [ ] PASS (Low 등급, 1차 검증 결함 0건으로 2차 생략)
- [ ] FAIL
