# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/units/unit-10-test.md` (+ 함께 작성한 `units/unit-10-note.md`, `traceability.md` REQ-009 행)
- 작성 에이전트: 본 세션(오케스트레이터, 05 소급 문서화 + 06 역할)
- 적용 Tier: High — 최소 2회, 1차 결함 0건이어도 2차 생략 불가
- 버전: v0(초안) → v1(1차 조치) → v2(2차 조치) → 최종

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 작성자(06 테스터)
- 일시: 2026-09-23
- 체크리스트
  - [x] 입력 계약 반영: 03 §4.2/§4.4, 04 [C-10]/[C-11], DEC-031/053 반영 확인
  - [x] 출력 계약: 템플릿 10개 절이 모두 있음
  - [x] 상위 산출물과 모순 없음: 상태값·응답 코드·재시도 조건을 03 §4.2 원문과 다시 대조
  - [x] 추측 항목 표시: 정적 추정(DEF-001)은 "미실측"으로, 기존 기록은 "인용"으로 구분
  - [x] 구조적 결함: 아래 결함 1·3
  - [x] 형식 오류: 아래 결함 2
- 재확인한 근거(코드 재대조):
  - `job_queue.enqueue_report_generation_job` → `register_job(...)` 호출 확인: 리포트 job도 watchdog 감시 대상
  - `job_watchdog._STARTED_TIMEOUT_SECONDS = 150`, 파일 안에 `report_status` 참조 0건: watchdog은 상태를 바꾸지 않음
  - `tasks.py` 163·170행의 유출 검사(`contains_persona_leak_marker`)는 턴 경로에만 있고 리포트 경로(345행 이후)에는 없음: DEF-003 근거 확인
  - `report/page.tsx` `POLL_INTERVAL_MS = 3000`, 폴링 횟수·시간 상한 없음
  - 리포트 관련 파일의 마지막 변경 커밋이 `2def886`임을 `git log -1`로 확인. HEAD는 `05f8d1a`
- 발견된 결함 목록:
  1. 7절 `git status`가 실제 실행 결과가 아니라 **예상 목록을 미리 적은 것**이었다(템플릿 "그대로 첨부, 요약 금지" 위반, 사실상 조작된 기록).
  2. 10절 검증 요약이 **검증을 하기 전에 미리 적혀 있었다.**
  3. DEF-002 설명에 "왜 복구되지 않는지"의 근거(watchdog이 150초 뒤 WS error만 발행, 리포트 화면은 WS를 쓰지 않음)가 빠져 있어 재현자가 원인을 오해할 수 있었다.
- 조치 내용(→ v1):
  - 결함 1: 모든 문서 작성이 끝난 뒤 실제 `git status` 출력을 7절에 붙이기로 하고, 3차 검증에서 실제로 붙였는지 확인한다.
  - 결함 2: 이 로그를 기준으로 10절을 다시 쓴다(최종 단계에서 반영).
  - 결함 3: DEF-002 설명을 보강했다(register_job, 150초, 폴링 상한 없음 명시).

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
- 검증자(역할): 이 문서를 처음 받은 심사자(보안·설계 20년차 관점)
- 일시: 2026-09-23
- 체크리스트
  - [x] 1차 지적 반영: 결함 3은 반영 확인. 결함 1·2는 최종 단계 반영 예정(3차에서 확인)
  - [x] 엣지 케이스: 긴 대화(TC-011), 워커 중단(TC-012), 파싱 실패(TC-005/013), 권한(TC-004), XSS(TC-009) 포함. 동시성은 solo 풀 직렬이라 단위 범위 밖이라는 근거 명시
  - [x] 다음 단계 에이전트가 추가 질문 없이 착수 가능: 8절에 개발 서버 실측 절차 6개 항목 명시
  - [x] 비가역 결정 근거: 이번 작업은 코드 무변경(DEC-053, Low)
  - [x] 보안·운영 위험: DEF-003(유출 검사 부재)을 Medium으로 등록
- 심사자가 제기한 의문과 판단:
  - "PASS가 아니라 FAIL로 판정한 것이 과한가?" → L3 정식 06은 실행 검증이 필수인데 실행하지 못했다. 이 세션 원칙("실측한 것만 완료", DEC-037)상 CONDITIONAL PASS로 두면 07이 착수 가능하다고 오해할 수 있다. FAIL 유지가 맞다. 9절에 "기능이 전혀 안 된다는 뜻이 아님"을 이미 명시했다.
- 발견된 결함 목록:
  1. DEF-001을 **실측 없이 High로 확정**했다. 심각도 판단 근거와, 실측 결과에 따라 재분류할 기준이 없었다.
- 조치 내용(→ v2): 심각도를 "잠정 High"로 바꾸고 근거(재시도로도 복구되지 않는 영구 실패)와 재분류 기준(임계 턴 수가 실사용 범위 약 10턴 밖이면 Medium)을 적었다.

## 3차 검증 (2차에서 결함이 나와 반복)
- 검증자(역할): 독립 심사자(2차와 같은 관점으로 전체 재독)
- 일시: 2026-09-23
- 확인 사항:
  - [x] 7절에 실제 `git status` 출력이 붙어 있다(최종 반영 후 확인)
  - [x] 10절 요약이 이 로그 내용(1차 결함 3건, 2차 1건, 3차 0건)과 같다
  - [x] traceability REQ-009 행이 결과서 판정(FAIL, 정적 검토판, 결함 3건)과 일치한다
  - [x] unit-10-note.md의 파일·함수 이름이 실제 코드와 일치한다(grep으로 재확인)
- 발견된 결함 목록: 없음(0건)

## 최종 판정
- [x] PASS (결함 0건, 3회 검증 완료) — **이 문서(결과서)가 완결되었다는 뜻이다.** 결과서가 내린 unit-10의 판정은 FAIL이며, 그 판정 자체는 바뀌지 않는다.
- [ ] PASS (Low 등급, 1차 검증 결함 0건으로 2차 생략)
- [ ] FAIL
