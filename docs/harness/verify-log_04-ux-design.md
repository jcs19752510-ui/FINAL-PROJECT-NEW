# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/04-ux-design.md` (및 동시 산출물 `docs/harness/decisions.md` DEC-022 추가분)
- 작성 에이전트: `04-ux-designer`
- 적용 Tier (Low/Standard/High): **High** (`decisions.md` DEC-002 — 규칙 B 완화 조항 적용 불가, 최소 2회 검증 원문 그대로 적용)
- 버전: v1 (검증 중 결함 3회 수정 반영본)

## 1차 검증 (작성자 관점 자가 재검토 — "모든 핵심 플로우가 설계서의 API/데이터와 정합하는지")
- 검증자(역할): `04-ux-designer` 본인
- 일시: 2026-09-19
- 체크리스트
  - [x] 입력 계약(02-planning §PASS, 03-system-design §PASS, traceability.md, decisions.md DEC-001~021)을 실제로 반영했는가 — 02-planning 전체 In-Scope Feature A~H, 03-system-design 전체 9개 섹션(아키텍처/스택/ERD/API/NFR/보안/운영/트레이드오프)을 재대조.
  - [x] 출력 계약에 명시된 8개 섹션(핵심플로우/화면명세/디자인시스템/컴포넌트/접근성/반응형/정합성체크/변경이력)이 빠짐없이 존재하는가 — 전부 존재 확인.
  - [x] 지시사항 1~5번(지원자 우선 플로우, 채용담당자 최소 뷰어, 큐 대기 정식 화면상태, API 응답 필드 매핑, 규제 화면 REQ-029~034 포함)을 실제로 수행했는가 — 확인.
  - [x] 각 화면이 사용하는 데이터 필드가 03-system-design ERD/API에 실제로 존재하는가(핵심 검증 항목) — **아래 결함 참고, 다수의 실질적 불일치 발견**.
  - [x] 빈 상태/에러 상태/로딩 상태가 모든 화면에 정의되어 있는가 — 20개 화면 전수 확인, 형식상 누락 없음(다만 내용 정확성은 결함 목록 참고).
- 발견된 결함 목록:
  1. **(API 정합성 위반, 심각)** [C-04] 화면이 "동의하고 시작"/"텍스트 전용으로 계속" 두 개의 진행 버튼을 제공하도록 설계되어 있었으나, 03-system-design §6.2는 `POST /interviews/{id}/start` 호출 전 미들웨어가 `CONSENTS(biometric_voice)` 존재 여부를 검사해 없으면 무조건 `403 CONSENT_REQUIRED`를 반환한다고 명시한다. 즉 텍스트 전용으로 세션을 시작하는 API 경로 자체가 존재하지 않는데도 화면이 그런 경로가 있는 것처럼 설계되어 있었다 — "설계서에 없는 데이터/동작을 화면에 요구하지 않는다"는 필수 원칙 위반.
  2. **(모호한 인터랙션)** [C-06] 큐 대기 중 "텍스트로 즉시 진행" 버튼이 어떤 API 동작을 트리거하는지(대기열 순서를 건너뛰는지, 새 턴이 다시 큐에 들어가는지) 명시되지 않아 프론트엔드 개발자가 구현 방식을 추측해야 하는 상태였다.
  3. **(정합성 미확인)** [C-11] STAR 피드백을 "상황/과제/행동/결과별 아코디언"으로 설계했으나, 03-system-design ERD(`EVALUATION_REPORTS`)에는 이를 뒷받침하는 4개 분리 필드가 없고 `summary_text` 단일 텍스트 필드만 존재한다 — 없는 데이터 구조를 있는 것처럼 그린 결함.
  4. **(누락)** [C-06] 세션 최초 진입 시 AI의 "첫 질문"이 어떤 API/WS 경로로 전달되는지가 03단계 명세에 없는데도 이를 지적하지 않고 "찰나의 로딩 상태"로만 얼버무렸다.
  5. **(용어 오류)** §5 접근성 기준에서 [C-04]를 "동의 모달"로 지칭했으나, §2/§1의 실제 정의는 독립된 화면(라우트)이며 모달이 아니다 — `Esc` 키 동작 설명이 존재하지 않는 UI 요소에 대한 것이었다.
  6. **(컴포넌트-화면 간 참조 누락)** §4에 `RubricEvidenceAccordion`의 `empty` variant가 정의되어 있으나 [C-11] 화면 명세의 "빈 상태" 행은 이를 활용하지 않고 단순히 "해당없음"으로 처리해, 실제로 존재 가능한 빈 데이터 케이스(`details_json` 비어있음)를 놓치고 있었다.
  7. **(누락)** [R-02](채용담당자 리포트 열람)가 REQ-034(법률자문 고지) 배너를 [C-11]과 동일하게 포함하는지 명시하지 않아, "recruiter 화면이니 생략해도 되는지" 개발자가 판단할 근거가 없었다.
  8. **(과신)** §3.1 색상 대비값을 헥스코드 근사 계산만으로 제시하면서 "AA 통과"를 단정적으로 표기해, 실제 렌더링 검증 없이 확정된 값처럼 보일 위험이 있었다.
- 조치 내용: (수정 후 → v1)
  1. [C-04]를 두 동의(ai_interview_notice, biometric_voice) 모두 필수인 단일 게이트로 재설계, "텍스트 전용으로 계속" 우회 버튼 제거. 03단계 실제 동작(전체 차단)을 그대로 반영하되, 이 정책이 개인정보보호법 자유동의 원칙과 긴장 관계가 있을 수 있음을 §7.3 확인 필요 항목 6으로 별도 기록(임의로 화면에서 우회 경로를 만들지 않되, 상위 단계 재검토 필요성은 명시).
  2. [C-06] 큐대기 상태 설명에 "텍스트로 즉시 진행은 대기열 순서를 건너뛰지 않으며 새 턴도 다시 큐에 들어가되 STT 단계가 없어 처리 자체가 짧다"는 점, 그리고 이 버튼이 음성 턴이 대기 중일 때만 노출된다는 조건을 명시.
  3. [C-11]을 "STAR 4단계 독립 아코디언"에서 "summary_text 기반 단일 문단형 텍스트"로 재설계하고, 데이터 세분화가 필요하면 03단계 보강이 필요함을 §7.3 확인 필요 항목 7로 기록.
  4. [C-06] 빈 상태 행에 첫 질문 전달 메커니즘 미정의 사실을 명시하고 §7.3 확인 필요 항목 8로 기록, 3가지 가능한 구현 방식 모두에서 화면이 깨지지 않도록 설계 원칙을 병기.
  5. §5-3에서 [C-04]를 "독립된 화면(라우트)"으로 정정하고 `Esc` 관련 서술을 모달(C-09 등)에만 한정, [C-04]의 fail-closed 근거를 서버측 `403 CONSENT_REQUIRED`로 재서술.
  6. [C-11] 빈 상태 행을 "리포트 레코드는 항상 존재하되 details_json이 비어있을 수 있음 → RubricEvidenceAccordion empty variant로 표시"로 수정.
  7. [R-02] 구성요소에 "법률자문 고지 배너 REQ-034 포함 — recruiter 화면이라고 생략하지 않음"을 명시적으로 추가.
  8. §3.1에 "대비값은 근사 계산이며 05단계에서 실제 렌더링 색상으로 도구 재검증 필요"라는 주의 문구 추가.

## 2차 검증 (독립 심사자 관점 — "이 문서만 보고 화면을 구현할 프론트엔드 개발자")
- 검증자(역할): "오늘 처음 이 디자인서를 받아 05단계 프론트엔드 구현에 착수하는 개발자" 관점
- 일시: 2026-09-19
- 체크리스트
  - [x] 1차에서 지적된 8건이 실제로 파일에 반영되었는가 — `Read`로 각 절을 재확인해 반영 여부를 직접 검증함.
  - [x] §7.3 "확인 필요 항목" 표의 갭 번호와 각 화면 명세 본문이 인용하는 "확인 필요 항목 N" 번호가 서로 일치하는가(개발자가 표를 찾아갈 때 헷갈리지 않는지) — **불일치 발견**.
  - [x] 문서 내부 상호참조(§N.N 형태)가 이 문서 자신을 가리키는지 03-system-design을 가리키는지 모호한 곳이 없는가 — **모호한 참조 1건 발견**.
  - [x] 컴포넌트 명세(§4)와 실제 사용처(§2 화면별 명세)가 서로 모순되지 않는가 — **모순 1건 발견**([C-04] 재설계 후 `ConsentCheckboxGroup`의 "필수/선택 구분" 설명이 낡은 정보로 남아있었음).
  - [x] 동의 철회(REQ-030) 같은 규제 플로우가 실제 API 동작 근거 없이 "당연히 될 것"으로 서술된 곳이 없는가 — **1건 발견**(진행 중 세션 텍스트 전용 강등이 서버측에서 실제 강제되는지 근거 없음).
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — 큐 초과·타임아웃·세션만료·권한거부 등 주요 엣지케이스는 §1.2/§2 화면별 상태표에 반영되어 있음을 확인.
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — LLM 출력 새니타이즈 범위(REQ-036)가 리포트 필드까지 명시적으로 커버하는지 애매했던 점을 §7.3 갭7에 이미 포함(1차 수정으로 해소).
- 발견된 결함 목록:
  1. **(상호참조 오류)** [C-07] 본문이 "확인 필요 항목 2"를 인용했으나 §7.3 표의 실제 항목 2는 "삭제 요청 처리 상태 조회 부재"(C-13 관련)이고, [C-07](코드 재조회 부재)는 항목 3이었다 — 번호 불일치.
  2. **(상호참조 오류)** [C-08] 본문이 "확인 필요 항목 3"을 인용했으나 실제로는 항목 4([C-08] 화이트보드 재조회 부재)였다.
  3. **(상호참조 오류)** [C-10] 본문이 "확인 필요 항목 4"를 인용했으나 실제로는 항목 5(지원자용 비동기 알림 부재)였다.
  4. **(모호한 참조)** [C-04]의 "빈 상태" 행이 "§4.2"만 인용해, 이 문서 자신의 §4(컴포넌트 명세)인지 03-system-design §4.2(API 엔드포인트)인지 구분되지 않았다.
  5. **(컴포넌트-화면 모순)** §4 `ConsentCheckboxGroup` 설명이 "필수/선택 구분 표시"로 남아있었으나, 1차 수정으로 [C-04]는 두 체크박스 모두 필수가 되어 "선택" 항목이 없는 상태와 모순됐다.
  6. **(근거 없는 서술)** §1.1 10번과 §1.2 "동의 철회 중 진행 중 세션 존재" 항목이 "진행 중 철회 시 세션은 즉시 텍스트 전용으로 강등된다"를 마치 서버가 보장하는 동작처럼 서술했으나, 03-system-design §6.2는 동의 검사 시점을 `/start` 1회로만 명시해 세션 도중 재검사·강제 로직의 존재 여부가 확인되지 않는다.
- 조치 내용: (수정 후 → v1, 최종)
  1. [C-07] 인용 번호를 "확인 필요 항목 3(§7.3)"으로 정정.
  2. [C-08] 인용 번호를 "확인 필요 항목 4(§7.3)"으로 정정.
  3. [C-10] 인용 번호를 "§7.3 확인 필요 항목 5"로 정정.
  4. [C-04] 참조를 "03-system-design §4.2 `pre-notice` 엔드포인트"로 명확화.
  5. `ConsentCheckboxGroup` 설명을 "항목별 '필수' 배지 표시(현재 [C-04] 사용례는 전 항목 필수)"로 수정해 실제 사용처와 정합시킴.
  6. §1.1/§1.2/§7.3 갭6에 "서버측 강제 검증 여부는 03단계에 명시되어 있지 않다"는 사실을 명시적으로 추가하고, §7.3 갭6의 "권장 조치"에 (b) 항목으로 "동의 철회 시 진행 중 세션의 음성 처리를 서버가 즉시 차단하는 로직 명시"를 추가. §7.2 매핑 표의 [C-13] 행에 갭6 참조를 추가.

## 3차 검증 (2차에서 결함이 발견되어 규칙 B에 따라 반복 — 결함 0건 확인)
- 검증자(역할): "이 문서만 보고 화면을 구현할 프론트엔드 개발자" 관점 재적용 + 전체 상호참조 전수 검색(`그렙 "확인 필요|갭[0-9]"`)
- 일시: 2026-09-19
- 체크리스트
  - [x] 2차에서 지적된 6건이 실제로 파일에 반영되었는가 — `Read` 및 `Grep`으로 전체 문서의 "확인 필요"/"갭N" 인용을 전수 검색해 §7.3 표 번호와 1:1 대조, 불일치 없음을 확인.
  - [x] 그 외 새로운 상호참조 오류가 없는가 — 전수 검색 결과 8개 갭 번호(1~8) 모두 인용처와 표 정의가 일치.
  - [x] §7.3 결론 문단의 "8개 항목"이라는 서술이 실제 표 행 수(8행)와 일치하는가 — 일치 확인.
  - [x] §0 서두의 갭 개수 요약이 §7.3 최종 개수와 일치하는가 — 최초 "화면 4곳"에서 "8건(GET 부재 5, 정책 재검토 1, 데이터 세분화 1, 메커니즘 미정의 1)"으로 수정 반영 확인.
- 발견된 결함 목록: 없음 (0건)
- 조치 내용: 없음. 2차 수정 반영본을 v1(최종)로 확정.

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료 — 실제로는 3회 수행) — 다음 단계로 handoff 가능
- [ ] PASS (Low 등급, 1차 검증 결함 0건으로 2차 생략) — 해당 없음 (Tier=High)
- [ ] FAIL

## 근거 요약
- Tier=High이므로 규칙 B 완화 조항이 적용되지 않는다. 1차 검증에서 결함 8건(API/데이터 정합성 위반 1건 — [C-04] 동의 게이트, 모호한 인터랙션 1건, 정합성 미확인 1건 — STAR 구조, 메커니즘 누락 1건 — 첫 질문 전달, 용어 오류 1건, 컴포넌트-화면 참조 누락 1건, REQ-034 노출 범위 누락 1건, 근거 과신 1건)이 발견되어 2차 검증으로 이어졌다.
- 2차 검증(구현 개발자 관점)에서 1차 수정사항의 실제 반영은 확인했으나, 그 수정 과정에서 새로 발생한 상호참조 번호 불일치 3건과 기존에 남아있던 모호한 참조·컴포넌트 모순·근거 없는 서술 각 1건씩 총 6건을 추가로 발견해 규칙 B에 따라 3차 검증을 수행했다.
- 3차 검증에서 전수 검색(Grep)으로 모든 "확인 필요"/"갭N" 인용을 §7.3 표와 대조한 결과 결함 0건을 확인해 PASS로 확정했다(규칙 B "2회는 하한선이지 상한선이 아니다" 준수).
- 질문 발생 여부: 규칙 A에 따른 "즉시 중단 후 질문"은 발생하지 않았음 — 대신 03단계 API/데이터 모델만으로 해소되지 않는 8개 항목(§7.3)을 "확인 필요"로 명시하고 각각 그레이스풀 디그레이드 방안과 권장 조치를 함께 제시해, 05단계 착수 전 오케스트레이터가 규칙 A로 재확인해야 할 항목으로 인수인계했다(규칙 A의 정신을 문서 내 명시적 플래그로 충족, 03단계의 "확인 필요" 선례와 동일한 방식). 그중 갭6([C-04] 생체정보 동의 강제와 개인정보보호법 자유동의 원칙의 긴장)은 단순 API 누락이 아닌 정책적 재검토가 필요한 사안으로 별도 강조했다. 되돌리기 어려운 자체판단(화면 구조/토큰 확정, 8개 갭 발견)은 `decisions.md` DEC-022로 근거와 함께 기록해 규칙 G를 충족했다.
- 기획서/설계서 대비 기능 커버리지: 02-planning §4.2 In-Scope Feature A~H 전체가 04-ux-design §1~2의 화면(지원자 14개, 채용담당자 3개, 운영자 1개, 공통 2개, 총 20개)에 최소 1개 이상 매핑되어 있음을 확인(커버리지 100%). Out-of-Scope(DEC-008) 화면·컴포넌트는 설계하지 않았음을 재확인.

---

## v3 보정 검증 (2026-09-23, 03 v4 §4.6 반영 — DEC-055)

- 대상: `04-ux-design.md` [C-11]/[R-02] 공통 "루브릭 항목별 평가" 명세, §8 변경이력 v3
- 적용 Tier: High — 최소 2회

### 1차 (작성자 관점)
- 확인: 03 §4.6 (5) 응답 필드(`rubric.criteria`, `answers`)와 화면 요소가 1:1인지 / REQ-036(JSX 텍스트만) / 빈 상태 / 플랫폼 제약(브라우저 `confirm()` 사용 불가 → 화면 내 확인)
- 결함: 없음(0건). Tier High라 2차 생략 불가.

### 2차 (처음 받은 심사자 관점, 접근성·예외 상황)
- 확인: 칩 펼침의 키보드 조작(`aria-expanded`), 모바일 전체 폭, 202·200·409 응답별 화면 처리
- 결함:
  1. 동료 recruiter 템플릿이 현재 적용된 경우 드롭다운에 없어 현재 상태를 알 수 없음 → "현재: {스냅샷 이름}(다른 담당자 템플릿)" 표시 추가
  2. 원문이 삭제된 근거 답변 처리 미정의 → 칩 비활성 + "원문이 삭제됨" 추가

### 3차 (전체 재독)
- 03 §4.6 3·4차 수정(`answer_map`)과 화면 명세 충돌 없음 확인
- 결함: 없음(0건)

### 4차 (03 5차 수정 연동)
- 결함:
  1. 03 §4.6 (2)에 503 `QUEUE_FULL`이 추가됐으나 [R-02] 응답별 처리에 없음 → 안내 문구 추가

### 5차 (전체 재독)
- 결함: 없음(0건)

### 최종 판정
- [x] PASS (결함 0건, 5회 검증 완료)
