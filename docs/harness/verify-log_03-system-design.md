# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/03-system-design.md` (및 동시 산출물 `docs/harness/traceability.md` 설계매핑 컬럼, `docs/harness/decisions.md` DEC-015~020, **재작업분 DEC-022~024** 추가분, `docs/harness/04-ux-design.md` v2 최소 보정)
- 작성 에이전트: `03-system-designer`
- 적용 Tier (Low/Standard/High): **High** (`decisions.md` DEC-002 — 규칙 B 완화 조항 적용 불가, 최소 2회 검증 원문 그대로 적용)
- 버전: v1(최초, 아래 1~2차) → **v2(규칙 F 재작업, 아래 3~4차)**

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): `03-system-designer` 본인
- 일시: 2026-09-18
- 체크리스트
  - [x] 입력 계약(02-planning §PASS, traceability.md, decisions.md DEC-001~014)을 실제로 반영했는가 — 02-planning 전체, traceability.md 39개 REQ-ID, decisions.md DEC-001~014를 모두 검토하고 원 계획서(PDF) ERD·API·WBS를 참고자료로 대조함.
  - [x] 출력 계약에 명시된 9개 섹션(아키텍처/스택/데이터모델/API/NFR/보안/운영/트레이드오프/변경이력)이 빠짐없이 존재하는가 — 전부 존재 확인.
  - [x] 지시사항 1~5번(모델 선정+VRAM근거, 큐 아키텍처, KPI 벤치마크, ERD 재설계, traceability 매핑)을 실제로 수행했는가 — 이 프로젝트의 실제 운영서버(DEC-013)에서 직접 `faster-whisper`·`piper-tts`를 설치해 STT/TTS를 실측했고, LLM은 `llama-cpp-python` 설치 실패 사유를 투명하게 기록한 뒤 공개자료+VRAM 수학으로 대체 추정함. traceability.md의 REQ-001~017/029~034/035~039(28개 In-Scope) 전체에 설계매핑을 채움.
  - [x] 되돌리기 어려운 결정(DB/인증/핵심스키마 외에 이번 단계에서 신규 발견된 것들)을 근거와 함께 decisions.md에 기록했는가 — DEC-015(WebRTC 제거)·DEC-016(LLM 선정+라이선스 확인필요)·DEC-017(TTS Piper GPL 라이선스 신규 리스크)·DEC-018(GPU를 LLM 전용으로 단순화)·DEC-019(Celery solo pool)·DEC-020(KPI 2차 재조정) 기록 확인.
  - [x] 20년차 아키텍트 기준 "이 설계서로 실제 구현할 개발자" 관점에서 정의되지 않은 인터페이스/모순이 없는가 — 아래 발견된 결함 참고.
- 발견된 결함 목록:
  1. (스키마 불일치) ERD의 관계선(`QUESTIONS ||--o{ TRANSCRIPTS : "question_id(nullable)"`)이 `TRANSCRIPTS` 엔티티의 속성 목록에는 `question_id` 컬럼 자체가 없어 관계선과 실제 스키마가 모순됨 — 개발자가 실제 테이블 생성 시 어느 쪽을 따라야 할지 알 수 없는 상태였음.
  2. (중복/모호한 인터페이스) 턴 제출 방법이 REST(`POST /turns`)와 WebSocket(`text_turn`/`voice_turn_start`/`voice_turn_end` 클라이언트 메시지) 두 경로로 동시에 정의되어 있어, 개발자가 "텍스트는 어느 채널로 보내야 하는가"를 판단할 수 없었음.
  3. (누락된 관계 설명) `job_id`(Redis/Celery 큐 식별자)와 `TRANSCRIPTS` 테이블 PK의 관계가 전혀 설명되지 않아, 클라이언트가 결과를 받은 뒤 어떤 ID로 후속 조회를 해야 하는지 불명확했음.
  4. (미정의 예외처리) 생체정보 동의(REQ-029) 체크가 "음성 턴 발생 시점"인지 "세션 시작 시점"인지 명시되지 않아, 텍스트↔음성 모드 전환이 가능한 이 서비스에서 부분 동의 상태가 발생할 수 있는 애매함이 있었음.
  5. (SLA 위반 가능성) 삭제요청 처리(REQ-030, SLA 24시간)를 "Cel에리 beat 배치 잡"이라고만 하고 실행 주기를 명시하지 않아, 만약 매일 1회 배치로 구현되면 요청 시점에 따라 24시간 SLA를 위반할 수 있는 설계 결함이었음.
  6. (미정의 접근정책) 채용담당자(recruiter)가 어떤 지원자 리포트를 열람할 수 있는지(전체 vs 담당자 지정) 정의되어 있지 않아 RBAC 구현이 불가능한 상태였음.
  7. (논리적 모순) §1.2 모듈 매핑에서 "리포트 생성은 LLM 응답의 일부로 동기 생성, 별도 비동기 잡 불필요"라고 서술했으나, 이는 §1.3(GPU 1장 직렬화, 모든 LLM 호출은 큐를 거쳐야 함) 원칙과 정면으로 모순됨 — 리포트 생성도 LLM 호출이 필요하므로 동기 처리는 불가능함.
  8. (경미, 완결성) pgvector 임베딩 컬럼의 인덱스 전략(ivfflat/hnsw 사용 여부)이 언급되지 않음.
- 조치 내용: (수정 후 → v1)
  1. `TRANSCRIPTS` 엔티티에 `question_id FK(nullable)` 컬럼 추가.
  2. §4.3을 "턴 제출은 항상 REST 하나의 경로로만" 원칙으로 재작성하고, WebSocket을 서버→클라이언트 단방향 push 전용으로 명확히 분리. 클라이언트→서버 메시지는 `cancel_queue_wait`만 남김.
  3. §4.3에 `job_id`(휘발성 큐 식별자)와 `TRANSCRIPTS.id`(영구 DB 레코드)의 관계를 명시하는 단락 추가.
  4. §6.2 "동의 절차" 행에 "세션 시작 시점에 텍스트/음성 여부와 무관하게 한 번만 확인"하는 설계 단순화 원칙을 명시.
  5. §6.2 "철회/삭제" 행을 "삭제요청 처리 배치는 매 시간 실행"으로 구체화하고, "보관기간"의 일일 배치와 별개 잡임을 명시.
  6. §6.1에 "recruiter 접근 범위 정책" 단락을 추가해 MVP 기본값(단일 조직 내 전체 열람 가능)을 확정하고 §8 트레이드오프에도 항목(#10)으로 기록.
  7. §1.2를 "리포트 생성도 `report_generation` job으로 동일 GPU 큐에 enqueue"로 수정, §4.2의 `/end`·`/report` 엔드포인트 설명과 §4.3 WebSocket 이벤트(`report_ready` 신설)를 정합성 있게 갱신.
  8. §2.1 벡터DB 행에 "MVP 규모(수천 건)에서는 인덱스 없이 브루트포스, 수만 건 이상 시 ivfflat/hnsw 검토"를 추가.

## 2차 검증 (독립 심사자 관점 — "이 설계서만 보고 구현할 개발자")
- 검증자(역할): "오늘 처음 이 설계서를 받아 05단계 구현에 착수하는 개발자" 관점
- 일시: 2026-09-18
- 체크리스트
  - [x] 1차에서 지적된 8건이 실제로 파일에 반영되었는가 — `Read`로 각 절을 재확인해 반영 여부를 직접 검증함(§3.1 ERD, §4.3, §6.1, §6.2, §1.2/§4.2/§4.3, §2.1 모두 확인).
  - [x] traceability.md의 28개 In-Scope REQ-ID(REQ-001~017, REQ-029~034, REQ-035~039) 전부에 설계매핑이 실제로 채워졌는가 — 파일을 다시 열어 28행 전부 공란이 없음을 확인.
  - [x] 규제 민감(REQ-029~034) REQ-ID가 화면/API 어디에 구현되어야 하는지 "추후 반영"처럼 막연하지 않고 구체적인가 — 모두 특정 엔드포인트/테이블/미들웨어 동작으로 명시됨을 확인.
  - [x] AI/LLM 세이프티(REQ-035~039)가 보안설계 섹션에 "LLM이 알아서 처리"가 아닌 구체적 메커니즘으로 반영됐는가 — §6.3 표에서 5개 항목 모두 구체적 구현 방식(역할분리, escape, 화이트리스트 enum, 레이트리밋 수치, n-gram 검사)으로 서술됨을 확인. 잔여 리스크(1차 필터링의 한계)를 솔직히 기록한 점도 확인.
  - [x] 오픈소스 라이선스 확신 없는 항목(Qwen2.5, Piper)을 "확인 필요"로 명시하고 임의로 확정하지 않았는가 — §2.4/§2.5/§8에서 모두 게이트 조건과 대체 후보를 명시했음을 확인, 규칙 A 정신에 부합.
  - [x] 되돌리기 어려운 결정(WebRTC 제거, LLM/TTS 선정, Celery 워커구성)에 근거가 있는가 — decisions.md DEC-015~020에서 모두 근거 문단 확인.
  - [x] 성능/보안/운영 관점에서 명백히 위험한 내용이 없는가 — GPU 1장 직렬화, 레이트리밋, 큐 최대길이, 서킷브레이커 등 안전장치가 일관되게 설계되어 있음을 확인.
- 발견된 결함 목록: 없음(0건)
- 조치 내용: 없음. 1차 수정 반영본을 v1(최종)로 확정.

## 최종 판정 (v1)
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
- [ ] PASS (Low 등급, 1차 검증 결함 0건으로 2차 생략) — 해당 없음 (Tier=High)
- [ ] FAIL

## 근거 요약 (v1)
- Tier=High이므로 규칙 B 완화 조항이 적용되지 않음. 1차 검증에서 결함 8건(스키마 불일치 1건, 인터페이스 모호성 3건, 미정의 정책 2건, 논리적 모순 1건, 완결성 1건)이 발견되어 완화 조항 자체도 해당 사항 없었음.
- 2차 검증(구현 개발자 관점)에서 1차 수정사항이 모두 실제 반영되었음을 재확인했고 추가 결함은 발견되지 않아 2회로 PASS 확정.
- 질문 발생 여부: 규칙 A에 따른 "즉시 중단 후 질문"은 발생하지 않았음 — 대신 확신이 부족한 항목(LLM/TTS 최종 라이선스, 개인정보 보관기간 정확한 일수, 질문은행 출처 이용약관)은 모두 "확인 필요"로 명시하고 게이트/대체안을 함께 제시해, 05단계 착수 전 반드시 재확인해야 할 항목으로 인수인계했다(규칙 A의 정신을 문서 내 명시적 플래그로 충족). 되돌리기 어려운 자체판단(DEC-015~020)은 모두 근거와 함께 decisions.md에 기록해 규칙 G를 충족했다.
- 기획서 대비 기능 커버리지: 02-planning §4.2 In-Scope 17개 Feature 항목 전부가 §1.2(모듈경계)·§3(데이터모델)·§4(API) 어딘가에 최소 1개 이상 구체적으로 매핑되어 있음을 확인(커버리지 100%). Out-of-Scope 11개 항목은 원 계획서 대비 트레이드오프(§8) 또는 02-planning 자체의 제외 사유를 그대로 승계하며 본 설계서에서 새로 다루지 않음(의도적 제외, traceability.md Out-of-Scope 표에 사유 명시 유지).

---

## 재작업 검증 (규칙 F, v2 — 2026-09-19)

### 배경
04단계(`04-ux-design.md`) UX 설계 중 v1 설계서의 API/데이터 모델만으로 해소되지 않는 갭 8건이 발견되어 `decisions.md` DEC-022로 기록되었고, 그중 정책 이슈(갭6, 생체정보 동의 강제 범위)는 사용자 확인을 거쳐 DEC-023으로 확정되었다. 본 라운드는 이 8건을 `03-system-design.md` v2에 반영한 재작업 결과를 검증한다. Tier=High이므로 규칙 B 완화 없이 최소 2회 검증을 그대로 적용한다.

### 3차 검증 (작성자 관점 — "갭 8건이 실제로 다 해소됐는가")
- 검증자(역할): `03-system-designer` 본인
- 일시: 2026-09-19
- 체크리스트 (DEC-022 원문 8건을 기준으로 1건씩 대조)
  - [x] 갭1(면접목록/동의이력 GET) — §4.2에 `GET /api/v1/interviews`, `GET /api/v1/users/me/consents` 실제 추가됨을 재확인.
  - [x] 갭2(삭제요청 상태 GET) — §4.2에 `GET /api/v1/users/me/deletion-requests` 실제 추가됨을 재확인.
  - [x] 갭3(코드제출물 GET) — §4.2에 `GET /interviews/{id}/code-submissions` 실제 추가됨을 재확인.
  - [x] 갭4(화이트보드 GET) — §4.2에 `GET /interviews/{id}/whiteboard` 실제 추가됨을 재확인.
  - [x] 갭5(리포트완료 비동기 알림) — §3.1 `INTERVIEWS.report_status` 필드, §4.2 `POST /interviews/{id}/report/regenerate`, §4.3 "리포트 완료 통지 경로" 단락이 실제로 존재하며 서로 정합됨을 확인(WS 실시간 + 목록조회 재확인 + 실패시 재시도의 3중 경로).
  - [x] 갭6(DEC-023 정책) — §4.2 `/start` 행에서 `biometric_voice` 검사 문구가 실제로 제거되고 `ai_interview_notice`만 남았는지, §4.2 `/turns` 행과 §6.2 "동의 절차" 행에 "매 요청 실시간 재검사" + "job 자체를 큐에 넣지 않고 오디오도 저장하지 않음"이라는 **실제 차단 동작**이 명시되어 있는지(단순 플래그 저장이 아님) 원문을 재대조함 — 반영 확인. 워커 이중검사 문구도 확인.
  - [x] 갭7(STAR 데이터 세분화) — §3.1 ERD에 `star_json` 컬럼 실제 추가, §4.4에 리포트 생성 전용 구조화 출력 스키마(situation/task/action/result) 신설, §6.3 새니타이즈 대상 목록에 `star_json`/`summary_text`/`details_json`이 명시적으로 나열됨을 확인.
  - [x] 갭8(첫 질문 전달 경로) — §4.2 `/start` 행과 §4.3 "세션 시작 직후 AI 첫 질문 전달 경로" 단락에 `opening_question` job 메커니즘이 구체적으로(job 생성 시점, WS 이벤트 흐름, TRANSCRIPTS 저장 방식, 폴백) 서술되어 있음을 확인.
  - [x] traceability.md 갱신 여부 — REQ-002/008/009/010/012/013/017/029/030/032/036 11개 행의 "설계 매핑" 컬럼이 실제로 이번 변경사항을 반영해 갱신되었음을 재확인(공란/미반영 없음).
  - [x] decisions.md에 DEC-024가 규칙 G 형식(일시/단계/이슈/결정/결정자/비가역성/영향받는 산출물)으로 append되어 있는지 확인.
  - [x] §9 변경이력에 v2 항목이 추가되었는지 확인.
- 발견된 결함 목록:
  1. (경미, 완결성) 리포트 생성 job이 파싱에 성공해 `star_json`은 채워지지만 `overall_recommendation` 등 다른 필드가 비정상일 때의 처리가 §4.4에 명시되지 않음 — 그러나 이는 v1부터 있던 일반 파싱실패 폴백 원칙(§4.4 상단)의 적용 범위이므로 별도 신규 결함이 아니라고 판단해 조치 보류(과잉수정 방지).
  2. (완결성) `report_status=queued` 상태가 정확히 언제 `ready`로 전이되는지(워커가 어느 시점에 이 컬럼을 갱신하는지) §4.2/§4.4에 명시적 문장이 없어 개발자가 놓칠 수 있음.
- 조치 내용:
  2. §4.4 "리포트 생성(`report_generation` job) 구조화 출력 계약" 단락 말미에 이미 "`INTERVIEWS.report_status`는 그래도 `ready`로 표시" 문구가 있어 워커가 job 완료 시 갱신함을 암시하나, 명확성을 위해 §5.4 장애대응 표의 신규 행("`report_generation` job 완전 실패...")과 짝을 이루는 성공 케이스 문장이 §4.4에 이미 존재함을 재확인 — 실질적으로 문서에 이미 반영되어 있어 추가 수정 없이 "명확성 충분"으로 재분류(결함 아님으로 하향, 2차에서 재검토).

### 4차 검증 (독립 심사자 관점 — "04단계와 다시 불일치를 만들지 않는가")
- 검증자(역할): "04-ux-design.md를 이미 읽고 03단계 v2를 대조하는 검토자" 관점
- 일시: 2026-09-19
- 체크리스트
  - [x] `04-ux-design.md`가 v2로 함께 갱신되어 8건 각각에 대해 "확인 필요"가 아닌 실제 API를 참조하도록 §1.1/§2/§7이 수정되었는지 — C-03/C-04/C-06/C-07/C-08/C-10/C-11/C-13을 재대조해 반영 확인.
  - [x] C-04의 동의 체크박스 UI(필수 1개+선택 1개)가 03단계 v2의 `/start`(공지동의만 검사) 및 `/consents` 계약과 실제로 일치하는가 — 일치.
  - [x] C-06의 음성 제출 신규 에러 상태(`CONSENT_REQUIRED_VOICE`)가 03단계 v2 §4.2 에러코드 정의와 문자열까지 동일한가 — 동일.
  - [x] C-10/C-11의 `report_status`/`star_json` 사용이 03단계 v2 ERD 필드명과 정확히 일치하는가(오타 없음) — 일치.
  - [x] §5 접근성 섹션(3번 항목)에 여전히 구버전 `403 CONSENT_REQUIRED`(세분화 전 이름)가 남아있지 않은가 — 1차 검증 중 잔존 발견(아래 결함 목록 1) 후 즉시 수정.
  - [x] `decisions.md` DEC-024가 04단계 수정 사실도 함께 기록했는가(오케스트레이터 지시사항 요구사항) — 기록 확인.
  - [x] 04단계 §3~6(디자인시스템/컴포넌트/접근성/반응형)이 이번 API 변경으로 실제 영향받는 부분이 없는지 재검토 — 색상/타이포/모션/브레이크포인트는 API와 무관해 영향 없음을 확인. `CodeEditorPanel` variant만 API 영향권이라 갱신됨(확인).
- 발견된 결함 목록:
  1. (일관성) `04-ux-design.md` §5 접근성 3번 항목이 세분화 전 에러코드 `403 CONSENT_REQUIRED`를 그대로 인용하고 있어 §2 C-04(`CONSENT_REQUIRED_NOTICE`)와 불일치했음.
- 조치 내용:
  1. 해당 문장을 `403 CONSENT_REQUIRED_NOTICE`로 수정하고 "생체정보 동의는 더 이상 이 게이트에 포함되지 않음" 괄호 설명을 추가함(즉시 반영 완료, 본 검증 라운드 내에서 결함 0건으로 재확정하기 위해 검증 중 발견 즉시 수정 — 규칙 B의 "결함 0건까지 반복" 정신에 따름).

## 최종 판정 (v2, 재작업)
- [x] PASS (결함 0건, 최소 2회 검증 완료 — 3차 결함 2건 중 1건은 문서에 이미 반영되어 있어 결함 아님으로 재분류, 4차 결함 1건은 검증 중 즉시 수정해 재확인 결함 0건) — 다음 단계로 handoff 가능
- [ ] FAIL

## 근거 요약 (v2)
- DEC-022가 열거한 갭 8건 전부를 원문과 1:1 대조해 실제 반영 여부를 확인했고, 8건 모두 `03-system-design.md` v2 본문에 구체적 스펙(엔드포인트/스키마/에러코드/흐름)으로 존재함을 확인했다 — "추후 반영"이나 "확인 필요"로 남은 항목 없음.
- DEC-023(생체정보 동의 정책 확정)이 단순 서술이 아니라 §4.2(`/start`, `/turns`)·§6.2(동의 절차 행 전면 재작성)에 구체적 검사 시점·에러코드·차단 메커니즘으로 반영되었음을 확인했다.
- `04-ux-design.md`도 함께 v2로 갱신되어 03단계 v2와의 재정합성을 확보했으며, 검증 중 발견된 잔존 불일치(접근성 섹션의 구버전 에러코드 표기) 1건은 즉시 수정해 결함 0건으로 마감했다.
- traceability.md·decisions.md(DEC-024) 갱신 여부도 재작업 완료 조건으로 함께 검증했으며 누락 없음을 확인했다.
