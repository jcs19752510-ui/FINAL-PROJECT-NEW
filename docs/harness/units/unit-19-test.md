# 테스트 결과서 — unit-19 (GET /interviews + [C-03] 지원자 홈) — **v0 작업 중 초안(미완성, 판정 없음)**

> 이 파일은 06단계가 한도 재발에 대비해 남긴 중간 저장본이다. 최종본으로 덮어쓰기 전까지 판정(PASS/FAIL)이 아니다.

## 진행 상태 (2026-09-20 07:5x KST 기준)
- 복원: 03:1x 한도 중단 후 07:21 재개. `.harness-tmp/u19t06_*`(백엔드 8720 pid 7412/9360, 스크립트), `.harness-tmp/u19t06_e2e/`(스펙 초안) 존재. `frontend/e2e/unit-19/` 미저장. DB 자기 계정 0건.
- 완료: 백엔드 API 스펙 21건(임시 `.harness-tmp/u19t06_e2e/api-list.spec.ts`) 20 pass → 1건은 테스트 자체 결함(rubric FK) 수정 후 pass. 규모 프로브 완료.
- 잠정 발견(측정 근거 있음): (1) lazy expiry 커밋 후 나머지 행 N+1 재조회 — 1건 만료 + 1000행 = 1178ms(기준 34ms), 5000행 = 6605ms(기준 135ms); 만료 행당 약 8.5ms(500건 = 4.2s). (2) POST /interviews 120회 연속 전부 201(레이트리밋 없음, REQ-038 unit-8 범위). (3) 무페이지네이션: 20000행 = 4.66MB / 약 657ms.
- 남음: Next 잠금 획득 -> 빌드(API 8720) -> UI 스펙 실행 -> lint/tsc/ruff/build 게이트 -> 결과서 1~10절 -> 검증 로그 2회+ -> Teardown.
