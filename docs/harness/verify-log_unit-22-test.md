# 내부 검증 로그 (Internal Verification Log)

> 파일명 규칙: `verify-log_<산출물파일명>.md` — 이 파일은 `docs/harness/units/unit-22-test.md`(unit-22 06단계 테스트 결과서)에 대한 규칙 B 검증 기록이다.
> Tier=High → 최소 2회 필수(1차 결함 0건이어도 2차 생략 불가). 2차·3차·4차에서 결함이 나와 5차까지 수행했다(결함 0건은 5차에서 확인).

## 대상 산출물
- 파일: `docs/harness/units/unit-22-test.md`
- 작성 에이전트: `06-unit-tester`
- 적용 Tier (Low/Standard/High): **High**(DEC-002), 속도 트랙 L3(DEC-031)
- 버전: v0 (초안, 2026-09-20 약 02:46 KST 작성) → v1(1차 후) → v2(2차 후) → v3(3차 후) → v4(4차 후) → v4 확정(5차 결함 0건)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 06-unit-tester (작성자 본인, "표에 적은 숫자·문장이 내가 실제로 얻은 출력과 1:1로 일치하는가"를 확인)
- 일시: 약 2026-09-20 02:47~02:50 KST (v0 작성 직후, 서버·venv가 살아 있는 상태에서 원본 출력과 대조)
- 체크리스트
  - [x] 입력 계약의 모든 입력을 실제로 반영했는가 — note §2 편차 5건·§5 게이트·§7 인수조건 13개, test-infra.md, 프런트/백엔드 코드, decisions DEC-027/028/029~032, unit-1 note(auth 계약), `models/*`(FK), `auth.py`, 양식 2종, 선례 unit-5-test.md 전부 읽고 반영(FK는 `pg_constraint` 실측과도 대조)
  - [x] 출력 계약의 필수 항목이 빠짐없이 존재하는가 — 템플릿 1~10절 전부, AC1~AC13 ↔ TC 1:1 추적표, 정상/경계/예외/권한 경계 포함(빈 목록·개행·대문자·죽은 포트·DB 불통·비마커 거부·`role=admin` 가입 거부 등)
  - [x] 상위 산출물과 모순 없는가 — note의 "2 passed / 6 passed / All checks passed"와 06 실측이 일치, 편차 5건은 실측으로 타당성 판정
  - [x] 추측으로 채운 항목이 있는가 — 아래 결함 3번(근거 없는 서술)을 발견해 제거. 그 외 "이상 없음"은 전부 명령·건수·출력으로 서술
  - [x] 20년차 관점 구조적 결함 — 테스트 자체가 가짜 통과일 가능성(테스트 설계 오류)을 별도로 점검: Playwright 변조 5종, 백엔드 스모크 변이 3종, ruff 음성 대조, FK 비공허성(TC-034)을 수행
  - [x] 오탈자·형식 오류 — 아래 결함 5번
- 발견된 결함 목록:
  1. 커버리지/실행 근거 요약에 "약 90개 단언"이라고 과대 기재 — 실측 합계는 A 7 + B 9 + C 4 + D 22 + E 4 + H 7 + F 4 + G 2 + I 4 = **63개**
  2. TC-041의 f-string 사용처를 "5개소"로 기재 — 실제 grep 결과는 6개소(메시지 출력 전부)
  3. TC-003에 근거 없는 서술("1208/1234는 타 프로젝트 잔존물") — 확인한 적이 없는 사실
  4. TC-045의 openapi 경로 열거가 불완전(`resume`·`ops/health` 누락, 필터 조건 미기재)
  5. 표 셀 안에서 `frontend/app|components|lib`의 `|`가 열을 깨뜨림(AC13 행)
- 조치 내용: 위 5건 모두 수정 → v1

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
- 검증자(역할): "오늘 처음 이 결과서를 받은 심사자(07/08 담당자·오케스트레이터)". 문서만 보고 재현 가능한지 확인하기 위해 **전체 명령을 다시 실행**했다(프런트: 스모크 3회 exit code 개별 기록·lint·eslint 대상 파일·tsc 전후 해시, 백엔드: pytest 6/6, 독립 시나리오 전량, ruff, 마커 dry-run, 실사용자 md5 비교, 소스 sha256).
- 일시: 2026-09-20 02:53~02:58 KST
- 체크리스트
  - [x] 1차 지적 항목이 실제로 반영되었는가 — 5건 반영 확인(수치 63, f-string 6, 근거 없는 서술 제거, 경로 열거 보강, 파이프 이스케이프)
  - [x] 엣지 케이스/예외 누락 여부 — 개행 종료 이메일, 대문자, `%`/`_` 와일드카드, 빈 목록 `[]`(전체 삭제로 오해석 금지), 중복 이메일, 참조가 **마커 템플릿만**인 경우(recruiter NULL), 마커 지원자↔비마커 채용담당자 방향, 롤백 시점을 첫/마지막 DELETE로 분리, DB 불통, 죽은 포트, 인증 헤더 변조를 포함함을 재확인
  - [x] 문서만 보고 다음 단계가 추가 질문 없이 시작 가능한가 — 재현 절차(명령·env·포트·PID 규칙)를 §3/§4에 명시. 07/08 제약과 후속 조치를 §8에 정리
  - [x] 비가역 결정 근거 — 06은 코드를 수정하지 않았고(sha256 동일), 결함 조치는 5단계 재작업 요청으로 분리. `--all` 실삭제 금지 준수
  - [x] 보안/운영 관점의 명백한 위험 — 하드코딩 자격증명 grep 0, SQL 바인딩, 서버 자동 기동 없음, 실사용자 md5 무변화를 확인
- 발견된 결함 목록:
  1. **신규 실측 결함(제품 측) 누락**: 재실행 중 `--all --dry-run`이 **타 에이전트(unit-19)가 02:53:21 KST에 만든 비-UUID 마커형 계정 3건**(`harness_test_<12hex>@harness-test.example`) 때문에 건수 없이 중단(exit 1). 이는 삭제 없는 dry-run까지 막아 08의 잔여 마커 확인 절차를 병렬 환경에서 불가능하게 만든다 → 결과서에 **TC-055/DEF-003**이 없었다.
  2. TC-020/AC7 서술("항상 users=0")이 02:53 KST 이후 사실과 불일치.
  3. note §2 편차 5건에 대한 **명시적 판정표**가 없었다(타당성 판정이 TC에 흩어져 있었음).
  4. AC13 소유 유닛 매핑에 unit-19 파일(`interviews.py`, `schemas/interview.py`, `app/page.tsx`, `lib/api.ts`)이 누락됐고, TC-021의 최종(모든 쓰기 종료 후) 스냅샷 비교 결과가 반영되지 않았다(users 19→22, interviews 9→16은 타 에이전트 증가분, 사전 존재 행 missing 0·changed 0).
- 조치 내용: DEF-003·TC-055 추가, TC-020/AC7 재서술, 4-1b 편차 판정표 신설, TC-049·TC-021 갱신, 집계(55건 중 52 PASS/3 FAIL)·9절 조건·§8 후속 조치 반영 → v2

## 3차 검증 (2차에서 결함이 나왔으므로 반복)
- 검증자(역할): 독립 심사자(2차와 동일 관점, 수정본 v2만 대상)
- 일시: 2026-09-20 02:58~03:00 KST
- 수행 내용:
  - 스크립트 교차 검증: TC-001~TC-055 연속·중복 없음, 표의 Pass/Fail 집계 **PASS 52 / FAIL 3(TC-053·054·055)** = 본문 집계와 일치, AC1~AC13 행 전부 존재, 4-1 표가 참조하는 TC 41종이 모두 실재, DEF-001~003이 각각 3회 이상 상호 참조, 템플릿 절 1~10 존재, 초안/미완성 표식 문구 0건.
  - Teardown 사후 조회: `.harness-tmp/u22*` 0건, 8422 LISTEN 없음, 06 python 프로세스 잔존 0(CIM), `frontend/test-results`·`playwright-report`·`backend/.pytest_cache` 없음, 소스 sha256 7종 불변, DB에 06이 만든 행 0건(엄격 UUID 마커·`u22t06%`·템플릿).
  - `git status` 전문을 결과서 7절에 그대로 첨부한 내용과 재대조(`99.현재상태/`는 시작 시점부터 존재하던 항목이라 명시).
  - 문서가 사실 이상을 주장하는 문장 재점검(예: "재다운로드 없음", "코드 무수정", "실사용자 무변화")을 각각 근거 TC로 역추적 — 모두 근거 있음.
  - 셀 안 파이프 문자·인코딩(git status의 8진수 이스케이프 경로 표기) 재확인.
- 발견된 결함 목록:
  1. 7절에 붙인 `git status` 전문 중 한글 디렉터리 항목(`"99.현재상태/"`의 8진수 이스케이프 표기)이 스크립트 이스케이프 오류로 **깨진 문자(mojibake)로 기록**됨 — "그대로 첨부" 원칙 위반(서식·정확성 결함).
- 조치 내용: git이 실제로 출력한 그대로(`"99.\355\230\204\354\236\254\354\203\201\355\203\234/"`)로 복구 → v3. **3차에서 결함이 나왔으므로 4차 수행**

## 4차 검증 (3차 결함 수정 후 재검증)
- 검증자(역할): 독립 심사자(수정본 v3 전체 대상)
- 일시: 2026-09-20 03:01~03:03 KST
- 수행 내용: 깨진 문자 정규식 검사 0건 + 8진수 이스케이프 경로 문자열 존재 확인, TC 표 스크립트 재실행(55건, PASS 52/FAIL 3, 연속·중복 없음), 절 1~10·AC1~13·DEF 참조 정합 재확인, 결과서 7절 `git status` 전문을 **verify-log 생성 후의 실제 `git status --short`**(53줄)와 재대조 → 차이는 `?? docs/harness/verify-log_unit-22-test.md` 1건뿐(결과서에 그 사실을 사전 명기, 이후 5차에서 tsbuildinfo 원복 건이 추가로 확인되어 결과서에 반영), Teardown 잔여 조회(`.harness-tmp/u22*` 0건) 재확인, 미해결 질문 목록이 9절/§8과 일치하는지 확인.
- 발견된 결함 목록:
  1. (심사 중 발견, 검증 로그 자체의 결함) 3차 항목을 적은 이 로그에서도 같은 8진수 경로 표기가 같은 이스케이프 오류로 깨져 있었고, 결과서 §8에 **규칙 A 형식의 미해결 질문 목록**이 명시적으로 없었다(§8/§9에 조치가 흩어져 있음).
- 조치 내용: 로그 표기 복구, 결과서 §8에 "미해결 질문(규칙 A 형식)" 소절 추가 → v4. **결함이 나왔으므로 5차 수행**

## 5차 검증 (4차 결함 수정 후 재검증)
- 검증자(역할): 독립 심사자(결과서 v4 + 이 로그)
- 일시: 2026-09-20 03:04~03:06 KST
- 수행 내용: 두 파일에 대해 깨진 문자 정규식 검사, 8진수 경로 문자열 존재 확인, TC 표 스크립트 재실행(55건, 52/3), §8 미해결 질문 소절과 9절 조건·최종 보고 항목의 일치 확인, `git status --short` 재측정(52줄) — 결과서 7절 첨부본(02:57 측정)과의 차이는 ① 06 산출물 `verify-log_unit-22-test.md` 추가, ② 타 에이전트가 `frontend/tsconfig.tsbuildinfo`를 원복해 ` M`이 사라진 것(06 무관) 2건뿐이며 결과서에 이 두 가지를 명기함을 확인.
- 발견된 결함 목록: (없음 — 결함 0건)
- 조치 내용: 없음 → v4 확정

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료 — 총 5회, 5차에서 0건) — 이 **결과서(산출물)**의 검증 판정이다. 다음 단계로 handoff 가능(단, 결과서 9절의 **테스트 판정은 CONDITIONAL PASS**: 인수조건 13개 PASS + Low 제품 결함 3건 Open)
- [ ] PASS (Low 등급, 1차 검증 결함 0건으로 2차 생략) — 해당 없음(High 등급)
- [ ] FAIL — 해당 없음

## 절차 흐름 요약
1차(자가, 결함 5건) → v1 → 2차(심사자·전체 재실행, 결함 4건 — 신규 DEF-003 포함) → v2 → 3차(스크립트·사후 조회·역추적, 결함 1건 — git status 표기 깨짐) → v3 → 4차(결함 1건 — 로그 표기 깨짐·미해결 질문 소절 누락) → v4 → 5차(결함 0건) → 확정
