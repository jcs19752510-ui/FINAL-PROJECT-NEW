# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/02-planning.md` (및 동시 산출물 `docs/harness/traceability.md`, `docs/harness/decisions.md` DEC-008~012 추가분)
- 작성 에이전트: `02-planning-writer`
- 적용 Tier (Low/Standard/High): **High** (`decisions.md` DEC-002 — 규칙 B 완화 조항 적용 불가, 최소 2회 검증 원문 그대로 적용)
- 버전: v1 (검증 중 결함 수정 반영본)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): `02-planning-writer` 본인
- 일시: 2026-09-18
- 체크리스트
  - [x] 입력 계약(01단계 산출물 + 기존 코드/문서 자체 스캔)을 실제로 반영했는가 — `01-trend-analysis.md` 전체, `decisions.md` DEC-001~007, PDF 원 계획서(`pdftotext`로 텍스트 추출), mermaid 흐름도, `추가개선사항정리.md`, `git log`(그린필드 확인)를 모두 검토함. 추가로 01단계 Critical 공백(로컬서버 하드웨어 미확인)을 메우기 위해 `nvidia-smi`/PowerShell `Get-CimInstance`로 실제 하드웨어를 직접 실측함.
  - [x] 출력 계약에 명시된 10개 섹션이 빠짐없이 존재하는가 — 1~10번 섹션 전부 존재 확인, 추가로 §0(문서 성격/조사방법 설명) 포함.
  - [x] `traceability.md`(REQ-ID 부여, Out-of-Scope 사유 포함), 규칙 I/J 대응 REQ-ID 별도 등록 여부 확인 — REQ-029~034(규제), REQ-035~039(AI세이프티) 모두 정식 등록 확인.
  - [x] 질문을 했고 답을 받았는가 — 이번 단계에서는 "치명적 공백"이 없어(하드웨어는 자체 실측으로 해결) 사용자에게 새로 질문하지 않음. 대신 비가역성 있는 자체판단(MVP 범위 축소, 성공지표 재정의 등)은 규칙 G에 따라 `decisions.md`에 DEC-008~012로 기록함.
  - [x] 트렌드 분석과 무관하거나 근거 없는 기능을 임의로 추가하지 않았는가 — 모든 In-Scope 항목이 원 계획서 REQ-F/N 또는 01단계 시사점에 근거를 두고 있음을 재확인.
  - [x] 20년차 실무자 기준 구조적/논리적 결함 없는가 — 아래 발견된 결함 참고.
- 발견된 결함 목록:
  1. (참조 모호) `traceability.md` REQ-034 행에서 작업 단위를 "unit-15 (또는 11단계 문서)"로 병기해, 어느 쪽이 실제 구현 위치인지 모호함 — 5단계 개발자가 두 곳 중 어디에 구현해야 하는지 판단할 수 없는 상태였음.
  2. (누락) `02-planning.md` §9 작업단위 표에서 unit-15 설명에 REQ-031·032·033만 나열되고 REQ-034(법률자문 권고 문구)가 누락되어, §9와 `traceability.md`(REQ-034→unit-15) 간 정합성이 깨져 있었음.
  3. (근거 부족) §5 KPI 표 "동시 활성 음성 처리 세션 최소 5건"이라는 수치에 산출 근거가 없어, 아키텍트가 이 숫자를 확정치로 오인할 위험이 있었음.
- 조치 내용: (수정 후 → v1)
  1. `traceability.md` REQ-034를 "1차 구현은 unit-15, 11단계 문서에도 동일 문구 재수록(이중 노출)"으로 명확화.
  2. `02-planning.md` §9 unit-15 설명에 "법률자문 권고 문구"와 REQ-034를 추가해 `traceability.md`와 정합성 확보.
  3. §5 KPI 표에 "(4GB VRAM·양자화 모델 기준 러프 추정치, 3단계 실측으로 확정)"라는 근거/한정 문구 추가.
  수정 후 `grep -P '[\x{FFFD}]'`로 두 파일 전체를 재검색해 인코딩 깨짐 문자가 없음을 확인함(00단계 사례 재발 방지).

## 2차 검증 (독립 심사자 관점 — "이 기획서만 보고 설계서를 쓸 아키텍트")
- 검증자(역할): "오늘 처음 이 기획서를 받아 3단계 설계서를 써야 하는 아키텍트" 관점
- 일시: 2026-09-18
- 체크리스트
  - [x] 1차에서 지적된 3건이 실제로 반영되었는가 — `Read` 없이 직접 편집 결과를 추적해 반영 확인(동일 세션 내 Edit 결과 신뢰).
  - [x] 모호한 표현/측정 불가능한 목표가 없는가 — §5 모든 KPI가 수치+측정방법을 갖추고 있고, 잠정치는 "잠정/추정" 표기와 검증 시점(3단계 벤치마크)을 명시해 "확정된 목표"와 혼동될 여지를 없앰. 단, 아래 결함 1건 추가 발견.
  - [x] 아키텍트가 이 문서만 보고 추가 질문 없이 3단계 착수 가능한가 — REQ-ID/작업단위/성공지표/제약/가정이 모두 상호 참조 가능한 수준으로 명시되어 있음을 확인. 단, 아래 결함 2·3건 추가 발견.
  - [x] 비가역적 결정에 대한 근거가 명시되어 있는가 — DEC-008~012 모두 근거 문단을 포함하고 있어 규칙 G "근거 없는 자체판단 기록 무효" 조건을 충족함을 확인.
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — 오히려 규제(REQ-029~034)·AI세이프티(REQ-035~039)·신규 보안위험(코드 실행 배제)을 명시적으로 다룸. 실제 키/개인정보 등 민감정보 노출 없음 확인.
- 발견된 결함 목록:
  1. (완결성) §6.1에서 "4bit/8bit 양자화된 소형 모델(3B~7B급)"이라고만 하고 구체적 후보(예: Llama계열/Qwen계열/EXAONE 등 실제 모델명)가 전혀 없어, 3단계 설계자가 "무엇을 기준으로 벤치마크를 시작해야 하는지" 출발점이 모호할 수 있음.
  2. (일관성) §4.2 In-Scope 표와 §9 작업단위 표의 순서/그룹핑(Feature A~H)이 일치하지만, §4.2 표 자체에는 Feature 명이 표기되어 있지 않아 두 표를 나란히 봐야만 매핑을 알 수 있음 — 표 하나만 보는 독자에게는 약간의 탐색 비용이 발생함.
  3. (경미) §7 리스크 표의 리스크 #2("50명 동시접속 시 GPU 병목")와 §5 KPI 동시성 지표가 사실상 같은 이슈를 다루는데 상호 참조(예: "§5 참고")가 없어 두 섹션을 따로 읽으면 같은 이슈로 인지하기 어려울 수 있음.
- 조치 내용: (수정 후 → v2, 최종)
  1. §6.1에 "3단계 벤치마크 출발점 예시(확정 아님)"로 국내에서 흔히 쓰이는 경량 오픈소스 한국어 지원 모델 계열(예: Llama 3.1 8B급, Qwen2.5 7B급의 4bit GGUF 양자화판)을 참고 후보로 병기하고, "최종 선정은 3단계 실측 벤치마크 결과에 따른다"는 단서를 유지.
  2. §4.2 표에 "Feature" 컬럼을 추가해 §9와 동일한 Feature 라벨(A~H)을 표기.
  3. §7 리스크 #1·#2 항목에 "(§5 KPI '응답 지연'·'동시성' 지표와 연동)" 참조를 추가.
  수정 완료 후 파일 전체를 다시 검토해 반영 여부 확인함(아래 3차 검증 참고).

## 3차 검증 (2차에서 발견된 결함의 수정 확인)
- 검증자(역할): `02-planning-writer` (수정 반영 확인)
- 일시: 2026-09-18
- 체크리스트
  - [x] 2차에서 지적된 3건이 모두 실제로 파일에 반영되었는가
  - [x] 수정 과정에서 새로운 오탈자/모순/인코딩 깨짐이 생기지 않았는가 (`grep -P '[\x{FFFD}]'` 재확인)
  - [x] `traceability.md`의 REQ-ID 개수(기능 17 + 규제 6 + AI세이프티 5 + Out-of-Scope 11 = 총 39개)가 `02-planning.md` 본문 서술과 일치하는가
- 발견된 결함 목록: 없음 (0건)
- 조치 내용: 없음. 수정 완료본을 v2(최종)로 확정.

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
- [ ] PASS (Low 등급, 1차 검증 결함 0건으로 2차 생략) — 해당 없음 (Tier=High)
- [ ] FAIL

## 근거 요약
- Tier=High이므로 규칙 B 완화 조항이 적용되지 않음. 1차 검증에서 결함 3건이 발견되어 완화 조항 자체도 해당 사항 없었음.
- 2차 검증(아키텍트 관점)에서 추가 결함 3건이 발견되어 3차 검증까지 수행했고, 3차에서 결함 0건을 확인해 PASS 확정.
- 질문 발생 여부: 없음 — 01단계 Critical 공백(하드웨어 미확인)은 자체 실측으로 해결했고, 나머지 판단은 오케스트레이터 지시사항에 따라 02단계 권한으로 위임된 범위(MVP 우선순위 재산정)였으므로 규칙 A의 "정말 결정 불가" 요건에 해당하지 않음. 대신 근거를 갖춘 자체판단은 모두 `decisions.md`(DEC-008~012)에 기록해 규칙 G를 충족함.

---

# v3 계획 변경 검증 (규칙 F, DEC-029~032) — 위 v1/v2 기록에 이어 추가

## 대상 산출물
- 파일: `docs/harness/02-planning.md` v3 (작업 단위 unit-19~22 신규 등록, Feature I 신설, L3 속도트랙 예외, 병렬화 판정 갱신)
- 작성 에이전트: `02-planning-writer` (규칙 F 갱신 호출)
- 적용 Tier: **High** (DEC-002 불변 — 규칙 B 완화 없음, 최소 2회 + 결함 0건까지 반복)
- 읽기 전용으로 참조한 파일: `docs/harness/traceability.md`, `docs/harness/decisions.md` (수정 금지 — DEC-032. 이 세션에서 두 파일에 대한 쓰기 호출은 없음), `03-system-design.md`, `04-ux-design.md`
- 검증 기록 시각(KST, 24시간제): 2026-09-20 01:43:26 (아래 3개 라운드를 이 시각에 일괄 기록. 라운드 간 순서는 실제 수행 순서와 같음)

## 1차 검증 (작성자 관점 자가 재검토 — "지시한 변경 범위를 빠짐없이, 그 밖은 건드리지 않고 했는가")
- 검증자(역할): `02-planning-writer` 본인
- 체크리스트
  - [x] (a) 개수/범위 단정 문장 전수 grep — 갱신 전 원문에서 `\d+ ?개|총 ?\d+|unit-\d+ ?[~\-–] ?\d+|A~[A-Z]|1~18|18개|feature|모든 유닛|L1` 패턴으로 스캔. 갱신 전 02-planning.md에는 "작업 단위 18개"라는 리터럴 문장은 없었고, 개수/범위/일괄 단정에 해당하는 지점은 §0 "Speed Track L1(최고속) 제약", §6.2 L1 부채 서술, §6.4 "각 업무단위는 기본 L1", §8 A10, §9 헤더 "모든 유닛은 기본 Speed Track L1", §9 "병렬화 참고"(unit-16~18, unit-4~8), §4.2/§9의 Feature 라벨 A~H였다.
  - [x] (b) 신규 유닛 REQ 매핑 ↔ `traceability.md` REQ 정의 대조 — 아래 표 참고.
  - [x] (c) 유닛 번호 중복/누락 — §9 표 22행이 unit-1~22를 정확히 1회씩 포함(스크립트 재확인: 표 행 22, 중복 0, 번호열 1~22).
  - [x] (d) Feature 표기 일관성 — §4.2와 §9에서 "B. 면접 세션 관리", "G. 규제 컴플라이언스", "I. 면접장 통합 UI" 문자열이 동일. Feature별 유닛 수: A1+B3(2,3,19)+C5+D1+E2+F2+G3(14,15,21)+H3+I1 = 21, 공통 인프라(unit-22) 1개 → 총 22.
  - [x] 설계 앵커 실존 확인 — 아래 "앵커 확인" 절.
  - [x] 변경 범위 격리 — `git diff --stat`으로 02-planning.md만 변경(+39/-9), 변경 라인 전수 목록이 의도한 절(헤더·§0·§4.2·§6.2·§6.4·§8·§9·§10)과 일치. `decisions.md`/`traceability.md`/`units/*`/`backend/**`/`frontend/**`는 이 세션에서 수정하지 않음. 문자 깨짐(U+FFFD) 0건.
- 발견된 결함 목록(갱신 전 상태 기준, 작성 중 조치):
  1. §9 헤더 "모든 유닛은 기본 Speed Track L1로 시작한다" — DEC-031(unit-8/10/11, unit-19~22는 L3)과 모순.
  2. §0 "Speed Track L1(최고속) 제약" 무조건 서술 — 동일 모순 소지.
  3. §6.2의 L1 부채 서술에 L3 예외 미반영.
  4. §6.4에 DEC-031 미반영(지시 4번).
  5. §8 A10 "각 feature는 기본 L1" — 예외 미반영.
  6. §9 "병렬화 참고"가 DEC-032의 판정(불가 조합 unit-8↔7, 10↔8, 10→11, 19↔8/10)을 담지 못함.
  7. 문서 어디에도 작업 단위 총수·Feature 수가 명시되지 않아 22개/A~I 9개를 독립적으로 대조할 수 없음.
  8. (작업 환경) 파일 편집 도구(Edit)가 비활성이라 전체 재작성(Write)을 해야 했고, 의도치 않은 변경 위험이 있음.
- 조치 내용: 1~5는 §0·§4.2 v3 주석·§6.2·§6.4·§8 A10·§9 헤더에 L3 예외를 명시해 해소. 6은 §9에 DEC-032 판정표(병렬 가능/불가/미판정)와 공통 규칙을 추가하고 v1/v2 원문 문장은 "초기 판단, v3에서도 유지"로 이력 보존. 7은 §0(v3 요약: 18→22개, A~H 8개→A~I 9개)과 §9 헤더(총 22개 = feature 소속 21 + 공통 인프라 1)에 명시. 8은 위 git diff 라인 단위 대조로 확인(원문 무단 변경 없음).

### 신규 유닛 REQ 매핑 ↔ traceability.md 대조 (검증 (b))
| 신규 유닛 | 02 §9 매핑 | traceability.md 근거(읽기 전용 확인) | 모순 여부 |
|---|---|---|---|
| unit-19 | REQ-002 | REQ-002 설계 매핑에 03 §4.2 `GET /interviews`(DEC-024 갭1) 포함, 비고에 "GET /interviews 미구현 — [C-03]이 의존, 후속 작업 단위 필요" 명시 | 없음 |
| unit-20 | REQ-008, 015, 017 | REQ-008 비고 "CodeEditorPanel 배선은 추후 통합 유닛 필요", REQ-015 비고 "[C-05]/[C-06] 미배선(통합 담당 유닛 몫)+DEF-001", REQ-017 구현 상태 "메인 화면 미삽입" | 없음 |
| unit-21 | REQ-030, 033 | REQ-030 비고 "하드 삭제 배치는 REQ-033/unit-15 책임", REQ-033 구현 상태 "자동 파기 Celery beat 미구현(pending→completed 전이 없음)" | 없음(단 두 REQ가 배치 책임을 unit-15로 적고 있어 '작업 단위' 컬럼 갱신 필요 → 최종 보고의 변경 제안) |
| unit-22 | REQ-ID 없음 | DEC-030/DEC-029에 REQ 없음으로 명시 | 없음(feature/REQ 아님 — A12에 가정으로 기재) |

### 앵커 확인 (지시 6번, 파일을 직접 열람)
| 앵커 | 결과 | 위치 |
|---|---|---|
| 03 `GET /interviews` | 존재 | 03-system-design §4.2 REST 엔드포인트 표, `/api/v1/interviews` GET 행(REQ-002, REQ-009; 신규 DEC-024 갭1) |
| 03 자동 파기 배치 | **내용은 존재, 인용 절은 불일치** | 실제 위치는 §6.2 표의 "철회/삭제" 행(`delete_requested_data`, Celery beat 매시간)과 "보관기간" 행(`purge_expired_data`, 매일 1회); §3.1 ERD `DELETION_REQUESTS`, §4.2 `DELETE /users/me/biometric-data`·`GET /users/me/deletion-requests`도 존재. 지시문·DEC-029·traceability REQ-033 설계매핑이 인용한 **§7.4는 "롤백 전략" 절로 파기 배치 서술이 없음**(§7.3은 Celery beat를 알림 스크립트용으로만 언급) |
| 04 [C-03] | 존재 | 04-ux-design §2, "### [C-03] 지원자 홈(대시보드)" (118행) |
| 04 [C-05] | 존재 | 04-ux-design §2, "### [C-05] 면접 준비 화면(장치 점검)" (143행) |
| 04 [C-06] | 존재 | 04-ux-design §2, "### [C-06] 면접장(메인) — 핵심 화면" (149행); 관련 [C-07] 168행, [C-08] 174행 |

## 2차 검증 (독립 심사자 관점 — "오늘 처음 이 기획서를 받아 설계·개발을 이어받는 사람")
- 검증자(역할): 갱신본만 보고 unit-19~22를 착수해야 하는 3~5단계 담당자
- 체크리스트
  - [x] 1차 조치 8건이 파일에 실제로 반영됐는가 — 갱신본 헤더·§0·§4.2·§6.2·§6.4·§8·§9·§10을 다시 열람해 확인.
  - [x] 신규 유닛 각각이 "무엇을/어느 REQ에/어느 Feature에/어느 트랙(L3)으로" 한눈에 읽히는가 — §9 표+헤더로 확인, unit-22의 REQ 없음·07 비대상 명시 확인.
  - [x] 서로 모순되는 문장이 없는가 — L1 기본값과 L3 예외가 §0·§6.2·§6.4·§8 A10·§9 헤더에서 같은 방식으로 서술됨. 병렬화 v1/v2 문장과 v3 판정표가 충돌하지 않음(v1/v2는 unit-9↔16~18 병렬 후보·unit-4~8 순차 권장이며 DEC-032와 방향 일치).
  - [x] 판단 불가 항목을 임의로 채우지 않았는가 — DEC-032가 판정하지 않은 조합은 "미판정(순차 원칙)"으로 남김.
- 발견된 결함 목록:
  1. (자기완결성) §8 A13이 "규칙 A 질문 Q1로 오케스트레이터에 보고했다"고만 적어, 이 문서만 읽는 사람은 Q1이 무엇이고 그때까지 unit-21을 어떻게 착수해야 하는지 알 수 없음.
  - (02 밖 관찰, 결함으로 집계하지 않고 최종 보고로 이관) ① `04-ux-design.md` 384행이 "02-planning §4.2 In-Scope Feature A~H 전체가 §1~2에서 최소 1개 화면 이상에 매핑"이라고 단정 — v3에서 Feature I가 추가되어 문언이 낡았으나 [C-05]/[C-06]은 이미 존재해 사실 관계는 성립. 04는 이 세션의 편집 금지 범위. ② traceability REQ-033 설계 매핑의 "§7.4" 오인용(위 앵커 확인). ③ `units/unit-5-test.md` 105행 "unit-1~18"은 과거 시점 기록이므로 수정 대상 아님.
- 조치 내용: A13을 자기완결적으로 수정 — 선택지 (a) unit-21에 포함 / (b) 별도 신규 유닛 / (c) REQ-033 해당 부분 후속 이월을 문서에 직접 기재하고, 결정 전에는 unit-21이 `delete_requested_data`만 범위로 착수, 결정 결과는 `decisions.md` 기록 후 규칙 F로 본 문서 갱신한다고 명시. "질문 Q1" 참조 제거(재grep으로 0건 확인).

## 3차 검증 (2차 조치 확인 — 결함 0건까지 반복)
- 검증자(역할): `02-planning-writer` (수정 반영 확인, 새 결함 유입 여부 재점검)
- 체크리스트
  - [x] 2차 결함 1건 반영 확인 — A13 문언 재열람, "질문 Q1" grep 0건.
  - [x] (a) 재grep — 갱신 후 02-planning.md에서 개수/범위 단정 문장 전수 재확인: "18개 → 22개(unit-1~22)", "A~H 8개 → A~I 9개", "feature 소속 21 + 공통 인프라 1", "unit-19~22" 표기가 §0/§6.2/§6.4/§8/§9에서 서로 일치. "unit-1~18"은 §0(변경 전 값 설명)과 §9 헤더("최초 등록분")에서만 등장하며 맥락상 이력 서술임을 확인. 잔존하는 "L1" 단정(§4.1, §4.3, §7 리스크 8, A6)은 v1/v2 당시 범위 산정 근거·리스크 서술로 L3 예외와 논리적으로 충돌하지 않음(L1 유닛이 여전히 존재).
  - [x] (b) REQ 매핑 표 재확인 — 변경 없음.
  - [x] (c) 유닛 번호 — 표 행 22, 중복 0, 1~22 연속 재확인.
  - [x] (d) Feature 표기 — 재확인, 변경 없음.
  - [x] 변경 범위 — `git diff --stat` 39+/9-(2차 조치 전후 동일 범위, A13 한 행 내 수정), U+FFFD 0건, 다른 파일 미수정.
- 발견된 결함 목록: 없음 (0건)
- 조치 내용: 없음.

## 최종 판정
- [x] PASS (결함 0건, 3회 검증 완료: 1차 결함 8건 조치 → 2차 결함 1건 조치 → 3차 0건) — 다음 단계로 handoff 가능
- [ ] PASS (Low 등급, 1차 검증 결함 0건으로 2차 생략) — 해당 없음 (Tier=High)
- [ ] FAIL

## 근거 요약 / 미해결 사항
- Tier=High이므로 규칙 B 원문을 그대로 적용, 2차에서 결함이 나와 3차까지 수행.
- **미해결(사용자 판단 필요, 문서 §8 A13에도 기재)**: `purge_expired_data`(03 §6.2 보관기간 만료 일괄 파기)를 담당할 유닛이 없음 — DEC-029는 `delete_requested_data`만 명시.
- **오케스트레이터 후속 조치 필요(02 밖)**: (1) traceability.md의 REQ-002/008/015/017/030/033 '작업 단위' 컬럼 갱신(최종 보고의 제안 문자열), (2) traceability REQ-033 설계 매핑의 "§7.4" → "§6.2" 정정 여부 판단, (3) 04-ux-design 384행의 "Feature A~H" 문언 갱신 여부 판단(규칙 F 영향 범위 판단은 오케스트레이터/사용자 몫).
- Teardown(규칙 K): 이 세션이 만든 임시 파일은 문서 치환용 1회성 스크립트 1개(시스템 임시 디렉터리)뿐이며 실행 직후 삭제함. `.harness-tmp/`에는 아무것도 만들지 않았고, 그 안에 있는 기존 파일은 다른 병렬 작업의 것으로 판단해 건드리지 않음.
