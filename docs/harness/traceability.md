# 요구사항 추적 매트릭스 (Requirements Traceability Matrix)

> 2단계(`02-planning.md`)에서 생성. 이후 3단계(설계 매핑), 5·6단계(구현/단위테스트), 7·8단계(통합/전체테스트)를 거치며 갱신한다. 8단계 완료 조건은 모든 행의 "구현"~"전체테스트" 컬럼이 채워지는 것이다(제외 항목은 사유 명시로 커버리지 인정).

## 기능 요구사항 (In-Scope)

| REQ-ID | 요구사항/기능 (기획서 §, 우선순위) | 설계 매핑 (설계서 §) | 작업 단위 (unit-n) | 구현 상태 | 단위테스트 (unit-n-test) | 통합테스트 (feature-x) | 전체테스트(08) | 비고 |
|---|---|---|---|---|---|---|---|---|
| REQ-001 | [Must] 회원가입/로그인/역할 구분(지원자/채용담당자) (02-planning §4.2) | 03-system-design §1.2(Auth/User API), §3(USERS 테이블), §4.2(auth 엔드포인트), §6.1(JWT/RBAC) | unit-1 | 구현 완료(05단계, `backend/app/api/v1/auth.py` + `frontend/app/{register,login}/page.tsx`) | PASS(06단계, L1 경량판 — 정상경로 2케이스: 회원가입/로그인+본인조회 06단계가 직접 재현, `docs/harness/units/unit-1-test.md`) | | | Feature A. **Speed Track L1 확정**(DEC-003) — 06 경량판 완료. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(에러경로/이메일중복/role422/비밀번호422/로그인401/프론트 화면흐름은 06이 아직 독립 재현하지 않음, 5단계 자체기록만 존재). 상세는 `docs/harness/units/unit-1-note.md`, `unit-1-test.md` 참고 |
| REQ-002 | [Must] 면접 세션 생성·시작·종료·상태관리 (02-planning §4.2) | 03-system-design §1.2(Session API), §3(INTERVIEWS 상태머신 + `report_status` 필드, v2 재작업), §4.2(`/interviews` POST/**GET(신규, 내 목록 조회, DEC-024 갭1)**, `/start`(v2: `ai_interview_notice`만 검사 + `opening_question` job 자동 enqueue, DEC-023/갭8), `/end`, `/report/regenerate`(신규, 갭5)) | unit-2 | 구현 완료(05단계, `backend/app/api/v1/interviews.py` — 생성/시작/종료 3개 엔드포인트만. `backend/app/models/interview.py`, `backend/app/models/consent.py`(최소 스키마), `backend/app/services/job_queue.py`(큐/워커 경계 인터페이스, 정당한 순서상 스텁), Alembic `fdd74cee7615`) | PASS(06단계, L1 경량판 — 정상경로 2케이스: TC-001 세션생성+recruiter역할게이트(403), TC-002 동의게이트(403)→동의부여→start(202,live)→end(202,completed,report_status=queued), 06단계가 직접 새 서버 프로세스(포트 8021)로 재현, `docs/harness/units/unit-2-test.md`) | | | Feature B. **Speed Track L1 확정**(DEC-003) — 06 경량판 완료. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(중복시작/종료 409, 수평권한상승 403, 인증없음 401, 존재하지않는 id 404는 06이 아직 독립 재현하지 않음, 5단계 자체기록만 존재). **범위 편차(unit-2-note.md §2 참고)**: (1) `GET /interviews`(목록 조회)는 이번 지시 범위에서 제외되어 미구현 — [C-03] 지원자 홈이 의존하므로 후속 작업 단위 필요. (2) `POST /interviews/{id}/report/regenerate`, `/resume`은 각각 unit-10/unit-3 범위, 미구현. (3) `INTERVIEWS.rubric_template_id`는 FK 제약 없이 nullable 컬럼만 생성(unit-13이 `RUBRIC_TEMPLATES` 생성 시 FK 추가 필요). (4) `CONSENTS`는 `/start`의 DEC-023 게이트에 필요한 최소 스키마만 선행 생성, 동의관리 REST API 전체는 unit-14/15 책임. (5) `opening_question`/`report_generation` job enqueue는 실제 Celery/Redis 큐가 아직 없어 `job_id` 발급 스텁(unit-4/7/10이 내부 구현 예정) — API 응답 계약(202 {job_id})과 상태 전이는 실제로 구현·검증됨. 상세는 `docs/harness/units/unit-2-note.md`, `unit-2-test.md` 참고 |
| REQ-003 | [Must] 텍스트 기반 질문-응답(채팅형 UI) (02-planning §4.2) | 03-system-design §1.1(WebSocket Gateway), §4.2(`POST /interviews/{id}/turns`, 텍스트 JSON — v2/v3 재작업으로 턴 제출은 WS `text_turn`이 아니라 REST 단일 경로로 통일됨, §4.3 채널 역할 분리 원문 참고), §4.3(WS는 서버→클라이언트 `queue_status`/`stage_update`/`turn_result` push 전용) | unit-4 | 구현 완료(05단계, `backend/app/api/v1/interviews.py`의 `POST /interviews/{id}/turns`(텍스트 전용)+`GET /interviews/{id}/transcripts`(신규, additive), `backend/app/models/transcript.py`, `backend/app/api/v1/ws.py`(WS 게이트웨이, 인증+연결관리+`cancel_queue_wait` 수신), `backend/app/schemas/transcript.py`, Alembic `0df1434883f2`, `frontend/app/interviews/[id]/page.tsx`([C-06] 텍스트 채팅 UI), `frontend/lib/api.ts` 확장) | PASS(06단계, L1 경량판 — 정상경로 2케이스: TC-001 텍스트 턴 2회 제출+`turn_index` 0/1 저장·조회(한글 왕복 바이트단위 확인), TC-002 WS 연결 실제 재현(인증성공+화이트리스트/정크메시지 내구성+인증실패 거부), 06단계가 직접 새 서버 프로세스(포트 8027)로 재현, `docs/harness/units/unit-4-test.md`) | | | Feature C. **Speed Track L1 확정**(DEC-003) — 06 경량판 완료. **범위 편차(unit-4-note.md 참고)**: (1) `GET /interviews/{id}/transcripts`는 03-design §4.2 표에 없었으나 [C-06] 초기 진입 상태가 구조적으로 요구해 unit-3 선례와 동일 근거로 additive 신설. (2) AI 응답 생성(LLM 꼬리질문)은 unit-7 미착수로 `job_queue.enqueue_turn_job`이 job_id만 발급하는 스텁 — WS `stage_update`/`turn_result`가 실제로 push되는 일은 이 유닛에서 발생하지 않음(API 계약·WS 메시지 스키마 자체는 설계서대로 완성, 프런트는 타임아웃 후 안내 문구로 그레이스풀 디그레이드). (3) 음성(multipart) 제출, `biometric_voice` 동의 실시간 재검사(DEC-023)는 unit-5 범위, 이번 유닛은 텍스트 전용. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(scheduled/completed 409, 422, 403/401/404, 410 만료는 06이 아직 독립 재현하지 않음, 5단계 자체기록만 존재. **인수조건 8(브라우저 실사용 UI 흐름 — 낙관적 UI/12초 타임아웃 안내)은 이 환경에 브라우저 자동화 도구가 없어 06단계도 재현하지 못함 — 07/06 정식화 시 반드시 실제 브라우저(또는 headless 자동화)로 검증 필요**. 큐초과 429/AI_SERVICE_TIMEOUT 등은 실제 워커가 없어 재현 불가). 06단계 세션 중 `.harness-tmp/` 공유 디렉토리가 병렬 진행 중이던 다른 세션에 의한 것으로 추정되는 정리 작업으로 통째로 소실되는 환경 이슈를 관찰함(unit-4 자체 판정에는 영향 없음, `unit-4-test.md` §7 참고) — 이후 유닛이 공용 venv(`venv_05_unit1`) 재생성 필요 여부 확인 요망. 상세는 `docs/harness/units/unit-4-note.md`, `unit-4-test.md` 참고 |
| REQ-004 | [Must] 음성 기반 질문-응답(턴제, 실시간 스트리밍 아님) (02-planning §4.2) | 03-system-design §1.3(큐/워커 순차처리), §2.2(WebRTC 제거·HTTP업로드로 대체, DEC-015), §4.2(`/turns`), §4.3(턴 제출은 REST 단일 경로, 텍스트 JSON/음성 multipart) | unit-5 | 구현 완료(05단계, `backend/app/api/v1/interviews.py`의 `POST /interviews/{id}/turns`를 `Content-Type`으로 분기해 음성(multipart) 제출까지 지원(`_submit_voice_turn`/`_submit_text_turn`/`_ensure_turn_submittable` 공통화), `frontend/app/interviews/[id]/page.tsx`에 `AudioRecorderControl`(idle/recording/uploading/error) 마이크 녹음 UI 추가, `frontend/lib/api.ts`의 `submitVoiceTurn`) | PASS(06단계, L1 경량판 — TC-001~TC-009: 동의없음403+미저장, 동의후202+turn_index0로 저장, 텍스트턴 회귀(turn_index1, unit-4 정상), 예외입력(audio누락/빈파일422, 디코딩불가504)+미저장, scheduled/completed409, 06단계가 신규 격리venv(`.harness-tmp/venv_06_unit5`, DEC-027)+새 서버 프로세스(포트8156)+신규 계정으로 2회 독립 재현(28/28 PASS), `docs/harness/units/unit-5-test.md`) | | | Feature C. REQ-F-002 축소판. **Speed Track L1(DEC-003)** — 06 경량판 완료. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(인수조건7 브라우저 마이크 UI 실제 클릭 상호작용은 자동화 도구 부재로 06도 미검증(코드 리뷰만), 401/404/타인소유403(수평권한상승), 동시 다중 요청 시 지연/큐잉(unit-7 실제 워커 필요)은 06단계가 아직 독립 재현하지 않음). 상세는 `docs/harness/units/unit-5-note.md`, `unit-5-test.md` 참고 |
| REQ-005 | [Must] 로컬 STT 파이프라인(Whisper 경량 모델, 턴제 배치) (02-planning §6.1) | 03-system-design §2.3(faster-whisper small/CPU/int8 선정, 실측 RTF 0.22), §5.1(지연 실측치) | unit-5 | 구현 완료(05단계, `backend/app/services/stt_engine.py` — faster-whisper `small`/CPU/int8 실제 로드·변환(스텁 아님), `backend/requirements.txt`에 `faster-whisper` 추가). 실제 한국어 음성(gTTS로 합성한 실제 발화 오디오, STT 테스트 전용 도구이며 런타임 의존성 아님)으로 curl 테스트해 `content_text`가 원문과 바이트 단위로 일치함을 확인(unit-5-note.md §6) | PASS(06단계, L1 경량판 — TC-002: 06단계가 독립 재현한 신규 세션에서 실제 faster-whisper 변환이 동작해 `content_text`가 원문과 부분일치(비어있지 않은 한국어 텍스트, "백엔드"를 "백앤드"로 인식하는 등 정상 오차범위 내)함을 재확인. TC-007: 디코딩 불가 바이트 입력 시 `SttTranscriptionError`->504 매핑 실측, `docs/harness/units/unit-5-test.md`) | | | Feature C. **Speed Track L1(DEC-003)** — 06 경량판 완료. STT는 실제로 동작함을 06단계가 독립 재현으로 재확인, AI 응답 생성(LLM, unit-7 범위)은 여전히 `enqueue_turn_job` 스텁. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(동시 다중 STT 요청 시 지연/큐잉은 unit-7 실제 워커 필요, 다양한 오디오 포맷(wav/webm 등)별 디코딩 성공은 06이 mp3 1종만 검증). 상세는 `docs/harness/units/unit-5-note.md`, `unit-5-test.md` 참고 |
| REQ-006 | [Must] 로컬 오픈소스 TTS(AI 면접관 음성 합성) (02-planning §6.1) | 03-system-design §2.5(Piper 선정, 실측 RTF 0.056, GPL-3.0 라이선스 리스크 및 격리 대응), §4.5(ITTSEngine 어댑터 인터페이스), §5.4(TTS 타임아웃 10초) | unit-6 | 구현 완료(05단계, `backend/app/services/tts_engine.py` — Piper(`ko_KR-kss-medium`, GPL-3.0-or-later 재확인) 실제 서브프로세스 연동(DEC-017 격리, 모델을 프로세스에 import하지 않음)으로 텍스트→WAV 합성이 실제 동작(스텁 아님), `backend/app/api/v1/interviews.py`의 `POST /interviews/{id}/tts-preview`(신규, §4.2 표 외 준비/검증 엔드포인트), `backend/app/main.py`의 `/media` 정적 서빙 마운트, `backend/requirements.txt`에 `piper-tts` 추가) | 06단계 대기 | | | Feature C. **Speed Track L1(DEC-003)**. 라이선스 주의 — GPL-3.0-or-later 재확인(DEC-017), 법무검토 필요(§8-2 미해결). **범위 편차(unit-6-note.md §2 참고)**: (1) `tts-preview`는 LLM(unit-7) 부재로 실제 `turn_result.audio_url`(§4.3) 파이프라인에는 연결되지 않은 준비용 엔드포인트 — unit-7이 `speak_text` 생성 시 동일한 `synthesize_speech_file()`을 재사용해 실제 턴 흐름에 연결해야 함(미완료 연결 지점, 인수인계). (2) 매 합성 요청이 서브프로세스 기동+모델 로드 비용(약 2~4초)을 포함해 STT의 "최초 1회만 느림" 패턴과 다름(§2 편차#3, 08단계 부하테스트 시 재확인 필요). **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(동시 다중 합성 요청 시 지연 누적, 다양한 텍스트 길이/특수문자 입력 견고성은 06단계가 아직 독립 재현하지 않음). 상세는 `docs/harness/units/unit-6-note.md` 참고 |
| REQ-007 | [Must] LLM 적응형 꼬리질문 생성 + RAG 질문은행 검색 (02-planning §4.2) | 03-system-design §2.4(Qwen2.5-Instruct GGUF 선정, DEC-016), §3(QUESTIONS.embedding/pgvector), §4.4(구조화 출력 JSON 계약) | unit-7 | Not Started | | | | Feature C |
| REQ-008 | [Must] 라이브 코딩 환경 — 에디터+제출/저장(실행 없음) (02-planning §4.2) | 03-system-design §2.1(Monaco Editor), §3(CODE_SUBMISSIONS 테이블), §4.2(`/code-submissions` POST/**GET(신규, 세션 재개 시 재조회, DEC-024 갭3)**), §6.3(제출값 새니타이즈, REQ-036과 동일기준) | unit-9 | 구현 완료(05단계, `backend/app/api/v1/code_submissions.py`(POST+GET), `backend/app/models/code_submission.py`, `backend/app/schemas/code_submission.py`(언어 화이트리스트), Alembic `b84a71b986c5`, `frontend/app/interviews/[id]/components/CodeEditorPanel.tsx`(Monaco 에디터, 독립 컴포넌트·미배선)) | PASS(06단계, L1 경량판 — 정상경로 2케이스: TC-001 python 코드 2회 제출(append-only, `id` 상이)+`GET`이 `submitted_at` 내림차순 정렬 확인, TC-002 제출 이력 없는 언어(`javascript`) 필터 조회 시 `200 []`(에러 아님) 확인, 06단계가 직접 새 서버 프로세스(포트 8033)+신규 격리 venv(`.harness-tmp/venv_06_unit9`, DEC-027)로 재현, `docs/harness/units/unit-9-test.md`) | | | Feature D. REQ-F-004 축소판. **Speed Track L1**(오케스트레이터 지정). 06 경량판 완료. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(화이트리스트 외 언어 422, 인증없음 401/존재하지않는 id 404, 타인세션 403, `CodeEditorPanel` 실제 브라우저 상호작용은 06이 아직 독립 재현하지 않음, 5단계 자체기록(unit-9-note.md §6-4)만 존재). 코드 실행 기능은 DEC-008에 따라 범위 밖. `content` 렌더링 시점 새니타이즈(§6.3/REQ-036)는 이 유닛 책임 아님 — 추후 렌더링 화면 구현 유닛이 적용 필요. `CodeEditorPanel`을 실제 면접장 화면(`page.tsx`)에 탭으로 배선하는 작업은 이번 유닛 범위 밖(추후 통합 유닛 필요). 06 세션 중 병렬 유닛(unit-5)의 `stt_engine`/`faster-whisper` 의존성 미완결로 서버 부팅이 일시적으로 막혔던 환경 이슈를 관찰·우회함(unit-9 결함 아님, unit-9-test.md §3/§6 참고). 상세는 `docs/harness/units/unit-9-note.md`, `unit-9-test.md` 참고 |
| REQ-009 | [Must] STAR 구조 기반 상세 피드백 리포트 자동 생성 (02-planning §4.2) | 03-system-design §3(EVALUATION_REPORTS.**star_json**(situation/task/action/result, v2 재작업 DEC-024 갭7) + `summary_text` 폴백), §4.2(`/report`, `/report/regenerate`), §4.4(리포트 생성 전용 구조화 출력 계약, 신규) | unit-10 | Not Started | | | | Feature E |
| REQ-010 | [Must] 루브릭 기반 채용 적합도 스코어링(1~5점) (02-planning §4.2) | 03-system-design §3(EVALUATION_REPORTS.technical/communication/cultural_fit_score), §4.2(`/report`) | unit-11 | Not Started | | | | Feature E |
| REQ-011 | [Should] 채용담당자용 리포트 열람 대시보드(최소 뷰어) (02-planning §4.2) | 03-system-design §1.2(Recruiter API), §4.2(`/recruiter/reports`), §6.1(RBAC — MVP 단일조직 정책, 세분화 없이 recruiter 전원 전체 열람) | unit-12 | 구현 완료(05단계, `backend/app/api/v1/recruiter.py`(`GET /recruiter/reports`, `GET /recruiter/reports/{interview_id}`), `backend/app/schemas/recruiter.py`, `frontend/app/recruiter/page.tsx`([R-01]), `frontend/app/recruiter/[id]/page.tsx`([R-02]), `frontend/app/recruiter/recruiter.module.css`) | PASS(06단계, L1 경량판 — 정상경로 2케이스: TC-003 recruiter 토큰 전체목록조회(200, 타 지원자 인터뷰 포함으로 §6.1 단일조직정책 실측 확인), TC-004 recruiter 토큰 리포트상세조회(200, report_available=false, 상태별 안내문구), 06단계가 직접 새 서버 프로세스(포트 8214, 06 전용 신규 venv)로 재현. 인수조건 1/2/4후반(401/403/404)도 저비용 추가 재현. `docs/harness/units/unit-12-test.md`) | | | Feature F. **Speed Track L1**(오케스트레이터 지정) — 06 경량판 완료. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(프론트 `/recruiter`, `/recruiter/[id]` 실제 브라우저 렌더링은 06단계도 도구 부재로 미검증 — 코드 레벨 대조와 ESLint/tsc 독립 재실행까지만 확인, unit-12-test.md TC-006 참고). **범위 편차(unit-12-note.md 참고)**: (1) 병렬 개발 파일충돌 회피 지시로 03-design §4.2가 지원자/recruiter 공유로 설계한 `GET /interviews/{id}/report`를 쓰지 않고 recruiter 전용 `GET /recruiter/reports/{interview_id}`를 신설 — Feature E(unit-10/11) 착수 시 중복 경로 정리 필요(인수인계). (2) `EVALUATION_REPORTS`(Feature E) 미착수로 리포트 본문 필드 없음, `report_available=false` 고정 + 상태별 안내 메시지만 반환(가짜 데이터 금지, unit-18 선례 준용) — `ready` 상태여도 동일. 상세는 `docs/harness/units/unit-12-note.md`, `unit-12-test.md` 참고 |
| REQ-012 | [Must] 평가 근거 노출(루브릭 적용 근거 최소 표시) (02-planning §4.2) | 03-system-design §3(EVALUATION_REPORTS.details_json), §4.2(`/report`), §6.3(details_json도 REQ-036 새니타이즈 대상, v2 재작업 명시) | unit-11 | Not Started | | | | Feature E. REQ-N-004(공정성/설명가능성) 최소 대응 |
| REQ-013 | [Must] 세션 중단/재접속 처리 규칙 (02-planning §4.2) | 03-system-design §3(INTERVIEWS.status=paused/expired), §4.2(`/resume`, **`/code-submissions` GET, `/whiteboard` GET — 신규, 재개 시 코드/화이트보드 복원, DEC-024 갭3·4**), §5.4(job idempotency), §5.3(가용성) | unit-3 | 구현 완료(05단계, `backend/app/api/v1/interviews.py` — `GET /interviews/{id}`(신규, 상세조회+`resumable` 파생필드), `POST /interviews/{id}/resume`. DEC-026로 SESSION_EXPIRED 임계값 24h 확정 및 `paused` 전이 트리거 메커니즘은 범위외로 보류) | PASS(06단계, L1 경량판 — 정상경로 2케이스: TC-001 scheduled 조회(resumable=false)+재개차단(409), TC-002 동의부여→start(202,live)→재접속GET(resumable=true)→resume 멱등(200,live)→DB로 paused 설정→GET(paused/resumable=true)→resume(200,live 복귀), 06단계가 직접 새 서버 프로세스(포트 8023)로 재현, `docs/harness/units/unit-3-test.md`) | | | Feature B. **Speed Track L1 확정**(DEC-003) — 06 경량판 완료. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(만료 410, completed 409, 수평권한상승 403, 인증없음 401, 존재하지않는 id 404는 06이 아직 독립 재현하지 않음, 5단계 자체기록만 존재). 도메인 특이케이스(01단계 §6). **범위 편차(unit-3-note.md §2 참고)**: (1) `GET /interviews/{id}/code-submissions`, `GET /interviews/{id}/whiteboard`는 CODE_SUBMISSIONS/WHITEBOARD_SNAPSHOTS 모델이 아직 없어 각각 unit-9/unit-17 책임으로 남김(미구현). (2) turn_index 등 실제 대화 콘텐츠 복원은 TRANSCRIPTS 모델이 없는 unit-4 이후 범위. (3) `paused` 상태로의 자동 전이(하트비트/WS 접속끊김 감지)는 신설하지 않음(DEC-026) — 현재는 재접속해도 `live`로 남아있는 세션을 그대로 조회/재개하는 경로만 지원. 상세는 `docs/harness/units/unit-3-note.md`, `unit-3-test.md` 참고 |
| REQ-014 | [Should] 채용담당자 질문지/루브릭 최소 커스터마이징 (02-planning §4.2) | 03-system-design §3(RUBRIC_TEMPLATES), §4.2(`/recruiter/rubric-templates` GET/POST/PATCH) | unit-13 | 구현 완료(05단계, `backend/app/models/rubric_template.py`(RUBRIC_TEMPLATES), `backend/app/schemas/rubric_template.py`, `backend/app/api/v1/recruiter.py`에 `GET/POST /recruiter/rubric-templates`, `PATCH /recruiter/rubric-templates/{id}` 추가(unit-12 소유 파일에 additive 확장), Alembic `d3f7a2c9e1b4_v7_rubric_templates`(테이블 신설 + `interviews.rubric_template_id` FK 제약 추가), `frontend/app/recruiter/rubric-templates/page.tsx`([R-03] 신규), `frontend/app/recruiter/page.tsx`에 메뉴 링크 추가, `frontend/lib/api.ts` 확장) | PASS(06단계, L1 경량판 — 정상경로 3케이스: TC-003 템플릿생성+recruiter_id 본인일치 확인, TC-004 목록조회 반영확인, TC-005 본인템플릿 PATCH 갱신확인, 06단계가 직접 새 서버 프로세스(포트 8299)+신규 격리venv(`.harness-tmp/venv_06_unit13`)+신규 계정 3명으로 독립 재현, 인수조건1~8 전건 TC-001~008 1:1대응, 결함 0건, `docs/harness/units/unit-13-test.md`) | | | Feature F. **Speed Track L1**(오케스트레이터 지정). **범위 해석(unit-13-note.md 참고)**: 03/04 설계서에 `/recruiter/questions` 같은 별도 질문은행 커스터마이징 API가 정의되어 있지 않아(QUESTIONS 테이블은 Feature C 소유, RUBRIC_TEMPLATES.criteria_json 항목이 "질문지"에 해당하는 평가기준 단위) 설계서에 명시된 `/recruiter/rubric-templates` GET/POST/PATCH만 구현. 목록 조회는 본인 소유 + 시스템 기본(`recruiter_id IS NULL`) 템플릿으로 한정(타 recruiter의 개인 커스텀 템플릿은 비공개) — `/recruiter/reports`의 단일조직 전체열람 정책과는 별개 해석. 시스템 기본 템플릿 시드 데이터는 이번 유닛에서 만들지 않음(03-design §3.3 "시드는 마이그레이션과 분리" 원칙에 따라 별도 시드 스크립트 필요, 후속 인수인계). **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(프론트 실제 브라우저 렌더링 미검증, weight 합계 검증 부재, 시스템 기본 템플릿 시드 데이터 부재는 06단계도 재확인만 하고 해소하지 않음). 상세는 `docs/harness/units/unit-13-note.md`, `unit-13-test.md` 참고 |
| REQ-015 | [Should] 웹캠 영상 프리뷰(AI 분석 없음) (02-planning §4.2) | 03-system-design §1.2(클라이언트 전용, 서버 미전송), §6.2(수집최소화 원칙과 직결) | unit-16 | 구현 완료(05단계, `frontend/components/WebcamPreview.tsx`(신규, `getUserMedia` 기반 로컬 미리보기 컴포넌트, 서버 전송 코드 없음). 04-ux-design §4 `WebcamPreviewTile` 상태 4종(`active/permission-denied/off/unsupported`)+로딩/일반오류 상태 포함 6종 구현) | PASS(06단계, L1 경량판 — 정상경로/안전성 2케이스: TC-001 `variant="panel"`/`"tile"` 초기 off 상태 렌더링(tile 160×120 고정 크기 포함) HTTP 200 확인, TC-002 `autoStart=true` SSR 환경(`navigator` 없음)에서 크래시 없이 `unsupported`로 안전 폴백 확인 — 둘 다 06단계가 전용 임시 라우트(`frontend/app/wcpreview-check-unit16-06/`)로 새로 재현(05단계 기록 미승계), `docs/harness/units/unit-16-test.md`) | | | Feature H. **Speed Track L1 확정**(DEC-003) — 06 경량판 완료. **DEF-001(Medium, Deferred, unit-16-test.md §6)**: `autoStart=true`일 때 SSR 결과(`unsupported`)와 카메라 지원 브라우저의 client 첫 렌더 결과(`requesting` 예상)가 달라 hydration mismatch가 발생할 가능성을 06단계가 발견(SSR 출력 차이는 curl로 실측, 실제 브라우저 경고 발생 여부는 자동화 도구 부재로 미확정) — 아직 어떤 화면에도 wiring되지 않아 현재 라이브 영향은 없음. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(실제 브라우저 카메라 권한 허용/거부/미지원 전이, 언마운트 시 트랙 정지의 라이브 확인, 콘솔 경고·네트워크 탭 라이브 확인, DEF-001 최종 확정은 06이 아직 독립 재현하지 않음). 아직 [C-05]/[C-06] 메인 화면에 wiring되지 않음(통합 담당 유닛 몫, `docs/harness/units/unit-16-note.md` §0/§4 참고). 상세는 `docs/harness/units/unit-16-note.md`, `unit-16-test.md` 참고 |
| REQ-016 | [Should] 운영자 기본 모니터링(세션 수, 에러 로그) (02-planning §4.2) | 03-system-design §4.2(`/ops/health`), §7.2(Prometheus/Grafana 지표) | unit-18 | 구현 완료(05단계, `backend/app/api/v1/ops.py`(`GET /ops/health`, admin 역할 전용), `backend/app/schemas/ops.py`, `frontend/app/admin/ops/page.tsx`([O-01] 4개 지표 카드+최근갱신시각, 5~10초 폴링, 에러 시 마지막 성공값 유지). `active_sessions`만 `INTERVIEWS.status='live'` 실측, `queue_length`/`gpu_memory_used_bytes`/`error_rate`는 근거 시스템(큐/GPU워커/Prometheus) 미구축으로 상시 0 — 응답 `notes` 필드에 실측/스텁 여부 명시(가짜 데이터 금지 원칙 준수)) | PASS(06단계, L1 경량판 — 정상경로 2케이스: TC-001 인증없음401/candidate403/admin200+스키마(4지표+notes 4키), TC-002 candidate 세션생성→동의→start(202,live) 후 admin `/ops/health` 재호출 시 `active_sessions` 직전 대비 델타 +1 확인(베이스라인 자체가 병렬 작업단위의 기존 live 세션으로 1이었음을 발견해 절대값이 아닌 델타로 검증), 06단계가 직접 새 서버 프로세스(포트 8060)로 재현, `docs/harness/units/unit-18-test.md`) | | | Feature H. **Speed Track L1 확정**(DEC-003) — 06 경량판 완료. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(프론트 `/admin/ops` 화면의 실제 렌더링 — 권한없음 안내/스켈레톤/지표카드/갱신실패배지 상태전이는 06이 브라우저 자동화 도구 부재로 코드 리뷰(타입 계약 일치)까지만 확인, 실 렌더링 독립 재현 아직 없음). 상세는 `docs/harness/units/unit-18-note.md`, `unit-18-test.md` 참고 |
| REQ-017 | [Could] 시스템 설계 화이트보드 캔버스(AI 자동분석 없음) (02-planning §4.2) | 03-system-design §3(WHITEBOARD_SNAPSHOTS), §4.2(`/whiteboard` PUT/**GET(신규, 세션 재개 시 재조회, DEC-024 갭4)**) | unit-17 | 구현 완료(05단계, `backend/app/models/whiteboard.py`, `backend/app/schemas/whiteboard.py`, `backend/app/api/v1/whiteboard.py`(`PUT`/`GET /interviews/{id}/whiteboard`), Alembic `c1a2f5e9b7d3_v5_whiteboard_snapshots`, `frontend/app/interviews/[id]/components/WhiteboardCanvas.tsx`(독립 컴포넌트, 메인 화면 미삽입)) | PASS(06단계, L1 경량판 — 정상경로 2케이스: TC-001 저장 전 GET(200 null), TC-002 PUT(stroke 1개, 200)→GET(즉시 반영)→PUT(다른 stroke, 새 id 발급)→GET(최신만 반환)+DB 직접 조회로 이전 스냅샷 행이 이력으로 남아있음까지 확인, 06단계가 격리 venv(DEC-027, `.harness-tmp/venv_06_unit17`)로 새 서버 프로세스(포트 8071)를 직접 기동해 재현, note-코드 일치 및 게이트1(ruff/eslint) 재확인도 완료, `docs/harness/units/unit-17-test.md`) | | | Feature H. **Speed Track L1 확정**(DEC-003) — 06 경량판 완료. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(401/403/404/422 에러경로는 5단계 자체 curl 기록만 있고 06단계가 아직 독립 재현하지 않음, 프론트 `WhiteboardCanvas` 컴포넌트의 실제 브라우저 렌더링/마우스·터치 드로잉/저장버튼 활성상태 전환은 06단계가 브라우저 자동화 도구 부재로 코드 리뷰까지만 확인, 실 렌더링 독립 재현 아직 없음). REQ-021(화이트보드 AI 비전 분석)은 Out-of-Scope 그대로 유지, 이번 유닛 코드에 분석 로직 없음(DEC-008, 06단계가 grep으로 재확인). 상세는 `docs/harness/units/unit-17-note.md`, `unit-17-test.md` 참고 |

## 규제 대응 요구사항 (규칙 I — 개인정보보호법 생체정보·자동화된 결정)

| REQ-ID | 요구사항/기능 | 설계 매핑 | 작업 단위 | 구현 상태 | 단위테스트 | 통합테스트 | 전체테스트(08) | 비고 |
|---|---|---|---|---|---|---|---|---|
| REQ-029 | [Must] 생체정보(음성) 수집 전 별도 명시적 동의 절차 + 미동의시 이용 제한 안내 | **[v2 재작업, DEC-023/024]** 03-system-design §3(CONSENTS 테이블), §4.2(`POST /consents`, `GET /users/me/consents`(신규, 갭1), **`POST /interviews/{id}/turns` 음성 제출 시 매 요청 실시간 재검사**), §6.2(동의 게이트를 `/start`에서 `/turns`(음성 제출 시점)로 이동 — 텍스트 전용 이용자는 더 이상 이 동의를 강제받지 않음, 미동의/철회 시 403 CONSENT_REQUIRED_VOICE, job 자체를 큐에 넣지 않고 오디오도 저장하지 않음) | unit-14 | 구현 완료(05단계, `backend/app/api/v1/consents.py` — `POST /consents`, `GET /users/me/consents`. 기존 `backend/app/models/consent.py`(unit-2가 최소 스키마로 선행 생성)를 그대로 재사용, 신규 스키마 없음) | PASS(06단계, L1 경량판 — 정상경로 2케이스: TC-001 `POST /consents`(ai_interview_notice) 등록→`GET /users/me/consents` 이력반영→`/start` 게이트 실통합(동의전 403→동의후 202)까지 06단계가 직접 새 서버 프로세스(포트 8051)로 처음부터 재현, `docs/harness/units/unit-14-test.md`) | | | Feature G. **Speed Track L1 확정**(DEC-003). **음성(`biometric_voice`) 게이트를 실시간 소비하는 `/turns` multipart 제출 자체가 아직 없음(unit-5, REQ-004/005 범위)** — 이번 유닛은 그 게이트가 참조할 `CONSENTS` 레코드의 생성/조회 API 표면만 완성했고, `403 CONSENT_REQUIRED_VOICE` 실제 반환은 unit-5가 `/turns` 음성 경로를 만들 때 검증 필요. `ai_interview_notice` 경로는 unit-2의 `/start` 게이트와 curl로 실통합 검증 완료. **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(재철회 409, 존재하지 않는 id 404, 타인 소유 403(수평 권한상승), 잘못된 enum 422, 인증없음 401은 06단계가 아직 독립 재현하지 않음, 5단계 자체기록만 존재). 상세는 `docs/harness/units/unit-14-note.md`, `unit-14-test.md` 참고 |
| REQ-030 | [Must] 동의 철회 및 수집 데이터 즉시 삭제 요청 기능 | 03-system-design §3(DELETION_REQUESTS 테이블), §4.2(`/consents/{id}/revoke`, `DELETE /users/me/biometric-data`, **`GET /users/me/deletion-requests`(신규, 처리상태 조회, DEC-024 갭2)**), §6.2(SLA 24h 배치 파기잡 + **철회 시 다음 음성 제출부터 서버가 실제로 차단하는 실시간 재검사 메커니즘 명시, DEC-023 — 단순 플래그 저장이 아님**) | unit-14 | 구현 완료(05단계, `backend/app/api/v1/consents.py` — `POST /consents/{id}/revoke`, `DELETE /users/me/biometric-data`, `GET /users/me/deletion-requests`. 신규 `backend/app/models/deletion_request.py`(ERD `DELETION_REQUESTS` 그대로, `status` enum은 ERD 원문대로 `pending`/`completed` 2값만), Alembic `8a55fda78a42_v4_deletion_requests`(down_revision=unit-3의 `0df1434883f2`, 실제 DB에 upgrade 적용 완료)) | PASS(06단계, L1 경량판 — 정상경로 2케이스: TC-002 `POST /consents/{id}/revoke`(200,revoked_at 채워짐)→`DELETE /users/me/biometric-data`(202,target=biometric_only/status=pending)→`GET /users/me/deletion-requests` 이력반영까지 06단계가 직접 재현, `docs/harness/units/unit-14-test.md`) | | | Feature G. **실제 하드 삭제(파기) 배치("delete_requested_data" Celery beat 잡, 03-design §6.2)는 이번 유닛 범위 밖 — REQ-033/unit-15 책임**(traceability REQ-033 행). 이번 유닛은 `DELETION_REQUESTS`를 `pending`으로 접수하는 것까지만 구현(상태를 `completed`로 전이시키는 로직은 unit-15가 그 배치를 만들 때 추가). **L1 부채 — 07(및 06 정식화) 08 착수 전 정산 필요**(재철회 409, 존재하지 않는 id 404, 타인 소유 403(수평 권한상승), 삭제요청 target/status 잘못된 값 등 예외경로는 06단계가 아직 독립 재현하지 않음, 5단계 자체기록만 존재). 상세는 `docs/harness/units/unit-14-note.md`, `unit-14-test.md` 참고 |
| REQ-031 | [Must] "AI는 보조 도구, 최종 결정은 인간이 함" 원칙 명시 + 자동 최종판정 기능 미제공 | 03-system-design §3(EVALUATION_REPORTS.overall_recommendation을 recommend/neutral/not_recommend 3등급 enum으로 제한, "합격/불합격 확정" 필드 스키마 자체에 미생성 — §3.2 원계획서 대비 변경점), §6.2(모든 리포트 응답에 고정 문구) | unit-15 | 부분 구현(05단계, L1) — 원칙 문구 노출부만 구현: `frontend/app/interviews/[id]/consent/page.tsx`(사전고지 필수동의 문구), `frontend/app/legal/page.tsx`(법률고지 문구)에 "AI는 보조 도구, 최종 채용 결정은 사람이 내린다" 원칙 텍스트 고정 노출(`frontend/lib/complianceContent.ts`). 03-design이 요구하는 EVALUATION_REPORTS.overall_recommendation 3등급 enum 스키마 강제(자동판정 필드 미생성)는 EVALUATION_REPORTS 테이블 자체가 아직 없어(unit-10/11 미착수, `backend/app/schemas/recruiter.py` 주석 참고) 구현 대상이 부존재 — 해당 테이블 생성 시 재확인 필요. 상세는 `docs/harness/units/unit-15-note.md` 참고 | PASS(06단계 회귀 재검증, L1 경량판 — 1차 라운드 FAIL(DEF-001, `LEGAL_DISCLAIMER_TEXT`에 보관기간 180일 임시값 문장 누락) 이후 05단계 재작업 완료. 06단계가 TC-003을 `grep -n "180\|임시값" complianceContent.ts`로 독립 재실행해 `LEGAL_DISCLAIMER_TEXT`(59행)에 해당 문장이 신규 추가됐음을 원문 대조로 확인, DEF-001 Fixed 확정. TC-001/TC-002(동의 플로우·마이페이지 동의관리, 1차 라운드 PASS)는 이번 수정 범위(문자열 상수 1개) 밖임을 diff/mtime으로 확인해 재실행 생략. `npm run lint`(0 errors)/`tsc --noEmit`(0 errors)/`npm run build`(성공) 06단계 독립 재확인 완료. `docs/harness/units/unit-15-test.md` 최종본 참고) | | | 개인정보 보호법 제37조의2(자동화된 결정 거부권·설명요구권, 2024-03-15 시행) 대응. 9단계에서 미반영 시 Critical 결함 **06단계 FAIL(DEF-001) → 05단계 재작업 완료 → 06단계 회귀 재검증 PASS 확정(2026-09-19)**. **L1 부채 — 07·(06 정식화) 08 착수 전 정산 필요**(인수조건 #1/#4의 실제 브라우저 UI 자동화 검증, #10의 예외경로(재철회 409/존재하지 않는 id 404/타인 소유 403)는 06단계가 아직 독립 재현하지 않음)(`unit-15-test.md`, `unit-15-note.md` §7 참고). |
| REQ-032 | [Must] AI 면접 진행/평가 사실 사전 고지 화면 | 03-system-design §4.2(`GET /interviews/{id}/pre-notice`, `POST /interviews/{id}/start`(v2: `ai_interview_notice` 동의를 유일한 세션 시작 게이트로 검사, DEC-023)), §6.2(세션 시작 버튼은 고지 확인 체크박스 선행 — 04단계 UI 반영 필요 사항으로 명시 인수인계) | unit-15 | 구현 완료(05단계, L1) — `frontend/app/interviews/[id]/consent/page.tsx`([C-04] 사전고지 화면: 고지문 스크롤 완독 시에만 필수 체크박스 활성화 → `POST /consents`(`ai_interview_notice`, unit-14 API) 등록 → `POST /interviews/{id}/start`(unit-2) 순으로 호출, curl로 3개 호출 모두 재확인). **범위 편차**: 03-design의 `GET /interviews/{id}/pre-notice` 엔드포인트는 어떤 유닛도 구현하지 않았고 이번 유닛은 `interviews.py` 등 백엔드 파일 수정이 범위 밖이라, 고지문은 `frontend/lib/complianceContent.ts`의 정적 텍스트로 대체. 상세는 `docs/harness/units/unit-15-note.md` 참고 | PASS(06단계 회귀 재검증, L1 경량판 — 1차 라운드 FAIL(DEF-001, `LEGAL_DISCLAIMER_TEXT`에 보관기간 180일 임시값 문장 누락) 이후 05단계 재작업 완료. 06단계가 TC-003을 `grep -n "180\|임시값" complianceContent.ts`로 독립 재실행해 `LEGAL_DISCLAIMER_TEXT`(59행)에 해당 문장이 신규 추가됐음을 원문 대조로 확인, DEF-001 Fixed 확정. TC-001/TC-002(동의 플로우·마이페이지 동의관리, 1차 라운드 PASS)는 이번 수정 범위(문자열 상수 1개) 밖임을 diff/mtime으로 확인해 재실행 생략. `npm run lint`(0 errors)/`tsc --noEmit`(0 errors)/`npm run build`(성공) 06단계 독립 재확인 완료. `docs/harness/units/unit-15-test.md` 최종본 참고) | | | 채용절차법 개정 동향(고지의무 강화) 대응 **06단계 FAIL(DEF-001) → 05단계 재작업 완료 → 06단계 회귀 재검증 PASS 확정(2026-09-19)**. **L1 부채 — 07·(06 정식화) 08 착수 전 정산 필요**(인수조건 #1/#4의 실제 브라우저 UI 자동화 검증, #10의 예외경로(재철회 409/존재하지 않는 id 404/타인 소유 403)는 06단계가 아직 독립 재현하지 않음)(`unit-15-test.md`, `unit-15-note.md` §7 참고). |
| REQ-033 | [Must] 데이터 보관기간 정책 + 자동/요청 삭제 라이프사이클 | 03-system-design §6.2(음성원본=STT완료 즉시삭제, transcript/리포트=기본180일 임시값·법률자문 확인필요), §7.4(Celery beat 배치 파기잡) | unit-15 | 부분 구현(05단계, L1) — 보관기간 정책 문구(음성원본 STT완료 즉시삭제, transcript/리포트 180일 임시값)는 사전고지 화면에 노출, 동의 철회/삭제요청 UI는 `frontend/app/mypage/page.tsx`([C-13])에서 unit-14 API(`POST /consents/{id}/revoke`, `DELETE /users/me/biometric-data`, `GET /users/me/deletion-requests`)를 소비해 구현(curl로 재확인 완료). **범위 편차**: 03-design §7.4가 요구하는 실제 자동 파기 Celery beat 배치 잡("delete_requested_data")은 이번 세션이 백엔드 파일을 수정할 수 없는 범위 제약으로 미구현 — `DELETION_REQUESTS`가 `pending`에서 `completed`로 전이하는 로직 없음(unit-14-note.md도 이 배치를 REQ-033/unit-15 후속 책임으로 명시). 180일 보관기간은 법률자문 미확정 임시값임을 화면에 명시(숨기지 않음). 상세는 `docs/harness/units/unit-15-note.md` 참고 | PASS(06단계 회귀 재검증, L1 경량판 — 1차 라운드 FAIL(DEF-001, `LEGAL_DISCLAIMER_TEXT`에 보관기간 180일 임시값 문장 누락) 이후 05단계 재작업 완료. 06단계가 TC-003을 `grep -n "180\|임시값" complianceContent.ts`로 독립 재실행해 `LEGAL_DISCLAIMER_TEXT`(59행)에 해당 문장이 신규 추가됐음을 원문 대조로 확인, DEF-001 Fixed 확정. TC-001/TC-002(동의 플로우·마이페이지 동의관리, 1차 라운드 PASS)는 이번 수정 범위(문자열 상수 1개) 밖임을 diff/mtime으로 확인해 재실행 생략. `npm run lint`(0 errors)/`tsc --noEmit`(0 errors)/`npm run build`(성공) 06단계 독립 재확인 완료. `docs/harness/units/unit-15-test.md` 최종본 참고) | | | 개인정보 보호법 일반 원칙 대응 **06단계 FAIL(DEF-001) → 05단계 재작업 완료 → 06단계 회귀 재검증 PASS 확정(2026-09-19)**. **L1 부채 — 07·(06 정식화) 08 착수 전 정산 필요**(인수조건 #1/#4의 실제 브라우저 UI 자동화 검증, #10의 예외경로(재철회 409/존재하지 않는 id 404/타인 소유 403)는 06단계가 아직 독립 재현하지 않음)(`unit-15-test.md`, `unit-15-note.md` §7 참고). |
| REQ-034 | [Must] 실제 외부 공개 전 법률/컴플라이언스 자문 권고 문구 고정 노출 | 03-system-design §4.2(`GET /legal/disclaimer`), §6.2(모든 리포트 화면 하단 노출), §8(전 트레이드오프 항목의 "확인 필요" 표기 방식 자체가 이 원칙의 실천) | unit-15 | 부분 구현(05단계, L1) — `frontend/app/legal/page.tsx`([C-14] 법률/컴플라이언스 고지 화면, 로그인 여부 무관 항상 열람 가능)에 법률자문 권고 문구 고정 노출, 사전고지 화면 하단에 링크 연결. **범위 편차**: 03-design의 `GET /legal/disclaimer` 엔드포인트는 미구현(신규 백엔드 엔드포인트 추가는 이번 세션 범위 밖)이라 고정 법정 고지문 성격에 맞춰 정적 페이지로 대체. 운영 관리자 화면·11단계 인수인계 문서에의 재수록은 각각 별도 유닛(운영자 화면) 및 11단계 소관으로 미착수. 상세는 `docs/harness/units/unit-15-note.md` 참고 | PASS(06단계 회귀 재검증, L1 경량판 — 1차 라운드 FAIL(DEF-001, `LEGAL_DISCLAIMER_TEXT`에 보관기간 180일 임시값 문장 누락) 이후 05단계 재작업 완료. 06단계가 TC-003을 `grep -n "180\|임시값" complianceContent.ts`로 독립 재실행해 `LEGAL_DISCLAIMER_TEXT`(59행)에 해당 문장이 신규 추가됐음을 원문 대조로 확인, DEF-001 Fixed 확정. TC-001/TC-002(동의 플로우·마이페이지 동의관리, 1차 라운드 PASS)는 이번 수정 범위(문자열 상수 1개) 밖임을 diff/mtime으로 확인해 재실행 생략. `npm run lint`(0 errors)/`tsc --noEmit`(0 errors)/`npm run build`(성공) 06단계 독립 재확인 완료. `docs/harness/units/unit-15-test.md` 최종본 참고) | | | 규칙 I-4 — 에이전트가 법적 판단을 대신하지 않음을 명시. 1차 구현은 unit-15(운영 관리자 화면), 11단계 인수인계 문서(`11-ops-handoff-runbook.md`)에도 동일 문구를 재수록해 이중 노출한다(제거 대상 아님) **06단계 FAIL(DEF-001) → 05단계 재작업 완료 → 06단계 회귀 재검증 PASS 확정(2026-09-19)**. **L1 부채 — 07·(06 정식화) 08 착수 전 정산 필요**(인수조건 #1/#4의 실제 브라우저 UI 자동화 검증, #10의 예외경로(재철회 409/존재하지 않는 id 404/타인 소유 403)는 06단계가 아직 독립 재현하지 않음)(`unit-15-test.md`, `unit-15-note.md` §7 참고). |

## AI/LLM 안전장치 요구사항 (규칙 J)

| REQ-ID | 요구사항/기능 | 설계 매핑 | 작업 단위 | 구현 상태 | 단위테스트 | 통합테스트 | 전체테스트(08) | 비고 |
|---|---|---|---|---|---|---|---|---|
| REQ-035 | [Must] 프롬프트 인젝션 방어(사용자 입력이 시스템 프롬프트를 덮어쓰거나 우회 못하게 하는 입력 경계 설계) | 03-system-design §6.3(role 분리 + 1차 패턴 필터링, 잔여리스크 명시), §4.4(구조화 출력 강제) | unit-8 | Not Started | | | | 09단계 인젝션 취약점 점검과 동일하게 Critical 취급 |
| REQ-036 | [Must] LLM 출력 새니타이즈(채팅/리포트 렌더링 시 HTML 이스케이프, XSS 방지) | 03-system-design §6.3(서버측 HTML escape+화이트리스트 마크다운, 클라이언트 DOMPurify 2중방어. **[v2 재작업, DEC-024 갭7] 대상 필드 목록에 `EVALUATION_REPORTS.star_json`/`summary_text`/`details_json` 명시적 포함** — v1은 `speak_text`/`key_observations`만 명시해 리포트 필드가 누락되어 있었음) | unit-8 | Not Started | | | | 코드 제출값(REQ-008)도 동일 기준 적용 |
| REQ-037 | [Must] LLM 도구/함수 호출 최소 권한(시스템 제어 신호는 화이트리스트 명령만 허용) | 03-system-design §6.3(function-calling 권한 미부여, RAG는 읽기전용 컨텍스트 주입), §4.3(`control` enum 화이트리스트) | unit-8 | Not Started | | | | 원안 5.1.2 "시스템 제어 신호" 확장 금지 |
| REQ-038 | [Must] 요청 빈도/리소스 사용량 레이트리밋(세션/사용자당 LLM 호출 제한 + GPU 4GB 제약 고려한 동시 추론 큐잉) | 03-system-design §1.3(큐 최대길이 50, 단일 GPU워커 직렬화), §6.3(세션당/일일 상한) | unit-8 | Not Started | | | | 02-planning §6.1, §7 리스크 2 연결 |
| REQ-039 | [Must] 시스템 프롬프트 유출 방지(유출 시도 탐지·거부) | 03-system-design §6.3(출력 후처리 n-gram 유사도 검사+AUDIT_LOGS 기록, 시스템프롬프트 자체 거절지침 포함) | unit-8 | Not Started | | | | |

## Out-of-Scope (사유 포함)

| REQ-ID | 기능 | 원 계획서 근거 | 제외 사유 |
|---|---|---|---|
| REQ-018 | 표정 기반 감정분석(DeepFace 7종 실시간 분석) | REQ-F-002 | GPU 4GB 제약, EU AI Act 감정인식 금지 관행 논의, 국내 생체인식정보 리스크, L1 트랙 부적합 (02-planning §4.3) |
| REQ-019 | 음성 운율(Prosody) 분석 | 원안 5.3.2 | REQ-018과 동일 사유 |
| REQ-020 | 실시간 자동 끼어들기(VAD 기반 turn-taking) | REQ-F-003 | 구현 난이도, 하드웨어 제약 — REQ-004(턴제)로 대체 |
| REQ-021 | 화이트보드 AI 비전 자동분석(GPT-4V 등) | REQ-F-005 | 오픈소스 대안의 로컬 구동 검증 안 됨, 예산 없음. 캔버스(REQ-017)는 유지 |
| REQ-022 | 코드 실제 실행/자동채점(샌드박스) | REQ-F-004 | 임의 코드 실행 보안위험, L1 일정상 샌드박스 구축 여력 없음 |
| REQ-023 | 동시접속 500명, K8s 오토스케일링 | REQ-N-002 | DEC-006으로 50명/단일 인스턴스 대체 확정 |
| REQ-024 | Oracle DB | 원안 6.1 | DEC-004로 PostgreSQL 대체 확정 |
| REQ-025 | Pinecone 벡터DB | 원안 4.1 | DEC-005로 pgvector/Chroma 대체 확정 |
| REQ-026 | 유료 API(OpenAI/Deepgram/Hume AI/ElevenLabs) | 원안 4.1, 5.1~5.2 | DEC-005로 오픈소스 대체 확정 |
| REQ-027 | 클라우드 배포(AWS/GCP/EKS) | 원안 4.1 | DEC-007로 로컬서버 확정 |
| REQ-028 | GDPR/CCPA 완전 대응 | REQ-N-003 | 국내(한국어/한국 채용시장) 대상 가정(A5) — 한국 개인정보보호법 대응(REQ-029~033)으로 대체 |

## 작성 규칙 (템플릿 원문)
- REQ-ID는 2단계에서 기획서의 기능 범위(In-Scope) 항목마다 하나씩 부여했다. 이후 단계에서 새 ID를 임의로 추가하지 않고, 새 요구사항이 필요하면 규칙 A에 따라 질문하거나 규칙 F(피드백 루프)로 상위 단계를 수정한다.
- Out-of-Scope 항목은 매트릭스에 포함하되 상태를 표기하지 않고(해당 없음) 사유만 명시해, "빠뜨린 것"과 "의도적으로 뺀 것"을 구분한다.
- 8단계에서 이 매트릭스를 검토해 커버리지 100%를 확인하지 못하면 PASS 판정할 수 없다.
- **L1 부채 관련**: 각 In-Scope REQ-ID가 속한 feature가 Speed Track L1로 진행되면(기본값, DEC-003), 07(통합테스트) 컬럼은 "L1 부채 — 08 착수 전 정산 필요"로 표기하고 5단계 착수 시점에 채운다(현재 02단계에서는 아직 트랙 확정 전이므로 비고에만 예고 표기).
